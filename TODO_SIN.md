# Sin — tasks and agent work log

Record all agent work here when working for Sin. Include status, file paths, verification, and blockers or handoff notes.

## MUST DO — Shared room test download

- [ ] **Prepare and verify the shared Haussmann room locally** — required for each collaborator.
- Use [HAUSSMANN APARTMENT](https://superspl.at/scene/4de797f4) as the shared Gaussian room test download. SuperSplat login may be required. Credit Stéphane Agullo (sa3d), CC BY 4.0; retain the downloaded license. This is an authored test scene with assumed scale, not a measured room.

The web app now downloads the Gaussian room automatically in the background when `/rooms` opens if local visual files are missing; see [browser download behavior](docs/frontend/room-capture/haussmann-apartment.md#automatic-browser-download). The manual workflow below remains available for the original ZIP and optional surface-reference GLB.

After downloading, keep the ZIP intact and run these commands from the repository root after saving current work. The `&&` chain stops at the first failure:

```bash
git switch main &&
git pull --ff-only &&
./setup.sh &&
python3 scripts/gaussian_room/prepare_haussmann.py "$HOME/Downloads/HAUSSMANN APARTMENT.zip" &&
python3 scripts/gaussian_room/review_surface.py &&
BACKEND_PORT=8222 FRONTEND_PORT=5222 ./run-local.sh
```

Open **[Haussmann directly](http://localhost:5222/?roomId=haussmann-apartment)**, or `http://localhost:<FRONTEND_PORT>/?roomId=haussmann-apartment` if using another frontend port. The homepage opens the entrance/gallery; Haussmann is now the first entry in `shared/public-rooms.json` and can also be opened through **Enter space** while its browser download completes.

Adjust the ZIP path if needed. Run preparation in a normal terminal for GPU access; the previously reported run took about 2½ minutes. Preparation is a one-time step and refuses to overwrite existing output. If already prepared, skip preparation and run the surface review and stack commands. Generated room assets are ignored by Git, so each collaborator must prepare them locally. Keep the attribution/license. This remains an experimental test room with assumed scale and known downward/ceiling/window artifacts. Stop the stack with Ctrl-C when finished.

Tracking update (2026-09-20): standardized this MUST DO checklist across `TODO_SIN.md`, `TODO_SAKETH.md`, `TODO_WILLIAM.md`, and `TODO_ADELLE.md`; synchronized owner-specific Kanban tasks. Checked the default `/` route in `frontend/src/App.tsx`. Local preparation and room verification remain pending for each collaborator; this documentation update does not claim they ran.

## In progress

- 2026-09-20: **Download missing Haussmann assets automatically in the web app — local main merge**: user authorized an immediate merge with minimal checks and fixes only for merge conflicts. Commit only the reviewed Haussmann/gallery/download changes; preserve the newer oak-platform-bed commit on local main and unrelated untracked demo/script files. Reuse the recorded 210 + 26 passing tests, production builds and browser QA; do not rerun suites or fetch/push for this merge.

- 2026-09-20: Building isolated cart animation on codex/shopping-cart-animation. User selected an Atelier origami bird and demo fulfillment. Higgsfield job d463f33f-1663-4104-aea3-ce6fee86f1fe completed and bundled (six seconds, approximately 2.4 MB); implemented lazy cart preview, reusable presentation trigger, replay, reduced motion and media failure handling. No payment/backend mutation. Browser playback/replay/skip, zero-item cart, failed-media recovery, programmatic trigger and no-API-call checks passed; desktop/mobile inspected. Subagent implemented automatic PlayCanvas materialization on newly accepted additions plus selected-object Preview summon button, presentation-only transforms and static capture geometry. Real browser GLB placement and summon replay passed. Source paths: frontend/src/checkout/{CartPreview.tsx,cart-preview.css,previewOrderDispatch.ts}, App.tsx, SplatEditor.tsx, scene/playcanvas/{furniture.ts,materialize.ts,materialize.test.ts}; both docs/frontend/{shopping-cart-animation,furniture-materialization}.md indexed. Final checks passed: 127 scene tests + 16 packaging/auth tests, production build (existing engine chunk warning and optional unprovisioned-room warnings), diff whitespace and both documentation index links. No commit or push. User requests cheaper Higgsfield models for any future generation; no further generation submitted.

- 2026-09-20: Resumed Evaluate Gaussian splats and surface reconstruction on codex/haussmann-quality from main1424ac2. Parallel agents inspect capture readiness, performance and explicit metric evidence; root owns shared renderer edits and browser QA. Reproduce matched problem views before changing compression or resolution. Retain assumed-scale label until an independent dimension is established. Existing Sin Kanban card remains incomplete.

- Gaussian room evaluation (Sin; Kanban `68a22eb5-dc7f-4cc5-8ee2-d248bd8369ee`) remains incomplete. Actual CC BY 4.0 HAUSSMANN asset is acquired and integrated as an opt-in test fixture; acquisition is no longer blocked. Down/ceiling/window capture smearing is fixed on codex/haussmann-quality; final full-resolution motion measured 53.5 FPS / p95 26.2 ms over 30 s at 1280 × 720, warm-ready 569 ms. Keep the mesh fallback and assumed-scale label; author page provides no usable metric calibration. See docs/frontend/room-capture/haussmann-apartment.md for source, transforms, geometry, coverage evidence and limitations. User authorized merging the experimental fixture and test-download note in all four TODOs; merged into local main at ae03845 after reconciling fetched origin/main; post-merge verification passed. No push.

- Working branch is codex/gaussian-splatting, created from the merged Three.js baseline on main. The PlayCanvas migration has not started. Incoming production furniture models remain pending; user-supplied Mori OTF fonts are integrated.

## Next

- Selected pivot: migrate the viewport to first-person PlayCanvas with SuperSplat-prepared fixed rooms; preserve GLB furniture, React/Mori UI and Django contracts. First prove one calibrated splat plus one movable GLB; renderer migration is not implemented yet.

- Integrate collaborator GLBs/metadata and agreed reconstructed room input. Run a final physical trackpad/reduced-motion/device rehearsal. Full mesh/clearance validation and valid-space overlay follow separately.

## Done

- 2026-09-20: **Download missing Haussmann assets automatically in the web app** — implemented pinned public-source SOG download/cache with byte/SHA-256 checks, cancellation, timeouts and retries; gallery entry starts it in the background and stays selectable without binaries. Added real-progress FRIDAY loading/error overlay with the empty-room image, responsive layout and reduced motion. Changed `shared/haussmann-download.json`, `frontend/src/scene/playcanvas/{haussmannDownload,assets,room,runtime}.ts`, `frontend/src/shopping/{haussmannPreload,RoomSelection}.tsx/ts`, `frontend/src/scene/{RoomArrival.tsx,room-arrival.css}`, `frontend/src/shopping/room-selection.css`, routing/types, room packaging and regression tests; updated both indexed Haussmann/gallery docs. Final `npm test` → `tests 210 / pass 210 / fail 0`, then `tests 26 / pass 26 / fail 0`. Missing-assets `VERCEL=1 npm run build` → `built in 7.68s`; emitted manifest/spatial/license with no room binaries. Isolated canonical `BACKEND_PORT=8235 FRONTEND_PORT=5235 ./run-local.sh` verified automatic gallery download, early entry and actual empty-room rendering from the public CDN; cache verification reported `cachedFiles: 8 / expectedFiles: 8 / networkRequests: 0`. Existing 5173 gallery refreshed successfully. Desktop/mobile and retry state inspected; finish review prompted removal of aria-busy around live loading announcements. Screenshots/logs under `.scratch/haussmann-*`. Final `npm run build` → `built in 2.81s` (existing large-chunk warning); finish review disposition: ship. `git diff --check` and existing INDEX links passed. Private stack stopped and lsof verified no listeners on 8235/5235; QA tabs closed and viewport reset. Existing no-key catalogue compile warning observed, unrelated live AI behavior not verified. Backend unchanged, suite not rerun. No fetch, commit or push.

- 2026-09-20: **List Haussmann apartment first in the room gallery** — corrected the preview after user review: `frontend/public/room-previews/haussmann-apartment.png` now shows the empty apartment. Captured with zero furniture instances through the existing renderer; restored the temporary rendering-fixture edit afterward. Updated preview provenance in `docs/frontend/design/friday-entry-gallery.md`. Verified the 960 × 640 PNG visually and reloaded the actual 5173 gallery: empty floor, no sofa/table/vase, Haussmann still first and selected. Image/docs only; earlier frontend tests/build remain the last suite run, no new suite needed. No servers launched, temporary capture tab closed, no commit or push.

- 2026-09-20: **List Haussmann apartment first in the room gallery** — completed on `codex/locate-haussmann-room`. Added Haussmann first in `shared/public-rooms.json` and a real 960 × 640 Gaussian capture at `frontend/public/room-previews/haussmann-apartment.png`; kept remaining room order and existing availability gating. Updated indexed `docs/frontend/design/friday-entry-gallery.md` with preview provenance/CC BY attribution and `docs/frontend/room-capture/haussmann-apartment.md` with current routes. Verification: `npm test` → `tests 203 / pass 203 / fail 0`, then `tests 25 / pass 25 / fail 0`; `npm run build` → `built in 13.75s` (existing chunk-size warning). `BACKEND_PORT=8234 FRONTEND_PORT=5234 ./run-local.sh`: gallery selected Haussmann as SPACE 01, Enter space opened `/room/haussmann-apartment`, loading completed and editor controls enabled. Refreshed Vite config timestamp to reload the build-provided room list on the existing 5173 stack; confirmed Haussmann first there too. Screenshot: `.scratch/haussmann-gallery.png`. Private stack stopped; `lsof` verified no listeners on 8234/5234; temporary browser tabs closed. Existing search compile/throttle warnings appeared during automatic catalogue initialization; no claim of live AI verification. Documentation links and `git diff --check` passed. No application logic changed, backend suite not rerun for this catalogue/preview change; no commit or push.

- 2026-09-20: **Evaluate Gaussian splats and surface reconstruction — locate missing Haussmann room**: investigation complete. Traced `frontend/src/App.tsx` and `shared/public-rooms.json`: commit `a80920e` replaced the default editor with the gallery; `5f24252` added the entrance. The three-entry public gallery omits Haussmann, while `/?roomId=haussmann-apartment` still opens it. Both generated assets remain in `shared/rooms/haussmann-apartment/assets/`. Verification: local `curl` manifest check → `HTTP 200`; SOG HEAD → `HTTP/1.1 200 OK`, `Content-Length: 43110214`; opened the direct link on the existing 5173 stack and visually confirmed the Gaussian room and HAUSSMANN attribution. Corrected this log’s launch link. Existing Sin Kanban card `68a22eb5-dc7f-4cc5-8ee2-d248bd8369ee` remains active for broader evaluation; this finding does not complete its quality/calibration work. No application changes or automated suite needed for this investigation. No servers/processes launched; existing owner stack preserved. No fetch, commit or push.

- 2026-09-20: **Update Kanban wording for OpenAI API and Codex** — updated `.github/extensions/project-manager/{data/board.json,server.mjs,bin.mjs,extension.mjs,README.md}` on main. The existing agent card now says **Connect room, catalogue, and user input to the OpenAI API agent**; Agent Harness and demo descriptions mention OpenAI API/Agents SDK and Codex. Removed old vendor wording from launcher/docs; retained the legacy adapter package import for compatibility. IDs, ownership, order and completion preserved. Verification: `node --test .github/extensions/project-manager/tests/board.test.mjs` → `tests 6 / pass 6 / fail 0`; launcher smoke → `HTTP 200`, both requested names visible, no old vendor wording in rendered board. Syntax and whitespace checks passed. Stopped the launcher; `lsof` confirmed no listener on port 50717. No commit or push.

- 2026-09-19: Prepared standalone Gaussian candidate research for local-main integration from fetched origin/main 68641c1 in an isolated worktree. Added docs/frontend/room-capture/haussmann-apartment.md, its INDEX.md entry and scoped TODO/Kanban ownership; preserved other owners’ entries. Explicitly labeled references to unmerged implementation. Fresh-main setup passed; backend 175 tests OK (one live test skipped), frontend 65 scene and 9 auth tests passed, six board tests passed, production build passed with existing large-chunk warning. Documentation/index links and diff whitespace checked. No runtime/assets included, no servers launched, shared dirty checkout preserved; no push. Candidate delivery remains blocked on authorized download.

- 2026-09-19: Resolved the user's pull of origin/main (44daea7) into codex/gaussian-splatting. Combined both sides of .env.example (SCENE_UNIT_CM=5 plus catalogue/model-service keys) and backend/api/urls.py (one health route, all scene/capture routes, and catalogue search/compile). Preserved incoming catalogue implementation, fixtures, dependencies, frontend dev search/types, Saketh's log and indexed catalogue docs. Synced the pulled uv.lock after the initial backend run found missing Elasticsearch dependencies; no lockfile changes. Verification: 117 backend tests OK with one opt-in live model test skipped, 27 frontend tests passed, TypeScript/Vite build, Django check and migration consistency passed; all 10 docs indexed with valid local links and no conflict markers. Resolved-file whitespace passes; incoming backend/catalogue/feed.py retains its existing extra blank line at EOF, and the existing Three.js bundle warning remains. No live model/Elasticsearch calls, branch switch, fetch or push; collaborator worktree untouched. Included the previously pending branch-handoff work-log update in this merge.

- 2026-09-19: Committed indexed pivot/reconstruction research as 6fdf9f0 on feat/3d-engine-frontend, merged the Three.js build into local main as 3cf1463, and created/switched to codex/gaussian-splatting. Resolved the sole AGENTS.md conflict by retaining mandatory documentation-index verification; the merge tree exactly matches the tested feature tip. Fresh verification passed: 27 frontend tests, 22 backend tests, TypeScript/Vite build, Django check, migration consistency, all nine docs indexed with working local links, and whitespace. Existing large Three.js chunk warning remains. Main is five commits ahead of the locally recorded origin/main; the user handles remote updates and pushing. No fetch or push. The codex/realistic-couch-model worktree remains untouched. This branch handoff changes only the work log; runtime migration is pending.

- 2026-09-19: Found existing room splat candidates and recorded them in the already-indexed docs/frontend/room-capture/research-and-integration-plan.md. Inspected Studio 11's live preview and verified its download requires login; verified the creator-linked Modlinek Villa room 03 public export folder contains an 831.2 MB ZIP with a download control. Recorded Mirror Room v3 as a smaller synthetic fallback, page-listed licenses/sizes, provenance and unverified scale/format/collision/performance. Documentation indexing and whitespace checks passed. No asset downloads, runtime changes or application tests; no commit, branch switch, fetch or push.

- 2026-09-19: Recorded the user-selected first-person PlayCanvas/SuperSplat pivot in the indexed room-capture research plan and foundation-plan banner. Updated furniture collaborator guidance to retain GLB/PBR/scale/pivot deliverables and inspect close-range surfaces. Verified current PlayCanvas model/React/collision documentation; updated INDEX description and checked all documentation indexing/local links plus diff whitespace. Planning/docs only; no runtime migration, installs, tests, commit, branch switch, fetch or push.

- 2026-09-19: Researched and documented local phone-video room reconstruction in docs/frontend/room-capture/research-and-integration-plan.md and indexed it. Incorporated user constraints: phone walkthrough, scanned room/furniture fixed, laptop-only processing. Compared local COLMAP/OpenMVS mesh pipeline, Brush/Spark splats, voxel/floor maps, Apple capture APIs, and DA3/VGGT alternatives from primary sources. Recommended preserving Three.js/R3F, first proving one calibrated mesh, and adding a versioned fixed-obstacle/free/unknown map. Plan includes repository integration points, proposed manifest, local job/storage model, calibration, capture/occlusion handling, staged estimates and acceptance criteria. Verified nine-doc indexing, local links, JSON example, voxel calculations and diff whitespace. No models installed, video processed or runtime benchmark; no application tests needed for documentation-only change. No runtime edits, commit, fetch, push or branch switch.

- 2026-09-19: Prepared the user-requested commit of engine placement results/capture jobs and migrations, frontend capture controls and rendering, room/glass polish, bundled Mori fonts/license, shared furniture asset serving/build packaging, and indexed documentation. Fresh verification: 27 frontend tests, 22 Django tests, TypeScript/Vite build, Django check and migration consistency passed. Existing Three.js chunk warning remains; captures require an open same-session editor and font public-use licensing remains a handoff. Staying on feat/3d-engine-frontend; no push.

- 2026-09-19: Established shared/models/furniture/ as the canonical furniture asset source, with a tracked directory marker. Added frontend/scripts/shared-furniture-assets.ts and Vite integration to serve allowed runtime assets at unchanged /models/furniture/ URLs and emit them into production builds. Updated indexed collaborator-handoff.md and editor-implementation.md; dimension metadata remains in shared/scene-fixtures.json. No production furniture existed in this checkout to move; couch worktree and frontend-only GLB test fixture untouched. Verified TypeScript/build, real GLB development GET/HEAD and MIME, byte-identical production output, missing/malformed/traversal rejection and metadata exclusion. Temporary QA assets removed. Existing bundle-size warning remains; no commit, branch switch, fetch or push.

- 2026-09-19: Integrated user-supplied PP Mori v2.6 Regular/Semibold/Black/Italic OTF files in frontend/public/fonts/pp-mori/ with the original license PDF. Updated style.css to declare real static weights and use bundled URLs; corrected heading weight to 600. Updated already-indexed editor-implementation.md and selected-direction.md. Build passed with existing Three.js size warning, all four local HTTP assets matched source bytes, and browser preview inspected. License excludes public websites/apps; appropriate licensing remains required for public deployment. No commit, push or branch change.

- 2026-09-19: Completed engine validation, selectable captures and visual refinement with three subagents. Backend added exact placement/dry-run results, capture jobs/leases/PNG validation, migrations and trusted Python adapters in backend/api/{scene_service,capture_service,engine_tools,scene_views}. Frontend added offscreen SceneCapture/useCaptureWorker/captureCamera plus coordinator CapturePanel/capture.css and App integration. Visual agent refined Room/RoomMaterials/SceneLighting/style.css; removed the artificial window projection after the user's oversized-X feedback. Preserved furniture assets and the separate codex/realistic-couch-model worktree. Added and indexed docs/backend/contracts/spatial-engine-tools.md; updated scene-api.md and editor-implementation.md.
- Verification: 27 frontend tests, 22 backend tests, TypeScript/Vite build, Django check and migration consistency passed; migrations applied. Visually inspected actual 1024×768 default 3D, top and custom 225°/50° PNGs. Fixed fallback/HMR job lifecycle and exact capture-buffer sizing bugs found in browser testing. Live Python adapters confirmed invalid/no-mutation, dry run, successful placement, frozen image retrieval after edits and pose restoration. Preview confirms the X is gone. Captures require an open same-session editor; actual SDK caller, final GLBs/Mori, physical trackpad and sustained performance remain handoffs. Existing large Three.js chunk warning remains. Prior placement/persistence work committed as 56f2ad9; this follow-up remains uncommitted. No branch switch, fetch or push.

- 2026-09-19: Improved placement and implemented both backend paths with parallel subagents on feat/3d-engine-frontend. build_editor added frontend placement/SAT/free-slot tests and Django session layout/receipt models, migration, validated snapshot/atomic command services/views/tests; build_room added grabbed-surface drag and rollback plus race-tested autosave/polling; build_furniture added larger hit targets and green/red footprints. Coordinator integrated App/Scene/editor history, shared/scene-fixtures.json, visible feedback, initial-load edit gate, cursor cleanup, numeric rollback, and Vite Host preservation for CSRF. Added and indexed docs/backend/contracts/scene-api.md; updated frontend implementation/plan/design notes.
- Verification: 22 frontend tests, 11 Django tests, Django system check, TypeScript/Vite build passed; migration applied. Browser through full-stack5174/8001 confirmed save/reload, valid drag, boundary/overlap rollback, numeric rejection, free duplicate slot, and clean cross-tab update/history reset. Green footprint visually checked; red mid-drag screenshot not isolated, code path reviewed. Existing ~947kB lazy scene chunk warning remains. Actual OpenAI/voice agent caller, reconstructed rooms, final GLBs/Mori, mesh/clearance validation and physical trackpad rehearsal remain handoffs. No commit, branch switch, fetch, or push.

- 2026-09-19: Prepared user-requested commit of frontend foundation, fixtures/tests, design references and indexed implementation/plan notes, README, configuration and documentation rules. Fresh five-test suite, TypeScript/Vite production build, six-document index/link verification, and whitespace checks passed. Existing lazy scene chunk warning remains. Stayed on feat/3d-engine-frontend; no push.

- 2026-09-19: Built the room-editor foundation with three parallel subagents, remaining on feat/3d-engine-frontend. Coordinator added scene types/units/fixtures, validated root SCENE_UNIT_CM exposure/default5, warm Atelier glass App/Icons/CSS, scene composition, keyboard/property controls, preserved API health, dev-only GLB/performance QA controls, explicit esbuild test dependency, README and indexed docs/frontend/editor-implementation.md. Room agent delivered room/grid/cameras/procedural materials/performance probe; furniture agent delivered dimensioned proxies, cloned meter-GLB loader with retry and reproducible true GLB fixture; editor agent delivered validated edits/history/drag and regression tests. No implementation commit or push.
- 2026-09-19 verification: 5 scene tests, TypeScript/Vite build, GLB binary roundtrip/bounds/clone checks, invalid Vite scale rejection, all6 docs indexed/local links, and diff whitespace passed. Full-stack browser exercised add/select/numeric/snap on-off/rotate/remove/undo-redo/top-reset; Escape drag + numeric cancellation; release beyond canvas; independent GLB copies/delete survival; failure retry recovery; slow-load pose retention and removal; actual 5/10cm stacks. Fresh final browser had no warnings/errors. Brief 3-proxy sample: 9 rendered frame intervals,64.4FPS,p95 16.9ms (not sustained benchmark). Main preview remains http://127.0.0.1:5174 with backend8001; temporary10cm stack stopped. Runtime models remain test shapes; final GLBs/Mori font, physical trackpad/blur/reduced-motion rehearsal pending. Large lazy Three.js chunk warning remains (~944kB raw). Doc records exact scope and limits.

- 2026-09-19: Merged furniture handoff commit 06d01df into local main through a temporary worktree with a detailed merge commit. Verified clean main worktree, documentation indexing/links, metadata JSON, image signature, and merge whitespace. Removed temporary worktree; feature checkout and uncommitted design work preserved. No fetch or push; user can push local main.

- 2026-09-19: Prepared standalone furniture collaborator handoff with approved warm Atelier reference image, Japandi material guidance, GLB units/pivot/material requirements, and rendering/creation workflow research. Included docs/frontend/3d-object/collaborator-handoff.md, docs/frontend/rendering-and-furniture-workflow.md, docs/frontend/design/images/06-warm-atelier-glass.png, and matching INDEX.md entries. Verified local links, metadata JSON, PNG signature, and whitespace. No runtime changes or integration claims; no branch switch or push.

- 2026-09-19: Corrected Atelier palette using user's attachment: warm ivory, terracotta, espresso, sand. Generated and visually inspected docs/frontend/design/images/06-warm-atelier-glass.png while retaining latest layout, room box, and liquid glass; saved attachment as atelier-user-reference.png. Updated selected-direction.md with current image, palette, and full prompt; aligned 3d-engine-plan.md and INDEX.md. All 5 docs indexed, local links and git diff --check pass. Still image only; no code, branch switch, commit, or push.

- 2026-09-19: Refined docs/frontend/design/selected-direction.md and 3d-engine-plan.md for Atelier colors, Noir box-room, Japandi/wabi-sabi materials, PP Mori, and user-confirmed subtle glass with fluid pane formation from its trigger. Researched official Mori specimen/web formats; no font installed. Generated and visually inspected images/05-atelier-japandi-glass.png using built-in image generation; preserved earlier references and recorded full prompt. Updated INDEX.md description. All 5 docs indexed, local links resolve, PNG signature and git diff --check passed. Still image does not demonstrate motion or exact font fidelity; no application implementation, branch switch, commit, or push.

- 2026-09-19: Recorded user-selected Atelier UI + Noir room/practicality direction. Generated and visually inspected the combined reference using the built-in image tool; saved docs/frontend/design/images/04-atelier-noir.png and docs/frontend/design/selected-direction.md with full prompt, palette/layout targets, and explicit scope limits. Updated INDEX.md, visual-directions.md, and 3d-engine-plan.md to point to the selected direction. All 5 docs indexed, local document/image links resolve, PNG signature and git diff --check passed. Generated reference is not implemented UI; no code, branch switch, commit, or push.
- 2026-09-19: Generated and visually reviewed three futuristic modern-furniture editor concepts using the built-in image tool: Noir, Solstice, and Atelier. Saved all three PNGs under docs/frontend/design/images/ and full prompts/comparison in docs/frontend/design/visual-directions.md; added its INDEX.md entry. Checked all 4 docs are indexed, local documentation/image links resolve, and PNG signatures are valid. Images are design references, not implemented UI or validated dimensions; decorative extras do not expand foundation scope. No application changes, branch switch, commit, or push.
- 2026-09-19: Generated and visually inspected a preview-only FRIDAY room-editor concept using built-in image generation. Shows spacious gridded cutaway room, sofa/table, selected footprint, camera toolbar, object inspector, cm coordinates and 5 cm snap. Image displayed inline; retained in default generated-images storage (exec-27df364c-9f6b-48b5-a306-dc6d1f29ece9.png). Concept illustrates the intended populated state, not an implemented UI or dimensionally validated scene. No new documentation file, asset integration, application changes, commit, or push.
- Review verification: all 3 Markdown files under docs/ have direct INDEX.md entries; all 29 checked local links resolve; JSON examples parse; superseded meter-based app-contract phrases removed; git diff --check passed. Application tests were not run because this pass changes only documentation.
- 2026-09-19: Completed user-authorized parallel review with review_contracts and review_execution. Rewrote docs/frontend/3d-engine-plan.md around canonical centimeter state/meter GLBs, minimal types/reducer, exact drag/history/load behavior, root + three file-isolated implementation agents, staged integration, and meaningful quality gates. Target 4–6 hours parallel with 7–10-hour contingency; no guaranteed speedup. Updated rendering-and-furniture-workflow.md to agree with units. Reorganized INDEX.md into an explicit frontend documentation table; all three docs already had entries and are now easier to find. Strengthened AGENTS.md with mandatory index verification before handoff. No runtime implementation, benchmarks, commits, fetch, push, or branch changes.
- 2026-09-19 (review_contracts, recorded by coordinator): Read-only audit of plan/handoff/config/scaffold confirmed existing index entry and identified competing cm/m contracts, vague room normalization, and unclear state ownership. Proposed canonical rectangular room/pose/edit types, one conversion per asset, independent GLB hierarchies, and failure/history checks. Findings incorporated into reviewed plan; runtime verification pending.
- 2026-09-19 (review_execution, recorded by coordinator): Read-only audit confirmed initial scaffold and index entries. Proposed non-overlapping coordinator/worker files, early integrated proxy loop, deferred remote protocol/solver work, required camera/pointer/GLB failure cases, focused tests and browser acceptance, and timeboxed delivery. Findings incorporated; no files or Git state changed by reviewer.
- 2026-09-19: User reports main pushed; verified current checkout already on feat/3d-engine-frontend. Added docs/frontend/3d-object/collaborator-handoff.md and indexed it in INDEX.md. Handoff covers first sofa/table delivery, Blender/Higgsfield routes, GLB/material/pivot/axis requirements, centimeter metadata with meter-based GLB conversion, sample JSON, suggested asset paths, initial performance budgets, and collaborator/frontend acceptance checks. Verified 20 local links, JSON syntax, scale example, and git diff --check. Documentation only; asset/browser integration remains pending. No branch switch needed, no fetch, main edits, commit, or push.
- 2026-09-19: Added docs/frontend/rendering-and-furniture-workflow.md and its INDEX.md entry; linked the unit discussion from docs/frontend/3d-engine-plan.md. Verified official PlayCanvas GLB/React/splat/collision docs, Spark for Three.js, Blender glTF export, Khronos meter convention, and live Higgsfield 3D catalogue (Meshy single/multi-image GLB and SAM 3 3D). Recommended R3F foundation with independent GLB assets, Blender cleanup/measurement and optional Higgsfield mesh generation; documented centimeter metadata versus meter-based GLB conversion and collaborator acceptance targets. Local documentation links and diff whitespace checks passed. No generation jobs, assets, code changes, or benchmarks; no branch switch, fetch, commit, or push.
- 2026-09-19: Per explicit user authorization, committed only AGENTS.md to main as 59d953e (Document repository-wide Markdown organization), using an isolated temporary worktree. Inspected staged diff, passed staged whitespace check, verified one-file commit and clean main worktree, then removed the temporary worktree. Original checkout remains on feat/3d-engine-frontend with planning changes preserved. Local main is two commits ahead of the cached origin/main reference: existing 912e222 and new 59d953e. No fetch or push; live remote freshness unverified. User can push main explicitly from the current branch with git push origin main.
- 2026-09-19: Moved FRONTEND_3D_PLAN.md to docs/frontend/3d-engine-plan.md, fixed its proposal link, and updated INDEX.md. Added repository-wide guidance in AGENTS.md and INDEX.md: new Markdown notes/docs belong in appropriate docs/ topic folders (nested folders allowed) and must be indexed. Verified 21 local links, old plan removal, CLAUDE.md symlink, and git diff --check. Remained on feat/3d-engine-frontend; main has not been modified, and the rule will reach it through merge. No application changes, fetch, commit, or push.
- 2026-09-19: Revised FRONTEND_3D_PLAN.md from user answers: laptop only, realistic visuals plus basic furniture manipulation, incoming models, room reconstruction delegated to a teammate, rendering/interaction foundation first. Specified SCENE_UNIT_CM=5 in root environment for future implementation, meter-based interchange conversions, an empty 6 × 5 × 2.8 m fixture, fine/major grid lines, camera controls, undo/redo, asset normalization and semantic wall requirements. Deferred solver/overlay and commerce integration; revised foundation estimate to 7–10 hours. Inspected current Vite/root environment loading; scale is specified but not yet wired up. Documentation diff check passed; no runtime tests needed or run. No branch switch, fetch, commit, or push.
- 2026-09-19: Added FRONTEND_3D_PLAN.md and indexed it in INDEX.md. Read proposal and current scaffold; compared R3F/drei, plain Three.js, Babylon.js, PlayCanvas/SuperSplat, and model-viewer against official documentation. Tested Ambic's public SuperSplat viewer: pause, camera drag, zoom, sofa click; no object-selection/replacement controls appeared. Proposed measured mesh room + GLBs, typed scene commands, sampled feasible-anchor overlay, exact final validation, and a 13–19 hour staged workstream. Research/draft complete; implementation and performance unverified. Documentation diff check passed; no application code or dependencies changed, no application tests run, no fetch/branch switch/commit/push.
- 2026-09-19: Fixed project-manager persistence in .github/extensions/project-manager/board.mjs and server.mjs on codex/fix-project-manager-sync. Fresh reads and mutations use an exclusive lock, writes atomically replace JSON, and server polling broadcasts external updates over SSE with cleanup on close. Added tests/board.test.mjs and tests/worker.mjs for four-process concurrent initialization/writes (80 tasks), atomic read safety, migration, detached reads, failure/invalid-JSON recovery, external SSE refresh and shutdown, and lock timeout/recovery. All 4 tests passed; loopback test required sandbox escalation after EPERM. Updated extension README, INDEX.md, and .gitignore. git diff --check passed; committed board.json unchanged. Limitations: abandoned crash locks require stopping all writers before manual removal; Git/manual writes do not use this lock; cross-machine sharing remains Git-based; Copilot host not runtime-tested. No fetch, commit, or push.
- 2026-09-19: Reviewed .github/extensions/project-manager for OpenAI Challenge fit. Standalone HTTP smoke check passed (200, six columns, two sample tasks); server stopped. Copilot adapter present but not runtime-verified; no OpenAI calls, TODO sync, or root-launcher integration found. Identified process-local board caching as a cross-session stale-write risk. Recommended FRIDAY milestones, owner/acceptance/evidence fields, and a concrete Codex debugging/demo record. No implementation or board-data changes; no fetch or push.
- 2026-09-19: Added AGENTS.md instructions to run ./setup.sh when local setup is missing and ./run-local.sh for development and manual/integration testing. Automated checks remain separate. Documentation-only change; verified diff formatting and CLAUDE.md symlink consistency.
- 2026-09-19: Prepared the full setup for a local commit on main with the user's explicit one-time authorization. Includes proposal.md, shared agent instructions/index/work logs, ignore rules, backend/frontend source and lockfiles, environment template, setup/run scripts, and README. Prior setup verification is recorded below; no fetch or push requested.
- 2026-09-19: Updated AGENTS.md so the user handles git fetch and notifies agents of updates. Agents still check local status/diffs and re-read files before editing. Reviewed the wording; no fetch performed.
- 2026-09-19: Scaffolded backend/config and backend/api with Django 5.2, REST Framework, SQLite, public GET /api/health/, authenticated-by-default REST settings, and uv dependencies/lockfile. Added frontend with React 19.2, TypeScript, Vite, lazy-loaded Three.js/React Three Fiber scene, API health status, and /api dev proxy. React 19.2 matches Fiber's current peer requirement.
- Added setup.sh (Node/npm checks, uv install when absent, environment template, dependency sync, migrations), run-local.sh and scripts/run_local.py (both dev servers, reload, port checks, process-group cleanup), and .env.example. Documented commands/layout in README.md and linked sections in INDEX.md. Working branch: codex/full-stack-setup.
- Verification: first and repeat setup passed; Django checks and 2 API tests passed; TypeScript/Vite production build passed; direct/proxied health and frontend HTTP checks passed; occupied-port guard and SIGINT cleanup passed. Three.js emits a large-chunk warning despite lazy loading. Browser rendering and the uv-absent installer path were not exercised. Test servers stopped; no commit or push performed. Remote freshness remains unverified because GitHub SSH authentication failed.
- 2026-09-19: Made AGENTS.md owner-neutral: list all four TODO files and ask which to use when the conversation does not identify the owner. Removed the current-user label from INDEX.md and recorded the confirmed stack in AGENTS.md. Added .gitignore for Python/Django, TypeScript/React, Docker local data, secrets, and generated files while keeping lockfiles, migrations, and Three.js assets trackable. Verification: all 21 ignore/keep cases passed, CLAUDE.md still matches AGENTS.md, and git diff --check passed. Remote freshness could not be verified: git fetch origin failed with GitHub SSH public-key authentication. No commit or push performed.
- 2026-09-19: Added rapid-collaboration rules to AGENTS.md: check remote updates at task start and before commits, re-read relevant files and local diffs before edits, and never assume code from earlier messages is unchanged. Verified final wording and that CLAUDE.md still resolves to the same instructions; documentation-only change.
- 2026-09-19: Initialized AGENTS.md with INDEX.md-first reading, simple MVP scope, mandatory owner work logs, short commit subjects with detailed bodies, and a prohibition on pushing to main.
- Created INDEX.md, TODO_SIN.md, TODO_SAKETH.md, TODO_WILLIAM.md, and TODO_ADELLE.md. Added CLAUDE.md as a relative symlink to AGENTS.md for Claude Code.
- Verification: checked all local links in AGENTS.md and INDEX.md, all four indexed TODO files, and the symlink target/content. All passed. No application code changed; no application tests needed. No commit or push performed.

## Blockers / handoff

- Recommend React/TypeScript/Vite + R3F/drei for the confirmed scope. Incoming room format, units, and separately addressable walls still require teammate agreement; these do not block the empty-room fixture.

## Notes from Saketh's lane (catalogue, language, region solver) — 2026-09-19

Appended by Saketh's agent, not Sin's work. Nothing above was changed. Full detail: [docs/frontend/region-solver.md](docs/frontend/region-solver.md).

- **`floor-grid-v1` — implemented exactly as you specced it**, including `unknown`, for both the solver's output masks and an optional `occupancy` input. Unknown floor is never lit; floor outside the grid counts as unknown. Good spec. One addition: masks are sampled at grid points (121 × 101 for the fixture room), so every pose your 5 cm drag snap produces is exactly one sample. `CELL_CM = 5` is fixed and not derived from `SCENE_UNIT_CM`, per your note.
- **Your `validatePlacement` is the oracle.** The solver calls it for every grid point instead of reimplementing bounds, height or overlap, and property tests assert the lit floor and your drop check agree. If you change `placement.ts`, `npm test` will say whether they still do.
- **Wall mapping — please confirm or object.** `Room` has no named walls, and compiled sentences say `w-n`, `w-e`, `w-s`, `w-w`. I mapped north = z 0 (your back wall), east = x `widthCm`, south = z `depthCm`, west = x 0 (your side wall). It lives in `frontend/src/region/boundary.ts`. "Left" and "right" stop meaning anything in first person, so the language layer uses compass words.
- **`openings` proposal.** `near(window)` and `clear(door)` have nothing to bind to until `Room` describes openings. Proposed optional, additive field: `openings?: { id, kind: 'door' | 'window', wall: 'n' | 'e' | 's' | 'w', startCm, widthCm, swingCm? }[]`, with `startCm` along +X for n/s walls and +Z for e/w. The solver already accepts it and degrades without it: those clauses are dropped with a reason and the rest still solve. Your type is untouched.
- **Stacking is unsupported, so `on(item)` is dropped.** `validatePlacement` rejects an item overlapping its support, so lighting a table top would show floor the drop then refuses. The rule exists behind `ALLOW_STACKING = false` in `frontend/src/region/clauses.ts`. A gap for you to fix or accept.
- **Units.** Centimetres stay yours, `dims_mm` stays mine. `frontend/src/region/boundary.ts` is the only place they cross (`mmToCm`, `listingToProduct`). Your three `kind`s only pick a stand-in shape, so the 12 catalogue categories fold onto them; neither type changed.
- **Shared files I touched, additively:** one import line in `frontend/src/scene/all-tests.ts` so `npm test` runs the region tests, and a `region:debug` script in `frontend/package.json`.
- **Catalogue placement PR — touches your files, please review rather than assume.** It was blocking integration: a scene accepted only the three products in `shared/scene-fixtures.json`, so nothing from the 12,040-listing catalogue could be placed. Now an instance may carry its `product` inline (optional field on `Instance` in `frontend/src/scene/types.ts`). Additive everywhere: fixture instances are byte-for-byte what they were, and all your existing tests pass unchanged.
  - Your files changed: `scene/types.ts` (optional field), `scene/placement.ts` (two product lookups now go through `productOf`), `scene/commands.ts` (add accepts a carried product; a shared product always wins), `scene/useSceneSync.ts` (fingerprint and snapshot parsing keep it), `App.tsx` (three lookups, one of which would have crashed on a catalogue item), `backend/api/scene_service.py` (`resolve_product`, snapshot product list), and your `docs/backend/contracts/scene-api.md` (new section). New: `scene/products.ts`, `backend/api/catalogue_products.py`.
  - The backend stays the authority: it re-derives the product from the catalogue and rejects carried dimensions that disagree.
  - Not done, yours to decide: the catalogue picker in `App.tsx` still lists only fixture products; `useCaptureWorker` and `SceneCapture` read products from the server snapshot, which now includes placed catalogue items, so screenshots should work but I did not run a capture.
- **Floor overlay and catalogue shelf — built on the current Three.js scene.** Hover a catalogue item → the solver runs → the floor lights; pick it up, click lit floor, it is placed at true scale through the normal edit path. Details: [docs/frontend/region-solver.md](docs/frontend/region-solver.md#in-the-app-hover-pick-up-place).
  - Your files changed, additively: `frontend/src/Scene.tsx` (an optional `overlay` prop, rendered after `<Grid>`), `frontend/src/App.tsx` (shelf, hover/pick-up/turn state, place handler, room label), `frontend/src/scene/fixtures.ts` (room presets), `backend/api/scene_service.py` and `scene_views.py` (per-room layouts), `shared/scene-fixtures.json` (a `rooms` map beside `room`). The modal fixture picker is untouched.
  - **If the splat pivot lands, the overlay needs a PlayCanvas equivalent of exactly one file, `frontend/src/region/FloorOverlay.tsx`.** Its interface is a mask in, a pose out. It needs three things from any renderer: (1) a floor plane in scene coordinates with a known origin and axis convention (today: corner origin, +X width, +Z depth, y = 0 floor, `cmToScene`); (2) a way to upload a single-channel texture and sample it in the floor material (today: R8 `DataTexture`, nearest filtering, a ~20-line shader); (3) a raycast from the pointer to a floor coordinate in cm. My estimate for the port is 2–3 hours including an eye-level check, provided the splat room keeps those coordinates. A test fails if any other file in `frontend/src/region/` imports three.js, R3F or PlayCanvas.
  - A tight second room, **Studio** (260 × 200 cm, a table and a chair placed), is selectable with `?room=studio` or one click in the shelf, so "this won't fit" can be demonstrated. Rooms are presets in `shared/scene-fixtures.json`; the server keeps one layout per session per room, chosen by a `friday_room` cookie. The default room is byte-for-byte what it was.
  - Seen while testing: `PUT /api/scene/` returned 503 "Scene storage is busy" once under SQLite contention with the capture worker's polling. Your retry path handled it correctly; mentioning it because it can happen on stage.

## Note from Saketh's lane — `AGENTS.md` changed (2026-09-19)

Appended by Saketh's agent so the edit is not silent; nothing above was changed. `AGENTS.md` has a new section, **Sync before starting work**:

- Pull at task boundaries only — at task start and after a merge to `main` — never mid-task. The existing "do not fetch mid-task unless asked" rule stands and now says so explicitly.
- At each boundary: `git checkout main && git pull`, `./setup.sh` (other lanes add Python and frontend dependencies), full test suite and frontend build on that fresh `main`, branch off `main` never off a previous feature branch, re-read `AGENTS.md` and `INDEX.md`.
- If clean `main` is red, stop and tell your owner; it is a whole-team problem.
- When merging a stacked PR, retarget the dependent PR to `main` before deleting the base branch. Deleting it first closes the dependent PR (it happened to #3).

## Note from Saketh's lane — `AGENTS.md` changed again (2026-09-19)

Appended by Saketh's agent; nothing above was changed. New section **Clean up after yourself**: stop every process you start and verify with `lsof` before reporting; use your own ports, never 8000/5173 (`BACKEND_PORT` / `FRONTEND_PORT`); scratch files go under the gitignored `.scratch/`; check a push succeeded before acting on it (`set -o pipefail`).

## Note from Saketh's lane — scene saves and SQLite (2026-09-20)

Appended by Saketh's agent; nothing above was changed. Two changes in your files, both about the 503 "Scene storage is busy":

- **`backend/config/settings.py` (shared):** SQLite now uses `transaction_mode: IMMEDIATE`, `journal_mode=WAL`, `timeout: 20`. The scene service reads then writes inside `transaction.atomic`; with SQLite's default deferred transactions that fails instantly under contention, ignoring the timeout. Since William's accounts work landed, scene saves, the cache table behind throttles, and sessions/MFA all share one file. `scripts/hammer_storage.py` reproduces it: 2,297 of 3,297 saves were 503 before, 0 of 1,733 after. `db.sqlite3-wal` and `-shm` files are normal and already gitignored.
- **`frontend/src/scene/useSceneSync.ts`:** a save that fails with a 5xx or a network error is now retried quietly with backoff (0.5 s doubling to 8 s) instead of going straight to "offline" and waiting for a click, and the status line never shows a status code. A refused save (4xx) and a 409 behave as before. **I changed one assertion in your `sync.test.ts`** ("retains dirty data through network retry": it now expects `unsaved` and an automatic retry rather than `offline` and a manual one) and added three tests beside it. The principle is written at the top of `docs/backend/contracts/scene-api.md`.
- Worth knowing for your scene-ownership work with William: room presets store a layout per session per room under `<session key>:<room id>`.

## Note from Saketh's lane — `AGENTS.md`: after every merge, run the suite on fresh `main` (2026-09-20)

Appended by Saketh's agent; nothing above was changed. It binds all four agents. New section **Trust the remote, not your terminal**:

- After a push: never pipe a git command whose exit code matters; check `git rev-parse HEAD` equals `git rev-parse origin/<branch>`; a behavioural claim in a PR description needs a test behind it.
- After every merge: `git checkout main`, `git pull`, delete the merged branch locally, **run the full suite and the frontend build on that fresh `main`**, then branch fresh. Merging updates GitHub, not your clone. A PR is tested against the `main` it was cut from, not the one it lands in, so two green PRs can take `main` down together with no conflict; it has happened here once already. Whoever merges runs the suite at once, and a red `main` is a whole-team stop, announced before anything else.

- 2026-09-19 (mesh integration): User corrected merge scope to include the current PlayCanvas mesh build and associated backend contracts. Integrating in isolated codex/integrate-gaussian against fetched origin/main; preserve latest teammate changes and unrelated room_mesh work. Verification and merge pending; visual lighting defects remain documented. No push authorized.

## Imported mesh workstream checkpoint history

- 2026-09-19: User clarified separate Gaussian ownership: search for a free similar bright mostly-empty room splat, or fully convert the existing mesh. Added/indexed gaussian-splatting-handoff.md with source Blender path, clean reference, license checks, complete multi-view interior coverage, local-only compute, fixed geometry/furniture contracts, ownership and deliverables. Updated parallel-visual-tracks.md prompt to defer to this brief; Studio11 is now explicitly baseline only. Document preparation is complete; separate user task has not been created or messaged. Mesh cabinet proof continues independently.
- 2026-09-19 (room_bake_fix, Lightmapper-One preflight): Downloaded the official newer Lightmapper-One WIP branch into ignored .room-preparation/lightmapper/one-vendor (26,290,421-byte archive). Untouched code imported and registered successfully in factory-background Blender5.2.1, exit0; proof markers ONE_IMPORTED and ONE_REGISTERED in preflight-one.log, script preflight-one.py. This establishes registration compatibility only; actual macOS baking/denoising remains untested (archive includes Windows OIDN binaries). No heavy bake, add-on patch, GUI/preferences/current source/served asset changes. Stopped at requested compatibility gate.

- 2026-09-19: User authorized a fast The Lightmapper small wall/cabinet trial and asked how to parallelize a separate Gaussian session. Assigned isolated addon compatibility/proof to room_bake_fix and read-only integration/capture audit to room_runtime_finish. Coordinator added/indexed parallel-visual-tracks.md with ownership, copyable handoff, shared contracts, dirty-worktree caveat and serialized heavy jobs. No separate user task created yet; no branch change, installation into GUI preferences, commit or push. Default room stays intact until a proof passes.

- 2026-09-19 (room_bake_fix, Lightmapper preflight blocked): Downloaded official GitHub release0.7.0 thelightmapper.zip (344,818 bytes) into ignored .room-preparation/lightmapper and tested untouched add-on in Blender5.2.1 factory-background mode. Import fails before registration: ModuleNotFoundError: No module named bgl at vendor/thelightmapper/addon/utility/encoding.py:1. Exact traceback in preflight.log; registration/proof script preflight.py preserved. No small-object bake was started, no add-on repairs attempted, no GUI/preferences/original scene/served asset modifications. Release metadata internally targets Blender3.1 and version0.6.3 despite0.7.0release. All experiment processes exited; coordinator to choose compatible release/version or another pipeline.

- Final V9 browser reload verified the source floor correction: wood is now muted brown rather than orange; white walls remain improved, but trim/cabinet mottling persists. Returned preview to normal URL without performance overlay. All edited documentation remains indexed and whitespace passes. No further bake started while evaluating alternatives.

- 2026-09-19 (coordinator handoff): User redirected to open-source alternatives after comparing actual Blender and FRIDAY screenshots. Extended already-indexed rendering-alternatives.md with The Lightmapper, Godot LightmapGI, and PlayCanvas GI limitation; recommend a small established-baker proof, not another blind whole-room iteration or engine rewrite. No alternative installed; Blender5.2 compatibility unverified. V8 browser walls are white and textured floor restored, but trim/cabinet lightmaps remain visibly poor; V9 source floor-color correction prepared and still needs final visual check. V6/V8 empty-room observer ~60 frame-event FPS at895×1335, warm-ready372–467ms (first localV6load1139ms); no cold-network/full-screen or sustained walking guarantee. Latest build/59tests passed. V8 perspective capture ready1024×768 after one transient storage-busy retry, but image appearance suggested another open editor may have served an older room asset; do not use it as final visual proof. Source GUI left to user. No heavy bake remains, no branch/commit/push.

- 2026-09-19 (room_bake_fix, V9 handoff): Completed full used architectural albedo-node audit: floor also required authored neutral-hue saturation0.5/value0.5; all other remaining used BaseColor inputs were direct images/constants/RGB nodes. Re-exported corrected wall and floor textures from checkpoint without new ray tracing. Final GLB68,229,960 bytes, eight images; header/floor texture mapping/UV1 carrier, Python AST and whitespace verified. Coordinator reported V8 substantially improved white walls, but trim remains mottled and overall fidelity is not accepted. User redirected to open-source alternatives; no further bake or pipeline changes started. All lane processes finished; no branch/commit/push.

- 2026-09-19 (room_bake_fix, V8): Isolated remaining brown walls to unsupported source Hue/Saturation nodes (Saturation0, Value2.6) bypassed by glTF export. scripts/prepare_cg_arch_room.py now evaluates that exact operation on the source wall textures in linear color, preserving texture detail and original wood; re-exported from checkpoint without ray tracing. Corrected wall reflectance mean0.883, final GLB65,027,928 bytes, source exposure metadata retained. AST/GLB header/material mapping verified; browser fidelity and separate trim-lightmap artifact gate remain coordinator-owned. No new bake/branch/commit/push.

- 2026-09-19 (coordinator performance baseline): V4 local room, empty layout, 895×1335 canvas, warm reload: runtime-to-ready 210ms; 28.8s observer window with two brief camera drags measured 59.7 frame-event FPS and p95 17.2ms across 1,720 intervals. This is mostly idle, excludes captures, is not a cold network/full-screen demo benchmark, and does not qualify the pending V6 asset. Browser confirms magenta glass fixed after scene-color grab. Latest runtime/performance-panel production build and all 59 frontend tests pass; existing bundle-size warning remains.

- 2026-09-19 (room_camera_validation perf): Adding opt-in development ?perf=1 frame-event observer in PlayCanvasScene.tsx and PerformancePanel.tsx, with bounded 30-second samples, load timing, p95 intervals, canvas dimensions and cleanup; no camera automation or heavy processes. TypeScript and whitespace checks passed; browser performance measurement remains coordinator-owned.

- 2026-09-19 (room_bake_fix, V6): Replaced failed flattened-color bake with illumination-only RGBM lightmap at UV1, preserving original full-resolution albedo/normal/roughness at UV0 and source texture transforms. Final local GLB60,372,436 bytes, seven images,12 flagged materials; RGBM max irradiance4.52 (no clipping), source floor5x tiling preserved, all lightmapped primitives haveUV0/UV1,7839 floor checks clear. OIDN RTLightmap unsupported; verified supported RT HDR on tiny probe and used it only on illumination. Added rawEXR + preparedblend checkpoint before postprocessing and disabled source autopack missing-file errors; final postprocess resumed without repeating bake. Unused material slot warning has0faces; export still hits known cleanup abort after durable output. AST/GLB/material/UV checks pass; final browser fidelity remains coordinator gate. No further heavy job, branch change, commit or push. Read-only checkpoint audit found source exposure +2.961538 EV (scale7.78954), previously omitted by realtime adaptation; emitted asset.extras.fridayColorManagement and handed exposure fix to runtime. Raw irradiance versus decoded RGBM means agree (~0.45 each), ruling out encoding darkness. Final GLB metadata-only size60,372,600 bytes.

- 2026-09-19 (coordinator QA): Browser produced ready 1024×768 perspective and schematic captures for cg-arch-interior revision0; inspected schematic image. Placed real sofa through UI at (945,465)cm, verified attempted x800 move reports Unverified area and retains x945, then removed only that test sofa and returned to empty Explore view. V4 visual quality still fails; bake agent's independent Blender render reproduces blurred floor/mottled trim, isolating asset fidelity from runtime. Proceeding with source material textures plus a separate lighting atlas; runtime and exporter agents coordinating contract. Whitespace passed.

- 2026-09-19 (coordinator): Reloaded and visually rejected V4 in the browser: severe grain is gone, but the floor is featureless gray, architectural trim remains mottled, and panes are magenta. Reopened the bake/material and runtime audit lanes; visual acceptance remains blocked. Corrected conflicting default-room statements in the already-indexed PlayCanvas implementation doc. User controls Blender GUI; explained Solid versus Rendered shading from their screenshot. Browser capture/placement checks continue independently; no branch changes or publication.
- 2026-09-19 (coordinator): Researched six rendering alternatives against primary documentation and added/indexed docs/frontend/room-capture/rendering-alternatives.md. Baked PlayCanvas remains recommended for movable furniture and free walking; panorama is the fixed-view fidelity fallback; Shapespark authoring requires Windows; Verge3D is commercial; browser path tracing requires progressive refinement. No alternative installs or benchmark claims. Current corrected runtime passes production build and all 59 frontend tests; all docs indexed and whitespace clean. Browser proof rejected due to severely speckled atlas; bake agent is correcting UV/bake quality, not marking visual work complete.

- 2026-09-19 (room_runtime_finish): Updated frontend/src/scene/playcanvas/lighting.ts to remove the imported-room ceiling spotlight and its shadow banding; retained gentle furniture illumination and unchanged splat lighting. Updated room.ts to disable additional lighting/skybox/shadows only for the actual Baked room illumination emissive material. Existing linear tone mapping/exposure 1 preserves the display-transformed atlas. TypeScript passed. Inspected the emitted GLB and found atlas texCoord=2, beyond PlayCanvas standard material UV0/UV1 support; handed required exporter UV cleanup to room_bake_fix. Verified corrected 15,402,832-byte GLB has KHR_materials_unlit and baseColorTexture UV0; runtime preserves its mapping. Browser appearance remains pending coordinator validation; no branch/commit/push.

- 2026-09-19 (room_camera_validation): Source geometry inspection complete: 7,839 floor/body rays at 5 cm intervals passed for expanded living-room x787..1121,z180..760. Updated shared/rooms/cg-arch-interior/{manifest,spatial}.json to allow source-like camera (1100,121,735), yaw0.898638/pitch-0.023463/FOV57; camera retains 20 cm boundary clearance. Added backend/api/test_prepared_rooms.py capture/clearance, real sofa placement and no-mutation rejection tests; all 12 prepared-room tests and whitespace checks pass. Updated already-indexed docs/frontend/room-capture/cg-arch-interior.md. Geometry-only Blender process exited cleanly; no render/bake launched. Updated indexed Cg Arch instructions for bake entrypoint, 2048 atlas/32 samples/four CPU threads, explicit OIDN, UV0 unlit export, removed spotlight, and pending final visual acceptance. Final baked browser framing remains coordinator QA; exporter sample-region update handed to room_bake_fix. No branch/commit/push.

- 2026-09-19 (room_bake_fix): Completed corrected 2048 atlas bake with 32 samples/four CPU threads and explicit bundled OpenImageDenoise in linear HDR. scripts/bake_cg_arch_room.py forces LightingAtlas for baking, removes unused UVs, and denoises before AgX; scripts/prepare_cg_arch_room.py checks the expanded 7,839 floor samples and emits standard KHR_materials_unlit/UV0 plus transparent nonmetallic glass. Final GLB14,210,704 bytes, 281,527 polygons, one embedded atlas; zero floor obstructions. Atlas visually inspected: prior severe salt-and-pepper noise removed, soft white illumination and brown wood retained. Verified Python AST, GLB header, unlit UV0, glass BLEND and whitespace. Blender still aborts during shader-node cleanup after successful output (exit134), so outputs validated separately. Final browser acceptance remains coordinator-owned and pending; source unchanged, no branch/commit/push, no further render job active.

- 2026-09-19: Correcting the imported room to match the supplied listing reference. Found unassigned source materials becoming implicit metallic glTF materials; adding explicit architectural white. Original Blender reference render verified bright source appearance. Preparing baked static illumination to retain indirect light and remove runtime shadow banding. No branch or Git publication changes.

- 2026-09-19: Installed/enabled official Blendkit add-on and downloaded Cg Arch Empty Interior Scene 2 (187 MiB) into the local BlenderKit cache. Exported a local 55.5 MB GLB with six embedded textures; added scripts/prepare_cg_arch_room.py and shared/rooms/cg-arch-interior metadata/license/spatial files. Default editor now loads it; old fixtures retain query routes. Configured conservative living-room-only placement zone; 4,422 floor-support rays passed. Added one local shadow light and indexed docs/frontend/room-capture/cg-arch-interior.md. Browser confirms room, textures and UI load; screenshot shown on request. Visual work remains incomplete: too-dark lighting, glass translation and shadow artifacts require correction; placement integration tests and final visual QA pending. Blender exported then aborted during cleanup; GLB header/textures and browser load verified. Python AST and whitespace checks passed; py_compile cache write denied by sandbox. No commit/push or branch change.

- Empty mesh room implementation complete; authored geometry is the default. Scan reconstruction and arbitrary-mesh collision remain separate work.

- Collaborator GLB integration complete; original prepared-room implementation final handoff remains pending from the earlier interruption. Keep testing light after the user reported Mac lag.

- User paused build/testing to diagnose Mac lag. Read-only process inspection found a transient high-CPU Grammarly/Superhuman WebKit group, 24 GiB physical memory and about 2.8 GB swap usage. No conversion/build/test process remains active; current 5180/8002 stack and collaborator 5181 server remain running. No process/app was stopped. Final browser gesture/material verification and build handoff remain pending; latest checks: 57 frontend tests, 127 backend tests (one skipped), TypeScript/Vite build and real preparation/hash-skip passed.

- Completing the prepared Studio 11 editor on `codex/gaussian-splatting`. Runtime, authoritative backend/session and Atelier UI are integrated. Parallel lanes: splat_runtime_build verifies graphics/capture/performance; splat_interaction_build fixes and verifies gestures/navigation; integration_review independently reviews retries and asset preparation. Root owns integration, browser capture checks and indexed documentation. No commit/push requested.

## Next
- Finish integrated browser checks and final build; retain the existing Three.js route at `?legacy`. The accepted laptop default is adaptive 2 million splats at DPR 1 after measured comparison.
- Integrate collaborator production GLBs and catalogue registration/search. Actual measured scan calibration, automatic video reconstruction, semantic annotations and precise collision remain later work. Preserve the separate `codex/realistic-couch-model` worktree.
- 2026-09-19: Added default empty-room fixture (600 × 500 × 280 cm interior, 100 × 220 cm doorway) with reproducible 56 KB GLB generator and shared manifest/spatial/license files. Extended existing prepared-room visual metadata with optional glb format, container loading and frame-end readiness for mesh captures; retained Studio 11 through ?room=studio-11 and isolated its saved layout. Kept Atelier UI, GLB sofa, 5 cm placement grid, analytical room bounds and navigation clearance. Added/indexed docs/frontend/room-capture/empty-mesh-room.md; linked existing implementation notes. Checks: generator GLB roundtrip floor/wall/ceiling/doorway bounds; 59 frontend tests; 28 backend prepared-room/scene/engine tests; TypeScript/Vite build; index completeness and whitespace passed. Browser verified loaded room, sofa placement/green grid, invalid wall move preserving position, floor plan, and 1024 × 768 perspective capture (ready server result and PNG visually inspected). Removed only our test sofa to leave this layout empty. Navigation boundary regression covers authored fixture; physical sustained walking/trackpad not rehearsed. Doorway is visual and walking remains within room rectangle. No arbitrary triangle collision, photographic lighting or reconstruction accuracy claimed. Existing build chunk warning remains. No branch switch, commit or push.


- 2026-09-19: Imported the textured Meshy/Higgsfield modular-sofa-grey-scan GLB, metadata and thumbnail byte-for-byte from the couch-model worktree into shared/models/furniture/modular-sofa-grey-scan/. Registered actual 270 × 86.4 × 76.4 cm dimensions and thumbnail, added validated catalogueVisible/thumbnailUrl metadata, and retired primitive/QA choices from normal PlayCanvas and legacy catalogues while keeping saved-scene compatibility. Removed our temporary QA chair and demonstrated real GLB loading/placement plus validated agent repositioning to (345,355,π/2). Verified source hashes, GLB container/embedded textures, 57 frontend tests, 32 backend API tests and TypeScript/Vite build (existing chunk warning). Final browser error log empty; all 15 documentation files indexed with resolving local links; whitespace check passed. Added/indexed docs/frontend/3d-object/sofa-glb-integration.md and updated PlayCanvas implementation notes. Stopped only verified couch Vite PID 36003; port 5181 is no longer listening. Grammarly/Superhuman helper explanation based on process paths/proofreader argument and official background-process docs; Grammarly was not stopped. No source-worktree edits, branch switch, fetch, merge, commit or push.

- 2026-09-19: Researched automatic surface generation and wrote/indexed docs/frontend/room-capture/surface-generation-methods.md; linked it from the active first-person plan. Verified Studio Generate is server-side, Convert is local browser processing, and SplatTransform can emit uncarved voxel data plus a triangle GLB. Documented separate walking geometry, unknown/solid-shell limitations, version-dependent PLY/PlayCanvas coordinate conversion, and optional local Record3D → Brush plus LiDAR/TSDF reference. Recommended one calibrated splat proof before expanding capture automation. All 13 Markdown docs indexed, 76 local documentation links resolve, and whitespace checks passed. No installs, downloads, training, conversion, runtime changes/tests, benchmark, commit, fetch, push or branch switch; pipeline remains untested on our laptop.

- 2026-09-19: Researched expected scan accuracy using primary room-survey studies and Apple's RoomPlan research. Added a proposed 5 cm independently checked demo-region target and qualification protocol to the indexed first-person plan, distinguishing grid resolution, scan/model/alignment error and frozen-state stability. Recorded study-specific 4.4 cm dimensional-difference standard deviation and 9.4 cm diagonal discrepancy without treating them as our measured performance. Tight-fit confidence must follow measured uncertainty; no capture or benchmark run. Documentation/index and whitespace checks passed; no runtime changes/tests or Git operations.

- 2026-09-19: Recorded the user's fixed-surface decision in indexed first-person-playcanvas-plan.md. Freeze reviewed scan/floor/walls/existing-obstacle geometry per room revision; sensor processing stays in preparation, only imported furniture moves, and placement uses fixed-floor constraints with rejection rather than general physics. New geometry requires explicit room activation/revalidation. Initial alignment/occlusion proof remains required. Documentation-only; index/link and whitespace checks passed, no application tests or runtime/Git changes.

- 2026-09-19: Addressed scan/GLB surface-interaction risk in indexed first-person-playcanvas-plan.md. Distinguished captured appearance, reviewed collision/support data and visual depth proxies; added local alignment checks, uncertainty handling, floor contact, occlusion, shadow-catcher and transparent-surface qualification before the full port. Verified current official PlayCanvas depth/shadow/relighting documentation. No mixed-scene runtime proof exists yet; package.json remains Three.js-only. Documentation links/index and whitespace checked; no application tests, implementation, branch change, commit or push.

- 2026-09-19: Researched iPhone LiDAR calibration from Apple's ARKit scene-depth/mesh and RoomPlan/shared-ARSession documentation. Recommended evaluating a capture export containing synchronized RGB, depth/confidence, camera intrinsics/poses/timestamps and reviewed room geometry; no extra external sensor is required for a one-room MVP. Alignment with reconstructed splats still needs a preserved or recovered coordinate transform, and 5 cm grid spacing is not a measurement-accuracy guarantee. This is feasibility guidance, not a selected capture app or implemented pipeline. No runtime changes/tests or Git operations.

- 2026-09-19: User approved the first-person Atelier concept; recorded approval in indexed selected-direction.md. Checked existing Grid.tsx shader and Furniture.tsx footprint implementation plus current official PlayCanvas splat depth documentation to assess grid feasibility. Recommend a world-aligned shader grid clipped to a local placement patch over calibrated floor geometry, with separate validation-driven footprint colors. Splat occlusion and reviewed geometry remain integration checks, not verified runtime behavior. Documentation-only update; whitespace passed, no implementation or application tests.

- 2026-09-19: Generated and visually inspected the proposed first-person Atelier UI with the built-in image tool. Saved docs/frontend/design/images/07-first-person-atelier.png; recorded the complete prompt, reference roles and concept limitations in selected-direction.md and refreshed INDEX.md. Image shows an interior room, warm glass inspector, terracotta controls and a green valid-placement footprint. It remains a concept for review; exact typography, scan fidelity and animation require implementation. Verified PNG copy, documentation links/index and whitespace. No runtime changes/tests, commit, push or branch switch.

- 2026-09-19: Planned the first-person PlayCanvas/SuperSplat migration with three read-only subagents: splat_renderer_plan checked renderer/assets/capture sources; splat_contracts_plan audited spatial/server/catalogue contracts and final plan; splat_execution_plan mapped dependencies, ownership and sequencing. Coordinator inspected Ambic's live SuperSplat embed (play/pause and camera dragging, no proven editing/walking), incorporated the user's confirmed Atelier glass/Mori direction, and wrote docs/frontend/room-capture/first-person-playcanvas-plan.md plus docs/product/PRODUCT.md. Preserved the supplied screenshot unchanged at docs/frontend/design/images/ambic-first-person-reference.png, indexed both new Markdown files and updated selected-direction.md/research links. Final review clarifications incorporated: server acceptance before history commits, screenshot representation/camera idempotency, and reviewed semantic room annotations. Verification: all 12 docs Markdown files indexed, local documentation links resolve, saved reference matches the supplied PNG byte-for-byte, and git diff --check passes. Planning only; no runtime edits or application tests, asset download/training/device benchmark, commit, fetch, push or branch switch. Already on codex/gaussian-splatting; collaborator couch worktree untouched. Actual room qualification remains the first implementation gate.


- 2026-09-19 (room_runtime_finish follow-up): Read-only asset audit after browser speckle report confirmed baked primitive TEXCOORD_0 correctly references atlas accessor 4 and contains normalized finite UVs. Viewed actual lighting.png: salt-and-pepper noise is present in the baked image; Glass/Glass .001 export with no transparency/PBR data. Coordinated with room_bake_fix on explicit active_render/bake UV-layer correction for v3, since correct GLB remapping alone does not prove the bake used that UV layer. No additional runtime edits; v3 visual verification remains pending.



- 2026-09-19 (room_runtime_finish lightmap handoff): Added explicit RGBM/UV1 carrier import in frontend/src/scene/playcanvas/room.ts, preserving source UV0 albedo and requiring fridayBakedLighting/fridayLightMapEncoding extras. Carrier uses raw linear texture channels and PlayCanvas RGBM decoding, excludes duplicate dynamic illumination, and tags only validated assets for runtime.ts ACES2 tone mapping (approximation to source AgX); older display-baked atlas stays linear. Enabled scene color grab for Cg Arch transmission materials; coordinator browser confirmed purple panes gone. Existing capture reuses the same camera/grab pass. TypeScript passed. Executed actual installed PlayCanvas material-options contract assertions: diffuse UV0 plus RGBM UV1 stay enabled with useLighting=false, lights empty, duplicate ambient suppressed; negative missing-UV1 case disables the lightmap. Source shader audit confirms RGBM decode (8*a*rgb)^2 and irradiance contributes to diffuse lighting. Final V5 asset appearance/capture verification remains coordinator-owned; no branch, commit, push or heavy rendering from this lane.

- 2026-09-19 (room_runtime_finish exposure correction): Diagnosed V6 darkening as omitted source exposure, not RGBM gamma or AO strength. Bake agent measured source AgX High Contrast exposure +2.961538 EV and validated PNG RGBM roundtrip; actual GLB now records exposureScale=7.789540973399098. room.ts reads and validates this metadata only for explicit RGBM carriers; runtime.ts applies it through scene.exposure, confirmed as the installed non-physical renderer exposure uniform. Older display-baked assets and splats stay at exposure 1. TypeScript passed; coordinator performing visual comparison.

- 2026-09-19 (room_runtime_finish bounded visual audit): Reconfirmed runtime albedo/lightmap UV separation, raw RGBM gamma/alpha decoding and AO strength independence. Flagged source material color operations omitted by glTF; bake agent confirmed wall Hue/Saturation saturation 0/value 2.6 and is preserving that operation in exported albedo. No further exposure tuning. At coordinator request set only RGBM lightmap minFilter=FILTER_LINEAR for visual A/B because 2048px atlas uses narrow trim islands and small padding; source color/normal map filtering unchanged. TypeScript passed; retain/revert decision and corrected-wall visual QA remain coordinator-owned. Removed temporary console diagnostic.

- 2026-09-19 (room_runtime_finish isolated-proof audit): Reverted RGBM FILTER_LINEAR experiment after coordinator saw no trim improvement; default sampler restored. Read-only Lightmapper integration audit: backend/asset middleware already discover isolated room manifests, frontend query-room allowlist needs explicit proof ID, and current RGBM loader is Cg Arch-ID scoped (standard unlit proof GLB avoids changing it). Identified stale-capture mechanism: successive asset builds reuse roomId/revision/geometryRevision/visualUrl, so an old same-session tab retains a cached GPU room and can win the shared capture lease while passing identity checks. Recommended unique proof room ID + immutable asset filename, and versioned visualUrl filenames for default-room appearance updates without geometry-revision conflicts. No capture/selector/backend edits or fixture replacement performed.

- 2026-09-19 (room_runtime_finish Lightmapper proof loading): Added explicit ?room=cg-arch-lightmapper-proof selection in frontend/src/SplatEditor.tsx while retaining cg-arch-interior as default; labeled proof as partial-room lighting test. Generalized room.ts RGBM handling to explicit material extras for mesh rooms; ordinary unmarked materials remain untouched and old name-based atlas fallback remains Cg Arch-specific. runtime.ts enables existing imported-fixture illumination/transmission grab for proof ID. Added room.test.ts and all-tests import covering independent proof RGBM/albedo preservation, ordinary AO preservation, and malformed carrier rejection before attachment. All 62 frontend tests and TypeScript passed. Proof asset/manifest are separate bake/coordinator work and not yet visually verified; default manifest/assets unchanged. No branch/commit/push.

- 2026-09-19 (room_bake_fix Lightmapper proof): Starting the authorized isolated Lightmapper-One cabinet proof after successful official branch registration. Using its actual enabling operator, unchanged UV preparation/configuration helpers and official headless bake worker; one 1024px Tv Wall.001 map, 64 samples, four CPU threads, all original room geometry as occluders. Experiment files remain ignored under .room-preparation/lightmapper/proof; default served room unchanged. Addon OIDN requires a CLI executable, while bundled binaries are Windows-only, so initial proof is explicitly un-denoised. Compatibility and visual quality remain pending.

- 2026-09-19 (Gaussian workstream): Started handoff ownership with user-confirmed TODO_SIN.md. Plan: short licensed downloadable empty-interior candidate search; real Gaussian inspection; otherwise assess full original-Blender conversion on this Mac. Own isolated scripts/gaussian_room/, splat fixture metadata and indexed documentation. Read handoff/parallel ownership/renderer and engine contracts; source Blender and clean reference exist. Coordinator contacted to serialize heavy work and agree integration. All coverage, sofa, capture and performance acceptance gates remain pending; no shared renderer/default-room edits, duplicate servers, branch changes, commits or pushes.

- 2026-09-19 (room_bake_fix Lightmapper proof result): Official Lightmapper-One worker completed one 1024px Tv Wall.001 diffuse-illumination map in 1m31s, exit 0, 64 samples/four CPU threads. Verified no swallowed bake errors; EXR pixels finite, max 4.46461, standard deviation 0.20879, 40.55% nonzero texels. Generated source-camera.png/lightmapped-camera.png under ignored .room-preparation/lightmapper/proof using identical full-scene camera/context; target retained source brightness and shape, but raw atlas remains visibly grainy without denoising, so this is compatibility success rather than accepted final quality. Exported isolated RGBM/UV1 proof-cabinet-v1.glb (1,516,108 bytes), verified GLB length, atlas UV binding, original exposure metadata and neutral source material. Added shared/rooms/cg-arch-lightmapper-proof/{manifest.json,spatial.json,license.txt}; empty freeAreas intentionally disables walking/placement because this fixture contains only the tested cabinet/shelf. Default room unchanged. Browser quality gate is coordinator-owned and pending; no full-room bake or further heavy process remains. Official downloaded branch archive SHA256: 882260df67bdf284b8184f8f8cd4f988dffacf4ef8169c83f02c415766d9462f. Stable 0.7 failed Blender 5.2 import on removed bgl; One registered and actually baked without upstream code modifications. No branch/commit/push.

- 2026-09-19 (Gaussian candidate gate): Found HAUSSMANN APARTMENT by sa3d, https://superspl.at/scene/4de797f4, with explicit CC BY 4.0 and 41.57 MB Download listing. Inspected live creator viewer across multiple headings: empty parquet floor, white walls, ceiling moulding, windows and closed doors; soft trim remains. SuperSplat download requires login; asked user for authorized local download path. Public linked source dataset is too large for Drive preview and has not been acquired or confirmed to contain trained splats. Added/indexed docs/frontend/room-capture/haussmann-apartment.md with candidate research, precise acquisition gate and remaining acceptance checks. Coordinator confirmed isolated ownership, frontend 5180/backend 8002 and no duplicate servers. No real asset yet, no local conversion, fixture, sofa/capture/performance or translation-coverage pass; do not mark final room complete. Documentation/index and whitespace verification passed; original/default assets and shared implementation preserved.

- 2026-09-19 (Integrate the PlayCanvas mesh editor checkpoint into main): Reconciled latest main catalogue authority/preset seeding with prepared-room state and explicit room query scoping. Added legacy-key forward/reverse migration regressions; canonical inline catalogue metadata survives load and undo. Packaging now skips unavailable optional local asset packages and uses the tracked empty-room default only when Cg Arch is absent; original local Cg Arch appearance remains unchanged. README and all feature documentation indexed. Full backend suite: 191 tests, one intentional live-model skip; frontend scene suite: 102 pass; asset packaging: 5 pass; auth: 9 pass; TypeScript/Vite production build and Django migration consistency pass. Known lighting noise/trim artifacts and incomplete browser quality remain explicitly pending. Merge not yet completed; no push.

- Integration browser verification through ./run-local.sh passed: clean-checkout default renders the included empty mesh; after local Cg Arch provisioning, explicit room route renders the current apartment and saved state. Existing mottled lighting remains unchanged, not claimed fixed. Temporary integration server and browser tab stopped; original user stack untouched.

- 2026-09-19 (Integrate the PlayCanvas mesh editor checkpoint into main): Completed the user-authorized local main integration of checkpoint 64c8eb7 after fast-forwarding to user-fetched origin/main 68641c1. Includes mesh frontend/backend/scripts/assets metadata and research; excludes unrelated room_mesh work and licensed local room binaries. Temporary QA ports 5185/8005 have no listeners. Original working checkout stays on codex/gaussian-splatting with its edits intact. No fetch or push; post-merge full suite/build verification follows on this exact main tree.

- Post-merge verification on local main passed: 191 backend tests (one opt-in live-model skip), 102 scene tests, five room-packaging tests, nine auth tests and production build including the locally provisioned Cg Arch room. Working tree clean before this verification note; documentation/index checks passed, temporary QA listeners absent. Main is two commits ahead of user-fetched origin/main and ready for the user to push.
- 2026-09-19: Started collaborator room bundle/import workflow. Added scripts/room_bundle.py, scripts/import-room.sh and indexed docs/frontend/room-capture/team-room-setup.md. Preserve branch and teammate changes; local ZIP only, no upload. Integrity, metadata compatibility, safe destination and non-overwrite verification pending.

- Collaborator room sharing complete locally: four importer regressions passed (roundtrip/idempotence, checksum/metadata mismatch, archive paths/corruption, existing files/symlink protection). Packaged current Cg Arch room in ignored .room-preparation/sharing/friday-cg-arch-room.zip with companion SHA-256; real full-size import into temporary clean fixture verified byte-identical GLB. Setup doc indexed. No Blender/server jobs, upload, branch switch, commit or push. Importer/scripts must reach collaborator checkout with the next code checkpoint.

- Room-sharing merge: combine importer commit 8f30af9 with mesh main; preserve both histories in the sole TODO conflict. Importer, tests and indexed guide included; current checkout untouched and no push. Post-merge verification passed: four importer regressions, actual CLI checksum match, 191 backend tests (one live-model skip), 102 scene tests, five packaging tests, nine auth tests and production build. Documentation index and whitespace checks passed. No new servers started.

- 2026-09-19 (Gaussian public dataset verification): Complete 1,951-entry central directory inspected via bounded ranges from creator-linked 2.6 GB ZIP. Contains training images/depth/normals/COLMAP and point clouds; first LFS PLY header has only XYZ/RGB (999,437 vertices), not Gaussian attributes. No ready SOG/SPZ/SPLAT or license file in inventory. Updated indexed haussmann-apartment.md; evidence under ignored .scratch/gaussian/. Kanban remains Sin/incomplete. Actual trained asset still blocked on authenticated SuperSplat download/local user path. No source conversion/training, server, commit or push; two bounded curl processes completed. Whitespace/index checks passed.

- 2026-09-19 (Gaussian acquisition): User supplied Downloads/HAUSSMANN APARTMENT.zip,455283594 bytes. Exact license credits Stéphane Agullo under CC BY4.0. Real PLY2995277 Gaussians with SH2 verified; full uncropped SOG preparation running via scripts/gaussian_room/prepare_haussmann.py. Isolated haussmann-apartment manifest uses provisional orientation/scale, unconfirmed calibration and empty freeAreas to prohibit premature placement. Coordinator authorized minimal selector and temporary5222/8222 QA stack; no mesh edits.

- 2026-09-19 (Haussmann test fixture): Added scripts/gaussian_room/{prepare_haussmann,review_surface}.py, shared/rooms/haussmann-apartment/{manifest,spatial,license}, opt-in SplatEditor selection and room-scoped high-precision unified storage. Preserved all 2,995,277 source Gaussians in local 43.1 MB SOG; generated separate shell and checked supported floor cells/body exclusion, excluding six unknown cells. Actual sofa placement and fixed-obstacle rejection preserved revision; perspective/top contracts returned PNGs; 33.9-second sampled walkthrough saved in ignored .scratch/gaussian/. Quality gate remains open for final down/ceiling/glass artifacts. Pre-integration checks: 191 backend tests (one live skip), 102 scene tests, five asset-packaging tests, nine auth tests and production build passed. Sandbox GPU re-preparation failed to obtain WebGPU; the unrestricted local rerun completed successfully in 115 s for SOG and 33 s for the shell. GPU clustering changed compressed bytes, so the browser-reviewed original SOG was restored; both run reports are retained locally. Temporary 5222/8222 stack stopped and no listeners verified. No mesh/cabinet work included.

## Note from Saketh's lane — more changes in `useSceneSync.ts` (2026-09-20)

Appended by Saketh's agent; nothing above was changed. Two fixes had combined into a trap: room links waited for a saved scene, and a failing save retried forever, so a wedged backend meant you could never switch rooms. Now: `canLeaveRoom()` reopens the links after two failed saves (or when offline/in conflict); a failed save parks the layout in `localStorage` per room (`frontend/src/scene/pendingEdits.ts`) and it is restored and saved when the room is next loaded; and after the initial load no transient failure sets the hard `offline` block any more, because a failed background poll was stopping saves altogether. `SyncState` gained an optional `saveFailures`. None of your existing assertions changed this time; six tests were added. Verified in a browser with the backend killed mid-session and brought back.

- Haussmann merge reconciliation: integrated current local main 62033af and already-fetched origin/main 590db19 in the isolated branch. Preserved both histories in the sole TODO conflict. New asset regression incorrectly required a retail catalogueListingId for the existing generated sofa; test now checks its canonical scene-fixture dimensions/model URL and measured floor/centering while retaining strict retail listing checks. Only that named pre-existing sofa retains its documented 10,814,596-byte/46,385-triangle ceiling; no production identity or geometry changed. Focused four asset tests and the full 195-test backend suite passed (one opt-in live test skipped). Combined frontend: 110 scene tests, five packaging tests and production build pass. Standalone room_mesh: 11 tests; room bundle: four tests pass. All docs indexed, four TODO download links verified.

- 2026-09-20 (Haussmann local-main merge): User-authorized merge completed at ae03845, including origin/main 590db19 and existing local mesh pipeline, with original dirty checkout/cabinet work preserved. On that exact main tree, reran 195 backend tests (one opt-in live skip), 110 scene tests, five packaging tests, nine auth tests, migration consistency and production build: all passed. Independent subagent confirmed all four shared-download notes, indexed resolving documentation, Sin/incomplete quality card and no pending cabinet files in scope. Checked QA ports 5222/8222 have no listeners. No fetch/push, no default-room change; photographic quality gate remains open. Final documentation-only checkpoint follows on the feature branch.

- 2026-09-20: Added post-download Haussmann preparation, geometry review, launch commands and browser URL to TODO_SAKETH.md, TODO_WILLIAM.md and TODO_ADELLE.md. Included saved-work prerequisite, GPU terminal requirement, local-only assets and experimental quality caveat. Verified all three command blocks and whitespace; documentation-only, no servers or runtime tests needed. Committed separately on codex/collaborator-room-setup; no push.

- 2026-09-20 (Gaussian quality): Parallel capture/performance/scale agents isolated stale camera sorting and checked primary author evidence. Updated frontend/src/scene/playcanvas/runtime.ts and runtime.test.ts: wait for a fresh applied camera sort before capture, use correct internal Camera map key, and enable Haussmann-only radial sorting to avoid rotation-only resorting. Clean floor/ceiling/window contract captures verified at896×672, matched source floor/window renders retained, full-resolution30s motion53.5FPS/p95 26.2ms. No crop, decimation, sofa scaling or reduced display resolution. Scale remains unverified: source page10m is a Z-depth range, not room dimensions. Updated indexed Haussmann doc; Sin/incomplete board retained for scale. Targeted runtime tests7/7 pass; full frontend112scene+5packaging tests and TypeScript/Vite build passed (existing chunk-size warning). QAserver stopped; ports5222/8222 have no listeners. User requested this reviewed four-file commit on codex/haussmann-quality; no merge or push.

- 2026-09-20 (Gaussian branch sync): User pull fetched origin/main4b3a258 but aborted in the dirty shared codex/gaussian-splatting checkout. Preserved that checkout in full and merged the fetched main into the clean codex/haussmann-quality worktree instead (0b20329). Post-merge checks passed:206backend tests with2opt-in skips,112scene+5packaging frontendtests and productionbuild. No stash/reset/clean or push; shared mesh owner notified that writes may resume.

- Split-screen demo preview completed at user request: rendered existing two Seedance Mini tests with native Higgsedit composition and sequential source audio. Saved docs/demo/assets/friday-split-screen-opening.mp4, preview JPEG and editable project ZIP; composition source in docs/demo/split-screen-edit.jsx and indexed docs/demo/split-screen-preview.md. ffprobe verifies20.00s,1920x1080,24fps,H264/AAC; inspected3s preview for correct wheelchair-left/responder-right placement, complete wheelchair, equal panes and readable labels. Inactive pane holds a still; original exact-dialogue/full lip-sync limitations remain. Current checkout codex/haussmann-quality lacks earlier package, so used retained output URLs without restoring another branch. No extra generation credits, application changes, branch switch, commit or push.

- 2026-09-20 (Local API recovery): Investigated Vite ECONNREFUSED on port8000; backend startup failed because this checkout lacked django-allauth. Synced backend locked dependencies with uv sync --locked (six missing packages installed), restarted ./run-local.sh on5173/8000, and verified direct and proxied /api/health/ both return HTTP200/status ok. No application code or lockfile changes; local stack left running for the user.

- Revised demo direction and logo exploration started: user specified wheelchair user full-screen0–6s, responder full-screen6–12s, shared split-screen “FRIDAY, plan for me,” voiced FRIDAY scanning response, then study furniture versus shelter beds/supplies materializing while both look around. Preparing updated timed treatment and three distinct FRIDAY logos using built-in Codex image_gen only; no new Higgsfield video credit spend. Clarifications about remaining flow and logo presentation pending.

- Logo direction steered by latest user reference: circular folded/faceted iris with central aperture and warm brown/cool blue contrast. Prior refinement run was interrupted; do not retry those jobs automatically. Generate three new reference-led iris directions using built-in image_gen, preserving Friday app typography/material warmth. User confirmed20s test scope and centered logo over both panes with voice/subtle pulse; earlier full checkout scope deferred for this test.

- Iris logo package completed: generated and visually inspected three latest-reference concepts using built-in Codex image_gen (no Higgsfield credits): Faceted Iris (blue/bronze), Atelier Iris (terracotta/cream), Breathing Iris (rounded glass fins). Saved PNGs and user reference under docs/demo/logos/; exact prompts and comparisons in docs/demo/friday-logo-concepts.md. Added docs/demo/twenty-second-treatment.md with0–6s wheelchair/fullscreen,6–12s responder/fullscreen,12–14s shared activation,14–17s centered speaking/pulsing logo and17–20s dual materialization. Indexed both documents; logo selection and new video production remain pending. Raster presentations are opaque, not transparent overlays/vector masters. No application changes, new paid video jobs, commit or push.

- Site-fit logo refinement started from latest screenshot: simplify the iris for small header use, muted terracotta/cream and existing espresso FRIDAY wordmark. Prepare refined logo and in-context header mockup with built-in image_gen; preserve original variants and do not change application code. Asset generation/QA pending.

- Site-fit refinement completed and visually reviewed: saved docs/demo/logos/friday-04-site-fit.png and friday-04-header-preview.png, with prompts in existing logo document. Header image is a generated mockup, not a pixel-identical edit or application change. Latest user direction confirms symbol only (no FRIDAY text) centered over both panes with spoken scanning line; preparing three warm icon-only variations and updating the 20-second treatment. No new paid video jobs.

- Icon-only logo set completed after interruption: no retained outputs found from interrupted jobs; generated Faceted, Soft and Orbit via built-in image_gen. Corrected Orbit background artifact and visually reviewed final three. Saved docs/demo/logos/friday-05-faceted-icon.png, friday-06-soft-icon.png and friday-07-orbit-icon.png; prompts in docs/demo/friday-logo-concepts.md. Updated indexed 20-second treatment for centered symbol without wordmark and exact scanning line. Opaque concepts only; icon selection, transparent overlay and new film production remain pending. No new Higgsfield spend or application edits from this demo work.

- 2026-09-20 (Local server shutdown): At user request, stopped FRIDAY standard stack, Haussmann preview and couch-model preview. Verified no listeners remain on ports5173,5181,5222,8000,8222. Unrelated application services left running.

- 2026-09-20 (Gaussian default): Switching default room to Haussmann, adding roomId query selection with room alias retained. Copied missing licensed SOG/surface from reviewed local worktree into ignored shared assets; verified both SHA256 against source. Packaging tests/build and database migration checks pending.

- Gaussian default verified: frontend scene suite and six packaging tests pass; production build passes (existing large-chunk warning). Live Vite serves restored SOG HTTP200 and proxied API health returns ok. Applied all15pending local database migrations successfully. Updated existing indexed Haussmann guide; roomId selects mesh, old room alias retained. No commit or push.

## Note from Saketh's lane — small follow-up in your editor: quiet saves (2026-09-20)

Not urgent, not changed by me. Context in `docs/backend/contracts/scene-api.md`, "The same principle in the PlayCanvas editor".

- Catalogue placements now survive a 5xx: my layer shows the item at once and drives your existing `session.retry()` on a 0.5 s to 8 s backoff (`frontend/src/catalogue/quietSave.ts`). `useRoomSession.ts` is untouched; the mount line in `SplatEditor.tsx` gained `retry={session.retry} status={session.status}`.
- **What lags, and is yours if you want it:** while a retry is running your header says *Disconnected* with a *Retry* button and shows the raw server message, the Objects list and layout count omit the item, and the editor is locked. For a save that fixes itself in a second or two, a quieter status (the legacy app says nothing until the second failure) would match the team rule that storage may delay a save but never refuse a placement.
- Drags and removes inside your editor still fail outright on a 5xx. Same fix, your call.
- Separate, already fixed on my side: the Cg Arch room's baked `scene.exposure` washed my floor overlay out to white; `regionOverlay.ts` now divides its emissive by it. If you change how exposure is applied, that line is the one to look at.

## Note from Saketh's lane — `AGENTS.md`: `main` freezes after the rehearsal (2026-09-20)

Two additions, from Saketh. Please read the new "Merging close to the demo" section.

- **Freeze.** Once the demo has been rehearsed end to end on the presenting machine, `main` takes only fixes to things that break that rehearsed run. No polish, no refactors. If unsure whether the freeze has started, ask before merging.
- **Chains stop on the first failure, and a status is quoted, not remembered.** `&&` not `;`, no pipe that eats an exit code, do not trust `set -e` blindly, and paste the `Ran N tests … OK` line you actually read. The full backend suite is `manage.py test` with **no app labels** (206 tests); a label list silently skips apps.
- **Said out loud, because this is what the freeze is for:** tonight #34 went into `main` without the suite being run and left `main` red, seven backend failures, until #37. Nobody was careless in an unusual way; it was a reasonable-looking asset PR late at night. That is exactly the change the freeze rule exists to stop from landing an hour before we present. Whoever merges runs the full suite on fresh `main` at once, and says so with the numbers.

## Note from Saketh's lane — the Cg Arch room now has a measured window; your bundle files are untouched (2026-09-20)

- New file **`shared/rooms/cg-arch-interior/openings.json`**: one window, `w1`, north wall, measured from your model's `Room Glass Windows` node (x 8.065 to 10.865 m, sill 7.5 cm, head 212.5 cm; the node is untransformed and your manifest places the model untransformed, so cm = m x 100). `python3 scripts/room_openings.py shared/rooms/cg-arch-interior` re-derives it.
- **I did not put it in `manifest.json` or `spatial.json`, on purpose.** `room_bundle.py` compares those byte for byte with the zip, so a new field would make `import-room.sh` fail for everyone with "Repository metadata differs". A test now fails if `openings` ever appears in either. If you would rather own openings in the manifest (and serve them on `Room`), that needs a re-packed bundle and is your call after the demo.
- It feeds the region solver only: "by the window" now narrows the lit floor to the patch in front of the glazing. Nothing in your engine reads it.
- For the real-GPU run: PlayCanvas 2.22.2 does support `KHR_materials_transmission` and your `requestSceneColorMap(true)` enables it. In software rendering the glazing reads as a flat pale panel. If frame rate is poor on the MacBook, that one line is the first lever; if the glass still looks flat there, a brighter emissive on that node would make the wall read as having an opening.

- 2026-09-20 (Evaluate Gaussian splats and surface reconstruction — main sync): Merged fetched origin/main0042078 into codex/haussmann-quality at b048e03; preserved both log histories and restored all stashed local demo documentation. Backend manage.py test: Ran221tests / OK(skipped=3); npm test:124+16passed,0failed; npm run build: built in4.69s. Existing owner-stack browser renders default Haussmann with Saved status and integrated catalogue. Gaussian board card remains Sin/incomplete because scale is still unverified. No new server, fetch or push.

- 2026-09-20 (Demo transition): Revising the indexed 20-second treatment so the responder shot continuously opens into two equal panes. Specify timing, subject-preserving reframing and edit method; no new media generation or application changes. Verification pending.

- Demo transition revision complete: docs/demo/twenty-second-treatment.md now specifies a 12–12.6s live pane reveal, gym reframing with aspect ratio preserved, synchronized activation at12.6–14s, and centered iris at14s. Added footage/framing, clipping-boundary and audio instructions. Verified old cut instruction removed,20s timing retained and INDEX link present. This is a production specification; transition has not been rendered or visually tested.

- 2026-09-20 (Seedance Mini production): User authorized revised film generation. Live preflight quotes14credits per14s720p source,28total; balance1121. Preparing two identity-referenced continuous shots with timed introductions, shared activation, native assistant voice and materialization; assemble20s edit with continuous pane reveal and Soft iris. New batch authorized separately from earlier two tests; no unbounded retries. Generation/QA pending.

- 2026-09-20 (One-shot correction): User rejected separate-shot/edit assembly and requested script review only. Stopped assembly work; rewriting treatment for one Higgsfield generation containing all camera transitions, split-screen transformation, dialogue, central icon and materialization. Two previous14s jobs completed and cost28credits; they are superseded tests. No further generation authorized in this correction. Mini live catalog limit is15s, so preserve requested20s director script while flagging submission incompatibility.

- One-shot script revision complete: rewrote docs/demo/twenty-second-treatment.md for one generated continuous sequence, including doorway transition and generated unfolding into equal panes. Marked twenty-second-edit.jsx superseded and updated INDEX. Verified links and removal of editorial assembly instructions. Flagged Mini15s maximum against requested20s timing, retaining15s alternative as unselected. No new jobs. Prior cloud composite/project export was blocked by automatic approval review; no composite was rendered or uploaded.

- 2026-09-20 (Video model pricing): User requested all model prices and recommendation. Reading complete41-entry connected video catalog and live cost estimates, including defaults/variants and exact20s quotes. Preparing indexed comparison; no generation or credits spent.

- Video model comparison complete: docs/demo/video-model-pricing.md and assets/video-model-pricing.json preserve all41connected catalog entries, live quotes/settings/errors and variant prices. Indexed and verified41/41IDs. Recommendation: Wan3.0 thinking-enabled720p20s at35credits; Seedance2.5 alternative140credits. Mini remains limited15s. No generations submitted or credits spent in this comparison.

- 2026-09-20 (Wan single-shot test): User selected720pWan after35-credit quote. Preparing one20sWan3.0 generation with native audio/thinking and all transitions, two panes, icon and materialization generated inside the same job. Preserve20s script; no editorial assembly. Uploading user-provided iris reference for the authorized generation; preflight/submission pending.

- Wan one-shot attempt failed: submitted exactly one20s720pWan3.0 job de7fa547-4831-4686-b5a2-4a63aae03176 with thinking/native audio and all scenes/transitions in one prompt. Live quote35credits, terminal failed status confirmed twice, no output/reason returned. Uploaded user iris reference f7e569ac-9119-47c2-adb4-4c5056956a4c successfully. Saved exact request/status in docs/demo/assets/wan-one-shot-request.json and corrected treatment status. Browser error inspection blocked because Chrome Computer Use was not approved; last balance1051, actual charge/refund unverified (no transaction ledger). No retry, model switch, edit or additional video submission.

- Wan retry authorized: user reports the first failed attempt was refunded and explicitly requested another try. Retrying once with the saved20s720pWan3.0 single-generation prompt, same references, native audio and thinking. Cost preflight/submission status tracked separately; no model switch or editorial assembly.

- Authorized Wan retry submitted as269355d1-822c-4109-badc-e0798d760d66 at live35-credit quote; terminal failed within seconds, no output/provider explanation. User-confirmed first refund recorded separately; retry refund unverified. Preserved identical single-generation20s720p settings and references in docs/demo/assets/wan-one-shot-retry.json; updated treatment and first-attempt history. No third submission, browser access attempt or model switch.

- Post-Wan alternatives checked: live20s720p/audio quotes remain Seedance2.5=140credits,FLUX3Video=110,Wan3Prime=60. Official Higgsfield docs confirm Seedance2.5 up to30s andFLUX3 up to20s with audio. Recommend Seedance2.5 next to preserve the script while trying a different model family from the failed Wan jobs; quality on this exact scene untested. No generations submitted.

## Note from Saketh's lane — CI now runs on every pull request (2026-09-20, 00:55)

`.github/workflows/ci.yml` is on `main`. Every PR gets two checks within about a minute: **Backend** (`manage.py test` with no app labels, plus `manage.py check`) and **Frontend** (`npm test`, `npm run build`). No secrets, nothing to configure.

- **A red X on your PR means do not press Merge.** Tonight #34 and #42 both went into `main` without the suite and left it red; each was caught only because someone happened to look. This is the thing that stops that.
- It is **advisory**, not a required check: it will never block an emergency fix.
- It does not replace running the suite on fresh `main` after you merge. CI tests your PR against the `main` it was cut from.
- `npm test` is now the whole frontend suite (it used to skip the two auth test files), and the backend suite is `manage.py test` with **no labels** (`test api catalogue` skips three apps). README has the three commands.
- When you report a result, name the command and quote its output line, e.g. "`python manage.py test` → Ran 217 tests in 6.951s / OK (skipped=3)", not "tests pass".

- 2026-09-20 (Evaluate Gaussian splats and surface reconstruction — latest main sync): Resolved the sole TODO_SIN.md conflict by retaining both branch histories while integrating fetched main0f44bc5. Backend manage.py test --noinput: Ran232tests in7.786s / OK(skipped=3). npm test:137+16passed,0failed; npm run build: built in2.48s with existing chunk warning. Browser verification unavailable because owner localhost5173 server is stopped; no servers started. Gaussian board card remains Sin/incomplete for unverified scale. Untracked demo assets preserved; no fetch or push.

## Done — Animate AI furniture moves with halo blobs

- Approved white halo-blob concept; AI placements/moves only. Isolated codex/angel-movers checkout. Build procedural walking/pushing characters, explicit accepted-agent motion metadata, bounded validated visual movement, and lifecycle/capture handling. Preserve authoritative target poses; initial loads and manual edits do not animate. Tests and browser verification pending.

- Implemented backend receipt metadata in backend/api/scene_service.py; validated frontend metadata and integrated procedural halo helpers, checked move paths, capture isolation and reduced-motion handling. Approved concept and behavior docs saved under docs/frontend/angel-movers/ and linked from INDEX.md. Manual drag/commands remain immediate; initial loads and unchanged event revisions do not animate. Camera-look cancellation fixed during review.
- Verification: backend manage.py test --noinput reported Ran238tests in26.786s / OK(skipped=4); npm test reported146+16passed,0failed; npm run build reported built in8.89s (existing chunk warning). Isolated browser rendered Haussmann, accepted backend AI additions/moves, displayed two procedural helpers, then returned to settled furniture without helpers. Unit regressions cover manual event clearing, invalid metadata, retry identity and blocked transit. No separate full-GPU performance benchmark or exact visual match to generated concept claimed. Preview processes stopped; lsof confirms5233/8233closed. Work isolated on codex/angel-movers, not committed or merged into the active demo branch.

- Angel scope update: user requested helpers for manual additions too. Single-item accepted add commands now emit the same motion metadata; choreography queues until manual placement finishes. Manual moves and multi-command history restores stay immediate. Focused regression and browser verification pending.

- User-add verification: manage.py test api.test_agent_motion --noinput:7tests/OK; npm test:146+16passed,0failed; npm run build:built in7.35s. Preview reopened at127.0.0.1:5233 and left running for the user-requested browser demonstration. Manual UI add choreography is enabled after acceptance; user is currently placing a sofa. Not committed or merged.

- Visibility fix: user reported missing angels. New-add approach candidates now favor the camera-facing side, and choreography lasts longer (0.9s approach, at least2.4s push,1.2s celebration,0.9s departure). Added direction regression; frontend147+16tests pass, build passes. Actual browser screenshot of two halo helpers pushing captured in .scratch/friday-angels-live.png using development-only ?angelScreenshot pause. Temporary5234/8234capture stack stopped; user5233preview remains running.

- Commit verification: final full backend manage.py test --noinput reports Ran239tests in25.994s / OK(skipped=4); frontend147+16passed; final build built in9.53s. User approved live screenshot and requested commit of the angel movers, manual-add support and visibility fix.

- Main integration verification: resolved editor, INDEX and Kanban conflicts while retaining halo-helper motion, incoming shopping UI and both task records. Merged fetched origin/main59d26c1. Root / explicitly opens Haussmann Gaussian; ?roomId retains alternate room access. Full backend: Ran270tests in8.479s / OK(skipped=5). npm test:152scene+16packaging/auth passed,0failed; npm run build:built in2.14s (existing chunk warning). Applied incoming migrations to isolated preview DB and verified actual Haussmann render at127.0.0.1:5233/ after reload. Preparing local main fast-forward; no push. Original dirty demo checkout preserved. User-requested5233preview remains available.

- 2026-09-20: Corrected furniture-demo handoff to the default Haussmann Gaussian room. Verified actual GLB placement on a valid footprint and Preview summon playback in an isolated browser session; inspected final scene. No renderer/default-room change was needed. Updated indexed furniture-materialization.md; empty-room remains a test fixture only.

- 2026-09-20 In progress: Build agent-observation shopping choreography. User rejected the ecommerce cart; replacing with automatic luminous-agent collection and origami dispatch over the real Haussmann Gaussian room. Remove quantity/checkout controls; retain discreet developer replay. Reuse existing generated video with no additional spend. Verification pending.

- Agent-observation shopping choreography: implemented automatic room-ready gather/compose/dispatch over Haussmann, observer-only renderer and hidden developer replay. Reused media; no additional generation. Initial browser review found the editor capture worker still polling; disabled it for observation so watching does not claim agent capture jobs. Isolated run-local.sh stack restored on 5230/8230 with its own database after the previous backend stopped. Final checks in progress.

- 2026-09-20 Done: Build agent-observation shopping choreography. Automatic Haussmann-backed gather/compose/origami dispatch, observer-only scene, hidden developer replay and media failure states implemented in checkout/CartPreview.tsx, cart-preview.css, SplatEditor.tsx and PlayCanvasScene.tsx. Browser checks passed: automatic playback, replay, reduced motion, failure recovery without false success, no horizontal overflow, no page errors and zero POST requests including keyboard shortcuts. Frontend: 127/127 scene + 16/16 packaging/auth tests; production build passed (existing large-engine chunk warning), design detector empty, whitespace passed. Documentation updated and indexed; no additional media generation, no commit/merge/push. Sequence is simulated presentation; live agent event wiring remains separate.

- In progress: Refine furniture summon reveal. User rejected the floating transparent fade. Replace it with an opaque, floor-up shader reveal with a narrow warm leading edge, no position/scale animation, and unchanged authoritative geometry. Verify on Haussmann.

- Done: Refine furniture summon reveal. Replaced float/scale/transparency with a 1.4s bottom-up fragment reveal and thin warm edge in scene/playcanvas/materialize.ts. Preserves textures, opaque depth, authoritative pose and captures; suppresses premature shadow and restores original materials/shadow state. Updated materialize.test.ts and indexed furniture-materialization.md. Real Haussmann GLB placement/replay and mid/final screenshots checked; no shader compile failures observed. Verification: 127 scene + 16 packaging/auth tests pass; build and whitespace pass. No added dependency or generation cost; no commit/merge/push.

- In progress: Refine furniture summon reveal with elegant golden threads. User requested a restrained champagne-gold weave: surface-anchored antialiased filaments lead the opaque reveal, then softly disappear into the original fabric. No additional generation.

- Done: Elegant golden-thread summon refinement. materialize.ts now uses a 2.2s restrained gold reveal; goldenThreads.ts draws three flowing curves with moving highlights using built-in PlayCanvas lines. Verified active and settled Haussmann GLB preview in Chrome; no shader errors. 127 scene + 16 packaging/auth tests pass; production build passes (existing chunk warning). Documentation remains indexed. Decorative threads ignore scene depth; authoritative placement and captures are unchanged. No generation cost, commit, merge or push.

- 2026-09-20 Commit handoff: reviewed cart choreography, bundled courier media, observer-mode safeguards, golden-thread summon, tests and indexed docs for commit on codex/shopping-cart-animation. Verification logs: npm --prefix frontend test → tests 127 / pass 127 / fail 0; tests 16 / pass 16 / fail 0. npm --prefix frontend run build → built in 13.80s (existing chunk warning). User-approved preview remains on the isolated stack; no push or merge requested.

- In progress: merge cart choreography and golden-thread summon into local main using isolated codex/merge-cart-summon. Preserve latest shopping routes, pointer-lock controls, collapsible catalogue and halo-helper motion. Concurrent dirty demo checkout untouched; full merged checks pending.

- Merge handoff: resolved shopping-cart-animation against local main fc507e9, preserving shopping routes, pointer-lock controls, halo helpers and both documentation histories. Backend manage.py test --noinput: Ran 270 tests / OK (skipped=5). Frontend npm test: 155 + 16 tests passed, zero failures; npm run build: built in 1.97s. Browser integration check was interrupted; user explicitly requested immediate merge. No push.

- Push rejection reconciliation: owner pulled origin/main into the checked-out demo branch, leaving local main behind origin/main 2dd8a53. Integrated that fetched main into the isolated merge branch without conflicts, retaining cart/summon and incoming category rail/jump/gravity work. Verification: backend 270 tests / OK (skipped=5); frontend 159 + 16 passed, zero failures; build built in 2.16s. Advance local main only; no fetch, push or edits to dirty demo checkout.

- In progress: resolve fetched skyscraper merge on main, retaining both documentation indexes, floor-bound capture readiness and observer capture suppression. Preserve active-floor readiness and observation notifications. Full verification pending; unrelated demo files remain untouched.

- Merge resolution complete: retained skyscraper floor readiness, keyed captures and observer capture suppression; preserved both documentation indexes and observation callbacks. Backend manage.py test --noinput: 274 tests / OK (skipped=4). Frontend npm test: 165 scene + 18 packaging/auth passed, zero failures; npm run build: built in 2.30s. User will perform run-local verification before pushing; no browser verification claimed, no servers started and no push performed.
