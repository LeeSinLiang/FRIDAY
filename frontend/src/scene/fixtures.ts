import type { Product, Room } from './types';
import fixtures from '../../../shared/scene-fixtures.json';
export const ROOM: Room = fixtures.room;
export const PRODUCTS: Product[] = fixtures.products as Product[];
