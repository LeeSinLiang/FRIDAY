# William — tasks and agent work log

Record all agent work here when working for William. Include status, file paths, verification, and blockers or handoff notes.

## In progress

- None.

## Next

- Confirm scope and ownership with the team.

## Done

- 2026-09-19: **Prepare the Kanban-only feature-branch commit.** Reviewed the extension, tests, board data, `AGENTS.md`, board README, and related index/work-log entries for the requested push to `codex/fix-kanban-controls`. Excluded concurrent furniture assets and their documentation entries. Clarified that agents must maintain the board after meaningful edits without a reminder; task inference from file changes is not implemented. All 9 board tests, board/server/extension syntax checks, and whitespace checks passed. The running standalone HTML still matches the Chrome-verified SHA-256 below. Commit documentation includes the exact standalone launch command and the owner/task upkeep workflow.
- 2026-09-19: **Import whiteboard tasks and update Kanban upkeep rules.** Added 21 open tasks from the user's `IMG_5126.HEIC` through the live board API into `.github/extensions/project-manager/data/board.json`. Preserved all six user-defined components, their IDs/order/metadata, and all three existing cards, including Sin's wireframe assignment. Assigned the clearly named object-model, UI/platform, constraint, and Visa work to Adelle, Sin, and Saketh respectively; other ownership remains Unassigned. Photo options such as Gaussian splats and ElevenLabs are evaluation tasks. No imported task was marked implemented.
- Updated `AGENTS.md` to require ongoing Kanban/TODO synchronization after meaningful edits, newly discovered tasks, verified completion, and handoff. Completed work moves out of active TODO sections into **Done** before its card is removed; obsolete-task removal needs a recorded reason. The board README now records the user's current six components, photo source, and upkeep workflow. `server.mjs` hides all column descriptions without modifying component data or the migration/default definitions.
- Verification: all 9 isolated board tests, server syntax, whitespace checks, and the `CLAUDE.md` symlink check passed. After restarting the exact standalone entrypoint, all 21 additions and the original cards persisted; an assertion compared every component's metadata with the pre-import snapshot. The user's existing Chrome tab shows six unchanged column names, no descriptions, and the imported cards/owners. Verified completion using this maintenance card; its evidence is retained here so it can be removed from the active board.
- Runtime: one standalone board process from this checkout, PID 68496 at verification, running `node /Users/williamxu/Desktop/Projects/hackmit2026/.github/extensions/project-manager/bin.mjs`; Chrome uses `http://127.0.0.1:56807/`. Served HTML matches fresh current-source output, SHA-256 `5e61b43a063160794aec99a2ee7c82e8168db3be57ca23c22afbff2ed98afafe`. Pre-import data backup: `/tmp/hackmit-whiteboard-LOU8T7/board-before.json`. This task did not change app code, other owners' logs, or concurrent asset work. Changes remain local and uncommitted.
- 2026-09-19: Replaced Kanban phase tags with named owners in `.github/extensions/project-manager/board.mjs`, `server.mjs`, and `extension.mjs`. William is blue, Sin amber, Saketh teal, Adelle purple, and Unassigned grey; named selectors, card tints/borders, and a legend use the same palette. HTTP and Copilot actions now assign `owner`; new cards default to Unassigned and completion stays separate.
- Aligned the board with the current proposal and supplied Visa context: Room & 3D Models, Spatial Solver, Catalogue & Elasticsearch, AI Agent & Voice, Frontend & UX, Backend & API, Cart & Visa IDX, and Integration & Demo. Each area has a concise scope description. Schema version 2 migrates old flat/phase-grid boards once, preserves custom columns and all task IDs/titles/completion, consolidates legacy generic areas, and retires phase labels without guessing ownership. Moved the existing `Build task list UI` card into Frontend & UX; all three original cards remain unassigned. Updated the board README and INDEX.md with component scope, owner colours, API/action changes, migration, and verification.
- Verification: all 9 isolated board tests passed, including old-format migration, preservation of later customisations, owner API validation/persistence, and the previous control regressions. Syntax checks for server/extension and `git diff --check` passed. In the user's existing Chrome tab, verified the five owner options, distinct colours for all four people, default Unassigned, ownership preserved through native dragging, and ownership/component preserved after a real server restart. Removed only the disposable owner-test card and compared the three original task IDs/titles/completion values against the pre-migration snapshot; all match.
- Current Kanban runtime: one standalone process from this checkout (PID 63821 at verification), launched through the exact `node /Users/williamxu/Desktop/Projects/hackmit2026/.github/extensions/project-manager/bin.mjs` workflow. Existing Chrome tab now points to `http://127.0.0.1:55272/`. Served HTML matches fresh current-source output, SHA-256 `2cb20482b05f50e0f736df49faf0cc843850e7a034d5c642b1340b114f43ada1`. Pre-migration data backup: `/tmp/hackmit-owner-migration-RuwXcE/board-before-owners.json`. The unrelated Copilot repository/runtime was not changed; its canvas host was not used for this standalone verification. Changes remain local and uncommitted on `codex/fix-kanban-controls`.
- 2026-09-19: Fixed standalone Kanban controls on `codex/fix-kanban-controls` for William; [issue #1](https://github.com/LeeSinLiang/hackmit2026/issues/1). `.github/extensions/project-manager/server.mjs` now accepts drops across columns and on cards, supplies standard browser drag data, offers a component selector, and shows save errors. Task drafts survive failed saves and board rerenders; successful actions refresh directly without depending on live events. `board.mjs` now calculates the original drop slot before removing a card, fixing downward moves to the bottom. Updated the board README with usage/restart guidance and linked it from INDEX.md.
- Verification: reproduced the inert column drop path and the incorrect `[B,A,C]` result for moving A to the end of `[A,B,C]`. All 6 isolated `board.test.mjs` regressions pass; syntax and whitespace checks pass. In the user's existing Chrome tab, verified task creation by Add and Enter, empty-title feedback, native dragging onto column space and to the bottom of three cards, the component selector, completion, phase changes, deletion, and persistence after restarting the standalone server. Stopping the server produced a visible save error and preserved the typed draft. The initially reported add failure did not reproduce in Chrome before the patch; the new feedback makes failures visible.
- Runtime delivery: stopped the original standalone process and relaunched the exact `node /Users/williamxu/Desktop/Projects/hackmit2026/.github/extensions/project-manager/bin.mjs` entrypoint. One process from this checkout is active (PID 59325 at verification); Chrome now uses `http://127.0.0.1:53601/`. Served HTML SHA-256 `bc23ca23c8fed8c3b0c128e7c5568694c9f875d2a2ca4ea455ea4b5a7687fd26` matches freshly generated current-source HTML. The Copilot board in `/Users/williamxu/Documents/HackMIT` belongs to a separate GitHub repository and was not changed. Removed only temporary test cards; `data/board.json` has no diff. A concurrent work-log review entry below was preserved.
- 2026-09-19: Reviewed `INDEX.md`, `AGENTS.md`, `README.md`, `proposal.md`, the four owner TODO files, and `.github/extensions/project-manager/README.md` for the requested project workflow summary. No application code or other documentation changed. Verification: read the current local files and checked branch status and diff; the existing `codex/fix-kanban-controls` changes remain in progress. Handoff: agree on named ownership and shared contracts before overlapping MVP implementation; no build or runtime verification was needed for this read-only review.

## Blockers / handoff

- The user's `test` card and original cards are retained under Frontend & UX. Remaining Unassigned photo tasks need team ownership; the import does not establish implementation status or guarantee that optional techniques fit the MVP.
- Delivery uses the requested `codex/fix-kanban-controls` feature branch; do not assume `main` contains the fix. Issue #1 remains open for integration review. The standalone launcher chooses a new URL on every restart; use the URL printed by the running process. Board processes cache their checkout's JSON; restart after external board-file edits or pulls. No Django/React application code changed or full-stack checks were needed for this standalone tool.
