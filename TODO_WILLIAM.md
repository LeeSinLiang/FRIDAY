# William — tasks and agent work log

Record all agent work here when working for William. Include status, file paths, verification, and blockers or handoff notes.

## MUST DO — Shared room test download

- [ ] **Prepare and verify the shared Haussmann room locally** — required for each collaborator.
- Use [HAUSSMANN APARTMENT](https://superspl.at/scene/4de797f4) as the shared Gaussian room test download. SuperSplat login may be required. Credit Stéphane Agullo (sa3d), CC BY 4.0; retain the downloaded license. This is an authored test scene with assumed scale, not a measured room.

After downloading, keep the ZIP intact and run these commands from the repository root after saving current work. The `&&` chain stops at the first failure:

```bash
git switch main &&
git pull --ff-only &&
./setup.sh &&
python3 scripts/gaussian_room/prepare_haussmann.py "$HOME/Downloads/HAUSSMANN APARTMENT.zip" &&
python3 scripts/gaussian_room/review_surface.py &&
BACKEND_PORT=8222 FRONTEND_PORT=5222 ./run-local.sh
```

Open **[http://localhost:5222/](http://localhost:5222/)**, or `http://localhost:<FRONTEND_PORT>/` if using another frontend port. **Haussmann is already the default; no `?room` parameter is needed.**

Adjust the ZIP path if needed. Run preparation in a normal terminal for GPU access; the previously reported run took about 2½ minutes. Preparation is a one-time step and refuses to overwrite existing output. If already prepared, skip preparation and run the surface review and stack commands. Generated room assets are ignored by Git, so each collaborator must prepare them locally. Keep the attribution/license. This remains an experimental test room with assumed scale and known downward/ceiling/window artifacts. Stop the stack with Ctrl-C when finished.

Tracking update (2026-09-20): standardized this MUST DO checklist across `TODO_SIN.md`, `TODO_SAKETH.md`, `TODO_WILLIAM.md`, and `TODO_ADELLE.md`; synchronized owner-specific Kanban tasks. Checked the default `/` route in `frontend/src/App.tsx`. Local preparation and room verification remain pending for each collaborator; this documentation update does not claim they ran.

## In progress

- **Connect room, catalogue, and user input to the OpenAI API agent — spoken placement cue** (existing Kanban `aa3279ee-e155-4920-a5e4-f51eba493418`, William). On clean merged base `5afe392`, traced microphone transcript → `CatalogueShelf.run` → `useRoomSession.design` → `/api/designer/`. Preserved speech provider and UI; added “do it now” before/after a request and as a standalone cue for a newly entered description; empty/repeated standalone cues make no edit. Changed `frontend/src/scene/designerIntent{.ts,.test.ts}`, `frontend/src/catalogue/CatalogueShelf.tsx`, `backend/api/designer_agent.py`, and the indexed scene-designer guide. `./setup.sh` succeeded. Baseline `uv run python manage.py test` → Ran 390 tests in 17.968s / OK (skipped=5). Final backend → Ran 390 tests in 12.001s / OK (skipped=5); `npm test` → 196 + 25 pass, 0 fail; `VERCEL=1 npm run build` → built in 3.88s. Deepgram transcribed a locally recorded WAV to “Place a workstation desk at x 300, z 200, and put a lamp on its desktop. Do it now.” (837 ms). Pasting that exact transcript into the live editor started an agent job with the cue removed and saved desk X300/Z200 plus lamp attached to reviewed Desktop Y75, revision 0→1; screenshot `.scratch/voice/saved-desk-lamp.png`. Repeating bare cue kept revision 1 and showed an error. Entering an unsubmitted chair description then submitting bare cue started a job with that prior description and saved chair X300/Z350, revision 1→2; screenshot `.scratch/voice/saved-chair-after-cue.png`. A browser tab closure during reasoning resumed the same job. The actual microphone button was not exercised through a physical microphone; provider transcription and the shared UI dispatch path were verified separately. Current main advanced to `46930f6` via lighting PR #102; reconcile before PR/merge or production refresh. Separate detailed asset lane remains untouched.
- **Match GLB furniture lighting and support shadows to prepared rooms** (Kanban `7424b217-243e-4f36-8ab4-19d407b83f0a`, William, open). Isolated `codex/room-furniture-lighting` from remote main `9adbf97`; locally integrated completed stacking `76fd9ae` without touching other checkouts. Baseline: backend `Ran 281 tests / OK (skipped=5)`, frontend `181+25 passed`, build `6.21s`; integrated: backend `Ran 284 tests in 15.195s / OK (skipped=5)`, frontend `184+25 passed`, build `5.15s`. Plan: matched camera captures in London, empty, CGArch and Haussmann; preserve architectural texture/exposure and authored PBR, add room-specific furniture illumination and support-aware contact; share setup with frozen captures; test move/rotate/undo/reload, disposal and frame pacing. Port prototype c12abd1 selectively. Initial implementation was local only; the follow-up below authorizes publishing and merging this work.
  - Implemented room-scoped furniture layers, authored PBR-preserving cloned materials, ambient/directional profiles, lit support shadows and restrained baked-floor contact. All visual pose updates now also update the finish origin using the same support height; frozen captures reuse that path. Added a development-only matched-room fixture and six regression cases. First real-engine inspection caught shadow acne on the table; tuning shadow resolution/bias before acceptance. Baseline images cover empty, London, CGArch and Haussmann. Studio 11 and CGArch lightmapper proof assets remain absent. Added indexed `docs/frontend/room-capture/furniture-lighting-migration.md`. First new checks: `npm test` passed `190+25`, `npm run build` completed `4.54s`. Close London view shows the vase casting a geometry shadow on its tabletop at the derived 74 cm support height.
  - Acceptance found two concrete issues: realtime shadow recalculation raised London frame times, and stacking selected floor-height bounds for raised items. Reduced the map to 1024/6 m, added bounded camera/edit/animation/capture shadow refresh, and corrected picking/selection footprints using supportHeightCm. Added cache invalidation and small off-centre raised hit/miss regressions; rerunning visual/performance checks. No collision, save or cart rule changes. Cached 30 s London walkthrough: median 19.90 ms, p95 25.00 ms, 1 frame above 50 ms (1361 measured frames); original baseline median 12.00 ms, p95 30.60 ms, 5 above 50 ms. First realtime version was rejected at p95 51.60 ms. These are shared-workstation browser timings. CGArch uses 7.78954× baked exposure; an inverse-exposure compensation trial was visually rejected for crushing furniture midtones. Retained the architectural exposure and tuned the room-specific ambient/key instead. Latest full checks before that small frontend correction: backend `Ran 284 tests in 13.861s / OK (skipped=4)`, frontend `192+25 passed`, build `3.65s`.
  - Normal editor acceptance: isolated `rendering.localhost:5247` prevents guest-cookie collisions with other running localhost stacks. Chrome selected the small raised vase, dragged it from (445,290) to (425,280), rotated 90 degrees, and undid rotation; server revision 3 recorded the rotation and the UI returned to 0 degrees after undo. Four production photographic captures are saved. The development fixture is now formatted/typed and included in `tsconfig.json`; `npx tsc --noEmit` passes. Reload/room-switch and final build checks continue. No session code was changed.
  - Final local acceptance: `manage.py test` → `Ran 284 tests in 18.841s / OK (skipped=4)` with provider keys blank; `npm test` → `192/192` and `25/25`, zero failures; `npm run build` → `built in 5.30s`. Chrome normal editor verified tabletop drag, rotate, undo, reload, London lobby-to-mezzanine floor movement, and returning to the saved empty-room layout. Persisted revision 4 has vase (425,280), yaw 0. Source renders and production photographic captures were visually inspected for all four available rooms. Fresh production preview at rendering.localhost:5248 renders the saved furniture; HTTP renderer bytes equal fresh `frontend/dist/assets/PlayCanvasScene-C6JgMZhl.js`, SHA-256 `b270f8630cda131a419965f184f573cb4b04faeb38af60ad40a7b68b655edf3a`. No browser warning/error after normal workflow. Indexed migration document records dependency/application steps and evidence. Card remains open for absent Studio 11/lightmapper-proof visual acceptance and any subsequently authorized hosted migration; no remote push/merge/deployment.
  - Cleanup: stopped this task's canonical stack and production preview, closed both temporary browser tabs, and verified `lsof -nP -iTCP:5247 -iTCP:5248 -iTCP:8247 -sTCP:LISTEN` returned no listeners. Other worktrees and owner ports were not stopped.
  - Main integration authorized by the user: publish this feature branch, open a PR and merge the rendering migration with its completed stacking dependency after checks. Remote main is still `9adbf97`; fresh detached-main `./setup.sh` passed, backend `Ran 281 tests in 15.878s / OK (skipped=4)`, frontend `181+25 passed`, build `4.10s`. Main is checked out in another teammate worktree, so verify its exact remote commit in this isolated checkout rather than switching or modifying that worktree. Re-run the full suite/build after the GitHub merge; Vercel publication is a separate, unperformed action. Card remains open for the explicitly recorded unavailable-asset/hosted acceptance limits.
  - Publication checks on the exact rendering branch: `manage.py test` → `Ran 284 tests in 13.980s / OK (skipped=4)`; `npm test` → `192/192` and `25/25`, zero failures; `npm run build` → `built in 4.09s`. No renderer code changed after the prior browser/production-artifact verification. Preparing the PR with these measured results and the missing-asset/performance limits.
  - PR #102 (`https://github.com/LeeSinLiang/hackmit2026/pull/102`) initial CI caught a dependency integration gap: frontend runner passed 191/192 scene tests but could not spawn `uv` for stackingAgreement (null exit status); its job had no Python environment. Added setup-uv and frozen backend install to the frontend CI job, retained the real agreement test, made the probe use the lock file and report spawn errors directly. No application code changed. Local repair checks: `npm test` passed `192+25` with zero failures; `npm run build` completed `2.92s`. Existing rendering card remains open; merge waits for successful CI.

  - Remote main advanced to `5afe392` while PR #102 CI ran. Fresh-main baseline: backend `Ran 390 tests in 17.735s / OK (skipped=4)`, frontend `195+25` passed, build `3.99s`. Merging main into this branch and preserving its explicit reviewed support/compartment attachments, designer, catalogue, backend, UI and saved-scene contracts. Retired this branch's superseded inferred-stacking probe/docs/tests and its temporary CI setup change; current main support tests remain intact. Renderer/finish now consume authoritative resolved `pose.yCm` (table 75 cm, middle shelf 51.5 cm), including parent transforms and detachment. New regression uses the current attachment resolver; development comparison fixture uses the reviewed table. Integration checks and visual replay are running; hosted deployment remains outside this merge.

  - Current-main integration acceptance: full backend `Ran 390 tests in 17.884s / OK (skipped=4)`; `npm test` `202/202` plus `25/25`, zero failures; build `5.87s`. Chrome normal editor selected the box through the cabinet opening, dragged X250→270, undid, moved cabinet X270→320, rotated 90 degrees, then undid rotation and reloaded. Server revision 9 recorded child (322,135,Y51.5) under the rotated parent with its local attachment unchanged. London fixture confirms the vase at Y75 with real tabletop shadow; move/rotation and production photographic capture preserve contact. Empty-room screenshot shows the shelf shadow and floor shadows. Saved evidence `.scratch/rendering/integrated-*`. The main designer, supports, catalogue, API, capture and navigation files remain byte-identical to `5afe392`; rendering consumes their existing resolved poses. Old 74 cm/LISABO results above are historical. The obsolete inferred-stacking files are removed from this branch because main superseded that contract, with all current support tests retained. Preparing integration push and CI.

- **Publish integrated FRIDAY test release and record demo** (Kanban `25675e31-aaa6-43d7-a563-b94360fc6706`). Isolated release on `codex/friday-demo-release`: snapshot parent scene designer and explicit supports, merge current gallery and room boundaries, run full tests and browser workflow, feature PR into main, refresh matching Vercel API/frontend, verify public placement and record demo. High-detail workstation assets remain unfinished in the parent lane and are excluded. Fresh main baseline: `manage.py test` → `Ran 281 tests in 15.790s / OK (skipped=5)`; frontend tests passed; `VERCEL=1 npm run build` → `built in 4.15s`. Combined snapshot verified: `uv run python manage.py test` → `Ran 390 tests in 14.199s / OK (skipped=5)`; `npm test` → `195/195` plus `25/25`, zero failures; `VERCEL=1 npm run build` → `built in 3.52s`. Live browser acceptance is running at friday-release.localhost:5286. Production and video verification pending.

- **Replace low-poly workstation props with textured GLBs** — William. User requests detailed, individually recognizable desk and accessory models, leaving books unchanged. Replace eleven `shared/models/furniture/pc-workspace-*` GLBs, preserve catalogue IDs/dimensions and reviewed support target geometry, embed real PBR textures, update source/license/hash metadata and thumbnails. Verify measured GLB bounds, desktop/shelf contact and saved attachments, canonical build/runtime identity, and browser screenshots. The existing scene-agent implementation remains in progress; this asset upgrade is the immediate priority.

- **Connect room, catalogue, and user input to the OpenAI API agent** — William, card `aa3279ee-e155-4920-a5e4-f51eba493418`. User explicitly requests FRIDAY's in-app agent to reference existing objects and place/move items relative to them. Plan: reuse current scene/command/capture services, add bounded inspection/catalogue/relative-placement tools, stage a validated layout then commit atomically with revision/idempotency and existing undo/retry semantics; send real capture pixels for visual verification. Reuse the existing catalogue sentence input and notices. Keep the stateless search compiler, checkout and geometry formats intact. Read the existing Blender MCP handoff; Saketh's broader context-research card remains assigned to him, this card owns execution wiring. Local ignored .env now contains the user-provided backend credentials (mode 0600); verified presence without logging values. Live-model acceptance remains pending.
  - Current user scope (supersedes the whole-building goal below): implement support-aware placement through the live in-app agent. Demonstrate a monitor on a reviewed desk surface and an object inside an explicit cabinet compartment, stable parent/surface IDs and local coordinates, derived height, full bounds/collision checks, inherited parent movement/rotation, screenshots, persistence and undo/redo. Reuse the existing supported-placement worktree and PC-workspace GLBs; preserve that lane and the current UI, floor placements and shopping/checkout. Autonomous furnishing of all 34 floors is cancelled. The active goal tool does not expose an objective-edit operation; this explicit user scope governs remaining work.
  - Baseline: PR100 merged as 9adbf97, followed by setup and fresh-main checks: manage.py test Ran 281 tests in 12.514s / OK (skipped=5); npm test 181+25 passed; build 3.01s. Latest main includes Saketh's editor fixes; preserve them. Skyscraper corrected release is merged; production refresh will include the verified agent integration.
  - Implementation in progress: api/designer_geometry.py, designer_agent.py, designer_service.py and designer_views.py provide scoped references, relative geometry, staged edits, idempotent commit and image review. Existing CatalogueShelf and useRoomSession route explicit edit requests through the same input and undo/retry flow. Added indexed docs/backend/scene-designer/README.md. Protected viewer clearance in candidate selection and aim verification cameras toward changed objects. Enforced 35 s planning + 8 s capture + 8 s image review inside the existing 60 s host budget. Tests and actual model/browser verification remain pending; card stays active.
  - Expanded user goal from the side conversation: autonomous design/furnishing across all 34 reviewed London floors, with capability sanity tests before integrated acceptance. Delegated bounded parallel lanes: scene context/measurement (new designer_context module), capture/previews (designer_vision and capture_service), independent acceptance audit. Parent owns durable async model jobs, typed tools, frontend polling and release. Shared contract: stable furniture/fixed IDs, explicit roomId and geometry/scene revisions, local floor centimetres, yaw radians, +Y up/+X right/-Z front, no invented architecture/support semantics. Initial live table placement exposed a long transaction around the old scene endpoint and missing browser persistence; screenshot/whole-building acceptance remains incomplete. Initial checks: backend Ran 293 tests in 12.374s / OK (skipped=5) with SEARCH_BACKEND=memory, frontend 184+25 pass, build 3.99s.
  - Parallel capability sanity checks: context/3D measurement Ran 13 tests in 0.277s / OK; independent rotated GLB/holes/obstacle acceptance Ran 10 tests in 0.278s / OK; vision+existing capture tools Ran 22 tests in 0.601s / OK, installed PlayCanvas worldToScreen matched 8 projected cases within 0.000498 px. New durable jobs and HTTP ownership/reload tests Ran 22 tests in 1.036s / OK. Parent added short /api/designer/ begin/poll requests using Responses background mode, Astra xhigh, leases, pre-edit two-view input, target capture, mandatory staged preview, atomic commit and post-review. This supersedes the earlier synchronous 35 s planning budget. Existing scene GET offers scoped active-job resume; unchanged checkout middleware is bypassed only by this separately authorized endpoint. Browser cookie collision between different local databases was reproduced with a fresh HTTP cookie jar; isolated friday-designer.localhost fixes test identity churn. Integrated live acceptance and all-floor work remain active.
  - Live foundation acceptance: Astra xhigh job `6b901a34-ae93-4241-9d79-df163e8b597e` completed 13 model turns in 155.68 s, adding a LISABO table and chair with measured 60 cm edge clearance on lobby G00. Real before, target, staged-preview and accepted-layout images reached the model; inspected final PNG shows both models without overlap. Existing UI undo removed both together (revision 2), redo restored both (revision 3), and reload retained their poses. Evidence: `.scratch/scene-designer/lobby-live-evidence.json` and `capture-d563775b-eac8-4c9a-8cd3-dd60af385211.png`. These design placements leave Cart (0) unchanged pending user confirmation. `manage.py test` -> Ran 349 tests in 13.163s / OK (skipped=5); latest `npm test` -> 186+25 pass, 0 fail; `VERCEL=1 npm run build` -> built in 3.54s; board tests -> 6 pass; `git diff --check` passed. Live relative movement, collision refusal, whole-building orchestration and production delivery are still pending.
  - Additional live proof: `e81f66e3-866c-45b2-8e41-3f2b2057b664` moved the existing chair to room +Z behind the unchanged table, exact 60 cm edge gap, revision 4; browser reload during reasoning resumed the same job and retained undo. Final screenshot shows the chair behind the table; model correctly reports partial occlusion. `9298b3bc-27df-43a1-831b-765937c51205` attempted the requested colliding floor pose, received a validator rejection, and left revision 4 and both poses unchanged. Evidence: `.scratch/scene-designer/lobby-move-evidence.json`, `lobby-collision-evidence.json`, and `lobby-moved-ui.png`. Independent review caught and fixed an older-receipt reload regression; new test preserves newer snapshots and omits obsolete undo. Final foundation `npm test`: 187+25 pass, 0 fail; build: 3.66s. Runtime PIDs and cwd confirm this worktree on 8234/5234. Support integration and deployment remain active.
  - Support integration: copied a source snapshot from the separate `codex/supported-placement` checkout (base 9adbf97), leaving it unchanged; reused attachment core, horizontal surfaces/compartments, panel collisions, elevated renderer/picking/drag, scene-aware compiler references, persistence/history, four authored support GLBs and twelve CC0 PC-workspace assets. Reconciled three frontend merge conflicts to retain both async designer and support callbacks. Initial combined backend run exposed 15 failures: missing synthetic-asset provenance, newly real thumbnails, bounded palette and an exact-cuboid test incorrectly applied to imported GLBs. Corrected metadata/data and scoped the fixture-specific assertion; `manage.py test api.test_supported_placement api.test_furniture_assets catalogue.test_scale catalogue.test_search` -> Ran 59 tests in 0.883s / OK. New vision support tests -> Ran 24 tests in 0.742s / OK. Added frozen-capture attachment resolution. Existing agent card stays active; live support workflow and full combined suite still pending.
  - Integrated verification: stable-source `manage.py test` -> Ran 387 tests in 14.499s / OK (skipped=5); `npm test` -> 195+25 passed, 0 failed; `VERCEL=1 npm run build` -> built in 4.24s; board -> 6 passed. All four support profiles match source and built GLB hashes. Independent geometry acceptance -> 12 tests / OK: actual desktop/shelf triangles cover targets; exact cabinet panel geometry and real monitor bounds verified, with conservative desk/monitor bounding-box limitations documented. Live Astra xhigh job `dceea439-df48-42ba-8fdc-ce5e8e99b888` completed 16 turns in 132.76s: desk plus monitor at local (25,-10), derived Y75, and cabinet plus box at local (-15,2), derived Y51.5; all four saved atomically at empty-room revision 1. Staged/final PNGs show correct support/contact; full desk left edge is outside final photographic crop, which the model reports honestly. Evidence: `.scratch/scene-designer/support-initial-evidence.json`, `support-initial-ui.png`, `support-capture-7f6a7376-ead4-4155-99cd-da1f5d39101a.png`. Parent move/rotation, integrated undo/reload and public delivery remain pending.

- **Connect existing Vercel projects to GitHub main** (Kanban `a4577790-c0d7-49fb-b441-a6cfd0c02db5`, [issue #74](https://github.com/LeeSinLiang/hackmit2026/issues/74)). Manual production is current, but both existing Vercel projects still report `gitRepository: null`. `vercel git connect https://github.com/LeeSinLiang/hackmit2026 --scope williamxu070s-projects --yes` returned `Failed to connect LeeSinLiang/hackmit2026 to project` on both projects. `WilliamXu070` has GitHub write/push permission but no admin permission on the private personal repository. The repository owner must authorize the Vercel GitHub integration for this repository and connect both existing projects to `main`, preserving roots `frontend` and `backend`; then verify a PR Preview and a main Production deploy. The existing `.github/workflows/ci.yml` already runs backend/frontend checks on PRs. Production `FRIDAY_API_ORIGIN` now targets the stable `https://friday-hackmit-api.vercel.app` alias; keep Preview and Production environment/database settings separate. This task remains blocked pending repository-owner integration access; do not describe future pushes as automatically deployed yet.
  - Current manual production pair after the concurrent skyscraper-room merge: source `e2ae0a80575b7f370e21318cba30d3597bfa3edf`, API `dpl_AvfZTu6JmRbtUkUPz1N8XAQ2DU35`, frontend `dpl_4HpXref96fRAM8KVdoWx7JMbATAR`. Both returned `READY`; frontend DOM and direct/rewritten API health share `c0b5858f4ea335ca209a4040d6ec1cb957605ac5434fa2199971ad9b7616f19c`. The hosted empty-room route again showed the compact category rail, Chairs expansion and a visible Space jump. This manual refresh does not establish Git-triggered deployment; owner connection and trigger replay are still needed.

- **Verify skyscraper scale and floor-surface dragging** — reopened after user correction: preserve the current main editor exactly as the supplied screenshot. Remove the test panel, test-only buttons and grid overlay. Wire the existing Floor map up/down controls to independently measured building levels, retaining normal catalogue, movement, placement and persistence. Refresh this feature branch from already-available main `9701b1b` without fetching, preserve prior geometry work, run main baseline and final tests, and verify the unchanged UI plus floor switching in the browser. Owner William; existing card `ea8e4665-bce1-4de3-b90d-71140ad6d8c3`. User additionally authorized adding a separate gallery room, pushing/merging all task changes and production Vercel deployment after verification. Keep main’s concurrent furniture-panel upgrade intact. Fresh-main baseline `9701b1b`: backend `Ran 263 tests in 9.054s / OK (skipped=5)`; frontend `142 + 16 passed`; build `built in 2.66s`.
  - Release from the checkout serving 5173, 2026-09-20: preserved its uncommitted Cg Arch board/TODO record, fetched merged main `9adbf97` at the new task boundary, and created `codex/room-release-from-5173` in that same checkout. The old running page still showed the 32-floor SVG gallery. Applied three real-renderer room previews and kept the current editor and lobby route from merged main. `./setup.sh` succeeded; `manage.py test` → `Ran 281 tests in 15.006s / OK (skipped=4)`; `npm test` → `181 + 25 passed, 0 failed`; `npm run build` → `built in 4.30s`; board tests → `6 passed, 0 failed`. Source identity `96c81e343127e74219a3223af6d33e56a3753961499b5ceec6897f849ab72d64`. Pushed branch commit `85f3279` and verified `HEAD` equals `origin/codex/room-release-from-5173`. Frontend deployment `dpl_Ah54qmfa7SriM8VvJrYgUQMn3GUn` from this checkout and matching API `dpl_3Q5aTaKrdvJQ4HfCEqL5WZJynh4f` are live; public frontend `data-build` and rewritten API `X-Friday-Build` equal the source identity. Actual browser flows at 5173 and public Vercel showed real screenshot cards, entrance Floor 1/34, mezzanine Floor 2/34, first tower Floor 3/34 and the full lobby floor-plan outline. Public Cg Arch is shown with its real preview but unavailable because the licensed GLB remains local and excluded from Vercel; its source package and local import remain untouched. The existing card stays active pending a fresh small off-centre inside/outside public drag, and Git-triggered deployment remains blocked by issue #74.
  - Preserved main's existing editor/catalogue controls and read task `01a0bd7b-e17b-78b2-b24d-7c8315d3b753` (category rail plus upcoming jump/gravity). This branch does not edit navigation, shopping, accounts or checkout logic. Generated 32 independently measured floor manifests, including conservative wall bounds on C20–C29, and kept the original GLB bytes/textures. Added the gallery entry and verified Sketchfab creator 99.Miles / CC BY 4.0 attribution. Existing minimap arrows now change room context; PlayCanvas reuses the immutable GLB after capture restoration. Added deployment metadata closure and regression checks; full-suite/browser verification pending.
  - Full checks now pass: `manage.py test` → `Ran 267 tests in 10.219s / OK (skipped=5)`; `npm test` → `tests 147 / pass 147 / fail 0` plus `tests 18 / pass 18 / fail 0`; `npm run build` → `built in 2.95s`. Initial test runs caught and fixed an Array.map unit-conversion callback bug and an invalid test-only product ID; TypeScript fixture casts were corrected. Browser canonical 8234/5234 gallery and existing floor controls load C01→C02→C01; a real sofa GLB placed at (250,470), dragged to (285,470), survived floor return, and undo restored X250. Added the checked reference fixture only to this local browser test session on C01/C20/C32. Production release and latest-main reconciliation remain pending.
  - Browser obstruction proof on C20: the small chair at (250,700) dragged to (265,700), revision 2→3; dragging into the off-centre wall at approximately X320 was rejected with revision 3 and pose unchanged. C32 loaded the complete building with the reference, disabled its upper boundary arrow, switched C32→C31→C32, and accepted reference drag X250→300. Original, served source and built GLB are all 38,784,232 bytes with identical SHA-256 `0d4d5160…dc87de9`. Current screenshots use the existing editor under `.scratch/skyscraper-research/current-*`. Updated indexed deployment/floor documentation; earlier technical-panel evidence is explicitly historical.
  - Integrated current main `2dd8a53`, including PR83 category rail, PR84 jump/gravity and Saketh's catalogue defaults. Board/TODO conflicts resolved by preserving main's records plus the three existing skyscraper/research cards. `navigation.ts`, furniture catalogue/CSS, shopping, accounts, checkout, Visa and catalogue engine match main exactly. Combined checks: `manage.py test` → `Ran 267 tests in 8.847s / OK (skipped=5)`; `npm test` → `151 + 18 passed, 0 failures`; build `built in 2.38s`; board `6 passed`. Existing empty-room browser flow preserved floor 1/1 and category rail, searched HERRÅKRA, previewed/confirmed placement, updated Cart (0)→(1), and displayed the unchanged guest sign-in/cart page with $149.00. Floor changes now unmount any unconfirmed catalogue preview so it cannot be confirmed into the next room.
  - Final combined browser check: keyboard-activated Floor up during an unconfirmed C02 catalogue preview removed the preview immediately, opened C03 and kept Cart (1) unchanged. C32 reference reload retained X300. Final frontend rerun after this boundary guard: `151 + 18 passed`, build `built in 2.36s`. Latest main UI/physics/catalogue code remains byte-identical outside the narrow editor floor wiring.
  - Pre-merge route audit found CartSummary's existing back link uses an item's floor room ID, while App originally allowed only gallery root IDs. Added a bounded floor→public-building mapping in `scene/buildingFloors.ts` and the App route guard, preserving CartSummary and account/cart logic. Unknown floors still fail the gallery allowlist. Tests `151 + 18 passed`; build `built in 2.31s`. Original PR86 checks passed; route follow-up will rerun CI before merge.
  - Upper-floor cart replay: direct `/room/london-skyscraper-c02` opens Floor 2/32, priced HERRÅKRA confirmation adds one item, and the cart's existing Back to your room href is `/room/london-skyscraper-c02`. Route mapping covers only the prepared building IDs; cart/account components remain unchanged.
  - User-reported geometry correction (2026-09-20): public screenshots confirm the old 4×8 m test-zone rectangle does not describe the building, and the Interior_Top-only floor detector omitted the entrance lobby (Y=-4.21677 m) and mezzanine. PR86 merged at 95c30e8 and both deployed runtimes passed their prior tests, but these real-geometry acceptance failures keep this card open. Plan: derive full mesh floor outlines/clear areas; start the gallery in the lobby; preserve old room IDs/layouts with new full-floor contexts; use existing UI, floor arrows and collision engine; visually check small off-centre inside/outside targets; rerun checks and publish the correction. Current branch codex/skyscraper-boundaries from main e2ae0a8; unrelated category rail/jump/catalogue/account/checkout code must remain intact.
  - [Issue #89](https://github.com/LeeSinLiang/hackmit2026/issues/89): generated new full-floor contexts for entrance G00, mezzanine M00 and C01–C32 using named interior floor meshes, complete polygon coverage, fixed-geometry subtraction and merged clear-cell rectangles within the existing 256-area limit. Added exact mesh outlines to the current minimap/floor-plan drawing. Gallery now targets london-skyscraper. Old 32 test contexts and cart links remain available with original coordinates; no persistence migration, catalogue, auth or navigation-code edits. Local canonical 8234/5234 stack started for the required visual checks; generation log full-floor-preparation.log, diagnosis plot boundary-diagnosis.png.
  - Correction verified locally: actual browser lobby/mezzanine screenshots show the two previously omitted levels; C20 plan shows the wider mesh footprint and walls. Small lobby chair pointer drag (1620,500)→(1670,500) saved at revision 4; dragging just outside the slanted edge to approximately (1870,500) returned Unverified area and preserved pose/revision. Full checks: backend Ran 269 tests in 12.594s / OK (skipped=5), frontend 154+18 passed/0 failed, build 3.75s. New floor metadata covers 266.67 m² lobby, 70.96 m² mezzanine and 461.08–1071.38 m² per tower floor, with 20–40 cm conservative boundary cells. Updated existing indexed floor/deployment docs. Main navigation/category rail/styles/catalogue/account/checkout paths are byte-identical to e2ae0a8. Public correction/merge still pending.
  - User expanded scope: multiply normal and fast walking by 1.5, remove the skyscraper's artificial jump cutoff, and improve its rendering/material appearance. Diagnosis: speed constants are 240/360 cm/s; the prepared 230 cm clearance with a 170 cm eye caps feet at 50 cm, below the normal ballistic jump. Rendering uses linear tone mapping, uniform ambient light, no environment reflections, and default texture filtering. Plan: 360/540 cm/s without changing gravity/landing; regenerate safely supported areas with sufficient checked headroom; add a skyscraper-only environment/material/lighting profile preserving original texture bytes, verify browser before/after and capture compatibility, and rerun regression checks. Other room rendering and catalogue/account/checkout logic remain outside this scope.
  - Expanded scope implemented: walking 360/540 cm/s, original gravity/landing retained; all 34 current floors permit full jump arcs using checked headroom (lobby 300 cm, mezzanine 500 cm). Regeneration conservatively reduces lobby/mezzanine clear areas to 245.70/57.28 m². Scoped skyscraper renderer preserves source GLB/textures and fixes opaque glass factors, adds CC0 daylight HDR/reflections, ACES, shadows and anisotropic filtering. An incompatible experimental CameraFrame import was removed before verification; no new engine dependency. Real browser 960×720 photographic and outlined spatial-plan capture jobs both returned ready; editor restoration/floor switching verified. Current checks: backend Ran 269 tests in 12.271s / OK (skipped=5), frontend 155+18 passed, build 3.45s. Final capture-outline checks: npm test 155+18 passed/0 failures; build 3.23s; board 6 passed. Browser sequence jump-full-arc.png shows ascent, apex and return to the same ground view. Publication remains pending; card stays active.
  - Integrated main 3bbfdec (halo helpers, materialization and cart preview) at 8b442da, retaining incoming runtime/UI files. Combined checks: backend Ran 276 tests in 11.155s / OK (skipped=5); frontend 168+18 passed; build 2.75s. Real combined editor still opens the entrance and retains its current controls. Incoming main changes the homepage/cart preview to Haussmann; prevent a deployment regression by packaging its existing metadata and explicitly allowlisting the two unchanged licensed assets copied from the haussmann-room checkout. Raw preparation files remain excluded. Final integration checks: manage.py test Ran 276 tests in 12.934s / OK (skipped=5); npm test 168+18 passed; build 3.62s. Deployment pending.





- **Merge account and checkout routes with the pushed 3D editor** — user authorized feature-branch push and GitHub main merge. Plan: preserve pending Haussmann setup records, reconcile latest main, run unlabelled backend tests plus frontend tests/build, push and verify the remote branch, create a PR, wait for passing CI, and merge only after confirming the demo freeze has not started. Re-test the resulting main. Vercel runtime refresh is separate from the GitHub merge; missing Haussmann assets remain blocked as recorded below.
  - Publication preflight: merged `origin/main` at `f6a330c` cleanly (incoming INDEX and Sin log only). `./setup.sh` succeeded. `OPENAI_API_KEY= DEEPGRAM_API_KEY= ELASTIC_URL= ELASTIC_API_KEY= COMPILE_LIVE_TEST=0 uv run python manage.py test` → `Ran 240 tests in 8.229s / OK (skipped=4)`; `npm test` → `tests 139 / pass 139 / fail 0` and `tests 16 / pass 16 / fail 0`; `npm run build` → `built in 2.58s`. Existing integration card remains active for merge and hosted acceptance. Awaiting demo-freeze confirmation before merge; no application server started.

- **Prepare and verify the shared Haussmann room locally** — user narrowed viewing/model-loading work to the documented Haussmann setup. Plan: download the creator's PLY ZIP with its license, preserve the archive, run existing preparation and surface review, then verify `http://localhost:5222/?room=haussmann-apartment` using the canonical 8222/5222 stack. No UI redesign or Vercel change. Missing room binaries confirmed in this checkout. `git switch main` refused because main belongs to the `sin-model-review` worktree; preserved the clean UI branch and did not pull into it or alter that worktree. Chrome has an existing SuperSplat login; download is in progress.
  - Blocked download: clicked the creator's **Download PLY file** in signed-in Chrome; its download navigation reports `s3.eu-west-1.amazonaws.com is blocked / ERR_BLOCKED_BY_CLIENT`. Did not bypass browser security. No ZIP appeared in Downloads, so preparation, surface review and 5222 runtime verification have not run. `./setup.sh` succeeded: `No migrations to apply`, `System check identified no issues (0 silenced)`, `found 0 vulnerabilities`; existing MFA keys preserved. Need the user to complete the licensed ZIP download or supply its local path. Card `bfec5726-48a4-4f49-8cfa-275f3b80e031` stays active. No application source changes, pull, deployment or app servers started.

- **Merge account and checkout routes with the pushed 3D editor** — completion recheck: found the authorized fix and local merge already committed as `e8d8d37`; did not duplicate or overwrite concurrent work. Verified `origin/main` is an ancestor and UI screens/styles/fonts are unchanged against `b8d4c15`. Fresh commands: `OPENAI_API_KEY= COMPILE_LIVE_TEST=0 uv run python manage.py test` → `Ran 240 tests in 11.171s`, `OK (skipped=4)`; `npm test` → `tests 139 / pass 139 / fail 0` and `tests 16 / pass 16 / fail 0`; `npm run build` → `built in 3.29s`. Issue #66 is closed. No listeners on 8211/5211; no servers started in this recheck. Local merge complete, GitHub main and Vercel unchanged; broader integration card remains open for those separate release gates.

- **Merge account and checkout routes with the pushed 3D editor** — authorized fix/merge follow-up: diagnosed the seven failures as cross-test pollution from `frontend/src/catalogue/shelf.test.ts`, which leaves a location-only global window behind; successful room saves then throw at cart-event dispatch. Plan: restore the original global descriptor in test cleanup, add an isolation assertion, reproduce the full bundled suite, run backend/build checks and complete the pending local merge. No production notification code or visual styles need changing. Existing integration card remains open for hosted acceptance; no push/deployment authorized here.
  - Fix verified ([issue #66](https://github.com/LeeSinLiang/hackmit2026/issues/66)): restore the previous window property descriptor using test cleanup and assert global isolation. `npm test` → `tests 139 / pass 139 / fail 0` and `tests 16 / pass 16 / fail 0`; `npm run build` → `built in 2.29s` (existing chunk warning); backend full command → `Ran 240 tests in 7.744s`, `OK (skipped=4)`. Canonical local stack at 5211/8211 uses this checkout's frontend/backend paths and isolated scratch database. Browser verified gallery → empty-room editor, main's model-only results, confirmation cart count 1→2, undo 2→1 and redo 1→2. UI/auth/checkout components, CSS and fonts compare unchanged against pre-pull `b8d4c15`. Local merge is ready; this supersedes the failed-test checkpoint below. Missing licensed Gaussian assets remain unavailable locally; no claim of a live Haussmann replay or hosted refresh.

- **Merge account and checkout routes with the pushed 3D editor** — user-requested main pull, 2026-09-20: preserved the planning documents in `b8d4c15`, then ran `git pull --no-rebase --no-commit origin main` on `codex/room-cart-vercel`. Fetched `0f44bc5`; one conflict in `frontend/src/SplatEditor.tsx` resolved by keeping route prop precedence plus both query aliases and main's legacy Haussmann default. `frontend/src/App.tsx` now recognizes both aliases before redirecting to the preserved gallery. Main's catalogue/portal/rendering changes and teammate logs are retained. Checks pending; existing integration card stays open. This pull does not push main or refresh Vercel.
  - Verification/blocker: `./setup.sh` succeeded and applied the existing shopping/recovery/checkout migrations locally, preserving the MFA key. `OPENAI_API_KEY= COMPILE_LIVE_TEST=0 uv run python manage.py test` → `Ran 240 tests in 7.931s`, `OK (skipped=4)`. `npm test` → `tests 138`, `pass 131`, `fail 7` in scene-session confirmation/retry/undo tests; the chained build was correctly not run. Log: `.scratch/main-pull-frontend-tests.log`. Stopped before the merge commit: fetched main is present, conflict markers are resolved, but Git remains in an unfinished merge pending regression investigation. No claim that clean upstream main is red; this result is for the combined tree. Existing William-owned integration card remains open. No servers started; checked ports 8211/5211 have no listeners. No push or redeployment.

- **Merge account and checkout routes with the pushed 3D editor** — integration planning update, 2026-09-20: preserve the current checkout's UI at `97f009e` while integrating onto fresh main. Added [UI/main integration plan](docs/handoffs/ui-main-integration.md) and indexed it. Live `git ls-remote` reports main `0f44bc5530a529566fc94a04e0fe853138cb95a7`; GitHub comparison reports 31 commits ahead of local base `cac9880`. Identified room/query/default, catalogue count/model-filter, portal-solver and packaging overlaps. Plan separates backend contract, preserved UI, and deployment PRs, with fresh-main and visual/runtime gates. Existing William-owned card `03bc6470-6a84-48d8-ba48-1df4ed0f3277` remains open; no duplicate card or status change. Planning only: no application edits, fetch, checkout, merge, push, deployment or test servers. Existing release test results are historical, not tests of the proposed integration.

- **Build room selection, confirmed furniture cart and Vercel deployment**: implement the approved guest room → confirmed placement/cart → account/MFA → sandbox checkout journey on `codex/room-cart-vercel`. Reuse the current editor/catalogue; add stable shopping ownership, atomic confirmation, authoritative pricing, deployment configuration, and public-runtime checks. Includes existing cards **Merge account and checkout routes with the pushed 3D editor** and **Define guest-room transfer and account scene ownership**.
  - **Current delivery, 2026-09-20** (supersedes the chronological checkpoints below): code is implemented and deployed at https://friday-hackmit.vercel.app with separate https://friday-hackmit-preview.vercel.app. Source commit `222311a`, runtime SHA-256 `b235edc083748083597f40a20edf259919017952b43cdd7db91908e2eea279e5`; frontend DOM and backend health header match. Both environments pass real Postgres concurrent retry, secure-cookie, CSRF, placement/persistence and authoritative-price checks. Hosted browser confirms room/editor/confirmation/cart and create-account summary. SMTP TLS/login passes with existing private mail settings configured in Preview and Production; user asked to verify inbox receipt and complete their own MFA. Full hosted account/approved-sandbox-checkout acceptance remains open, so all three cards stay active. No GitHub push, real email send by this agent, Visa submission or vendor order.
  - Baseline: refreshed `origin/main` to `cac9880`; `./setup.sh`, all 221 backend tests (4 optional skips), frontend tests and production build pass. Another worktree owns `main`; this branch was cut directly from fresh `origin/main`. Vercel CLI is authenticated as the existing user. No implementation or deployment verification claimed yet.
  - Order: deployment configuration/access; shopping persistence and contracts; gallery/editor/cart UI; account handoff and checkout; full suite, browser workflow, hosted verification. Preserve local accounts and existing running stacks; use dedicated test ports.
  - Implementing: `backend/shopping/` defines stable guest identity, cart membership, atomic confirmation and retry receipts; scene edits synchronize cart presence, checkout approval/submission locks the cart revision. `frontend/src/shopping/`, App, SplatEditor, catalogue layer and CheckoutReview now connect gallery, explicit preview/confirmation, cart/auth summary and real-cart snapshots. TypeScript/Vite build passes; new integration tests and browser checks are in progress. Board cards remain active under William.
  - Deployment: user accepted Neon terms. Created `friday-hackmit` and `friday-hackmit-api` with frontend/backend roots and shared-source access, provisioned free `friday-preview` (resource `store_aDseLHtcdJLFOzzv`), and connected only Preview. Uploaded fresh Preview Django/MFA keys and security/origin settings. Located the documented private Visa/auth worktree's SMTP and Visa configuration and uploaded it without displaying values; user also explicitly asked for SMTP setup. Hosted schema/cache migrations pass. No local account database/MFA records were uploaded. Vercel unexpectedly assigned the first deployment to Production; removed `dpl_EqasJs9J1SHRTSDDo2p2hjxxf6c4` and made hosted DEBUG default false. Explicit Preview `dpl_B9GtEe1aEibTmhYBt17BJrHHwuD9` is BLOCKED by its upstream commit-author permission. Preparing a genuine local feature commit under existing William Git identity before retrying; no GitHub push. Hosted packaging, Postgres concurrency and real-email checks remain unverified; production has no database yet.
  - Verification: 227 backend tests pass (4 optional skips), 138 frontend tests pass, production build passes with existing renderer-size warnings. Further checks are being rerun after persistent recovery acknowledgement and ownership locking. Local canonical launcher at 5211/8211 with isolated scratch SQLite verified guest gallery, explicit confirmation, reload persistence, cart sign-in summary, removal/undo and 390px cart overflow. Added indexed `docs/frontend/room-cart-deployment.md`, release helpers, production env template and upload exclusions. No real email, Visa request, push or production promotion performed.
  - Hosted integration: genuine local commit `3977d81` resolved the Vercel author gate. API Preview `dpl_4DMQtiGVy7kyScrQSTvZrv3BTMtW` builds and serves matching health identity; scene loading exposed Vercel's flattened root (`/var/task`), making the old parent/shared lookup invalid. Added `backend/build.py` and `api/shared_data.py` to package only 14 allowlisted runtime JSON files beside Django; local packaged-room/catalogue verification passes. Both preview and separate free production Neon schemas/cache are migrated, with independent secret keys. Frontend Preview alias exists but is not yet ready for handoff until the API packaging fix is deployed. Unexpected first frontend Production artifact was also removed; no production frontend is being handed off.

## Next

- Release cleanup verified: removed six superseded/blocked test deployments created during this task, retaining the current Preview/Production pairs and all Neon data. Old artifacts can be rebuilt from local feature commits. Stopped the canonical test stack and board; `lsof` confirms no listeners on 8211, 5211 or 56777. Owner ports 8000/5173 were untouched. Documentation-only release evidence follows runtime commit `222311a`; source hash remains unchanged. All three related Kanban cards were re-read and remain assigned to William and open solely for hosted account/transaction acceptance.

- Hosted routing correction verified: `frontend/vercel.mjs` uses per-environment `FRIDAY_API_ORIGIN` plus explicit trailing-slash rewrites. Preview passes Neon concurrent-idempotency, authoritative pricing, secure cookies, CSRF refusal and scene persistence. SMTP is configured in both environments and provider TLS/authentication passes; inbox delivery still needs verification. Latest automated baseline: 229 backend tests (4 optional skips) and 138 frontend tests, plus production build.

- Finish hosted verification for **Merge account and checkout routes with the pushed 3D editor** (`03bc6470-6a84-48d8-ba48-1df4ed0f3277`) and **Define guest-room transfer and account scene ownership** (`4147c58c-2029-40ef-93ee-537ddad4a25e`); implementation is now included above, not deferred. Keep both active until the deployed runtime passes.
- Finish user-driven hosted email receipt, MFA enrollment/login, password reset and explicitly approved sandbox checkout. Production and Preview are published; SMTP/Visa settings and both database migrations are complete. Real payment authorization and merchant ordering remain outside the sandbox contract.

## Done

- **Publish integrated FRIDAY test release and record demo** (Kanban `25675e31-aaa6-43d7-a563-b94360fc6706`) — completed 2026-09-20. PR [#103](https://github.com/LeeSinLiang/hackmit2026/pull/103) merged as `5afe3929723fa2e129b96d47a208e35001728244`. Integrated scene designer, explicit reviewed attachments and current room gallery/boundaries. Fresh merged-main `uv run python manage.py test` → `Ran 390 tests in 12.545s / OK (skipped=5)`; `npm test` → `195/195` plus `25/25`, zero failures; `VERCEL=1 npm run build` → `built in 2.55s`; board tests → `6/6`. Production frontend `dpl_J8y7o8sywLSzK59oxbvH1wa66ytn` and API `dpl_2nECz4rWZ7SwT5voZTj6NgNuxht9` are READY from that commit, both identity `778d7e434fa26ce392a6a4179af5ed496bac83053f345e6cd119173e9664b1bd`; live DOM and direct/rewritten health match. Production migration 0005 applied. Real hosted C32 designer job placed four objects; monitor Y75, cabinet box Y51.5. Drag/90-degree rotation/undo/redo retained attachments at saved revision5; restored original empty floor at revision8 and preserved cart. Local acceptance also verified reload. Public model bytes match source SHA-256. Captured 1920x1080 H.264 demo under `.scratch/demo-release/FRIDAY-public-demo.mp4`; waiting time removed and asset credits retained. Separate detailed asset, sidebar and lighting work is excluded from this production release. Main advanced afterward to lighting merge `46930f6`; that merge is not the deployed source. Parent integrator was notified to verify it before later deployment. Task-owned 5286/8286 servers stopped and no listeners remain. Evidence and tester instructions: `docs/demo/public-test-release.md`.

- 2026-09-20: **Fix Empty Interior Scene 2 walking and placement boundaries** — integrated into the local app serving port 5173 on `codex/room-release-from-5173`; Kanban `dd5ede57-1071-44eb-8fe8-56faa3493797` complete for local delivery. [Diagnosis and evidence](docs/tickets/cg-arch-boundaries.md).
  - Merged reviewed commit `7a0ae0e` with gallery release `964f7e8` in `/Users/williamxu/Desktop/Projects/hackmit2026-fresh`, preserving the gallery, preview images and both board records. Combined full `manage.py test` → `Ran 284 tests in 14.014s / OK (skipped=4)`; `npm test` → `tests 181 / pass 181 / fail 0` plus `tests 25 / pass 25 / fail 0`; `npm run build` → `built in 4.28s`. Baseline was 281 backend tests, the same frontend counts, build 4.30s. Dependencies were unchanged.
  - Replaced the living-room-only cutoff with 36.730 m² of model-supported living room, corridor and entry floor (21 conservative rectangles; zero unsupported/blocked/lost old area). Changed `shared/rooms/cg-arch-interior/{manifest,spatial,openings,boundary-review}.json`; added reproducible `scripts/prepare_cg_arch_boundaries.py`. Existing map/plan now use the same outline. GLB SHA-256 remains `f5f5ab55…3a59c`; textures, dimensions and model transform are unchanged.
  - `backend/api/scene_service.py` accepts only the validated Cg Arch v1→v2 expansion, retaining saved instances, revision and receipt history. Updated `backend/api/{test_prepared_rooms,test_room_openings}.py`, `frontend/src/scene/playcanvas/interaction.test.ts`, and region tests. Existing room note in `frontend/src/SplatEditor.tsx` names the expanded area; no new UI controls.
  - Final checks: full `manage.py test` → `Ran 284 tests in 12.794s / OK (skipped=4)`; `npm test` → `tests 181 / pass 181 / fail 0` plus `tests 25 / pass 25 / fail 0`; `npm run build` → `built in 3.58s`; no-write geometry check passed. Initial TypeScript test-fixture cast was corrected. Historical rectangle tests remain explicit; current room tests cover the real L-shaped floor.
  - Browser: walked past X787 to corridor X363/Z460 and entry turn X40/Z462; closed-door barrier held. Placed HERRÅKRA at (600,490), dragged to (500,490), rejected cabinet-area drag, and verified reload preserved (500,490). Also furnished entry at (85,650). Screenshots in `.scratch/cg-boundaries/`; browser error log empty. Full entry-and-return route passes the actual navigation function regression; browser southward return contacted a cabinet corner and is not recorded as a completed return.
  - Runtime verified through canonical stack at 8242/5242, isolated hostname `cg-arch-review.localhost` to prevent competing local cookies. Served metadata matches disk; source/served/built GLB hashes match. Closed the test tab and stopped owned servers; `lsof` verified no listeners on both ports.
  - Local delivery: existing Vite 87771 and reloaded Django 36128 serve the reviewed source on 5173/8000; served metadata is v2 with all 21 rectangles, and source/served/built GLB hashes match. The requested 127.0.0.1 gallery route shows the L-shaped plan and accepts corridor placement. Its browser guest lost identity on reload; fresh HTTP cookie-jar requests were stable. On the same app through localhost:5173, placement, drag to X500/Z490, cabinet rejection and reload passed visibly. Removed both verification chairs; screenshots and logs are in `.scratch/cg-boundaries-release/`. Updated the existing Cg Arch guide and INDEX. No servers started; user runtime preserved. Local integration only: no GitHub push, remote-main merge or Vercel deployment.

- 2026-09-20: **Import and verify the supplied Cg Arch living room locally** (Kanban `7e82963e-80af-4fd4-a9a8-6c1f34a30fdd`) — checked `/Users/williamxu/Downloads/friday-cg-arch-room/` against `bundle.json`: all four file hashes matched; manifest, spatial data and license were byte-identical to this checkout. Packaged only those files in ignored `.scratch/cg-arch-room-import.zip` and ran `./scripts/import-room.sh .scratch/cg-arch-room-import.zip` → `Installed room. Restart ./run-local.sh, then open /?room=cg-arch-interior.` Installed ignored `shared/rooms/cg-arch-interior/assets/room.glb` is 68,229,960 bytes, SHA-256 `f5f5ab5566ee040c400faeb8ae66ff00e8d3d12a4db1836aa312e9feb903a59c`, identical to the supplied GLB. Restarted the canonical 5173/8000 stack; Vite no longer reported Cg Arch missing. Browser `/rooms` showed its card as a link; opening `/room/cg-arch-interior` displayed the actual interior mesh, `Empty Interior Scene 2` attribution and `Saved` status. Existing layouts and source package were untouched; no source code or public deployment changed. The user-facing local stack remains running by request. Tracking paths changed: this TODO and `.github/extensions/project-manager/data/board.json`.

- 2026-09-20: **Update Kanban wording for OpenAI API and Codex** — updated `.github/extensions/project-manager/{data/board.json,server.mjs,bin.mjs,extension.mjs,README.md}` on main. The existing agent card now says **Connect room, catalogue, and user input to the OpenAI API agent**; Agent Harness and demo descriptions mention OpenAI API/Agents SDK and Codex. Removed old vendor wording from launcher/docs; retained the legacy adapter package import for compatibility. IDs, ownership, order and completion preserved. Verification: `node --test .github/extensions/project-manager/tests/board.test.mjs` → `tests 6 / pass 6 / fail 0`; launcher smoke → `HTTP 200`, both requested names visible, no old vendor wording in rendered board. Syntax and whitespace checks passed. Stopped the launcher; `lsof` confirmed no listener on port 50717. No commit or push.

- **Add gravity and furniture-top jumping to Walk mode** (Kanban `1de24261-fcb7-4008-8cf5-f65bb81e84c8`). User requests Space to jump, gravity, and standing or walking on placed objects. Current `frontend/src/scene/playcanvas/navigation.ts` is flat 2.5D: fixed camera height and all placed furniture treated as full-height horizontal obstacles; it cannot jump or land. Plan: add a bounded vertical camera velocity with gravity and ceiling/floor limits, use placed product dimensions as conservative solid boxes for horizontal blocking and top support, retain reviewed floor and fixed-room collision boundaries, document controls and proxy limits, and verify physics with targeted tests plus the actual room route. Work starts from fresh `main` after PR #83; the post-merge baseline reported `Ran 263 tests in 9.283s / OK (skipped=5)` and Vite `built in 3.00s`.
  - Implemented `frontend/src/scene/playcanvas/navigation.ts` vertical velocity, Space jump, ceiling clamp, dimensioned furniture-top support and falling off; added regression cases in `frontend/src/scene/playcanvas/interaction.test.ts`, updated `frontend/src/SplatEditor.tsx` help and this indexed editor document. Local `npm test` → `tests 145 / pass 145 / fail 0` and auth `tests 16 / pass 16 / fail 0`; `npm run build` → `built in 2.80s`. Canonical 8211/5211 browser showed the Space jump rising visually, then returning to the original floor view under gravity. Tried a sofa placement probe for live landing, but its hovered location was outside the room, so cancelled it without saving; landing on a furniture top is verified by physics and collision tests, not yet a live 3D landing replay. Fixed room obstacles have no height metadata and remain full-height barriers; placed product boxes are a conservative approximation to their visual mesh.
  - Final branch checks after adding elevated collision regression: backend `manage.py test` → `Ran 263 tests in 8.550s / OK (skipped=5)`; `npm test` → `tests 146 / pass 146 / fail 0` and auth `tests 16 / pass 16 / fail 0`; `npm run build` → `built in 2.52s`; board tests → `tests 6 / pass 6 / fail 0`. Canonical browser landed after a floor jump, but a persisted sofa-top landing was not demonstrated; the initial floor-view placement aim was outside the room, and a later floor-plan probe did not remain visible after the view change. Keep that acceptance limit explicit in the PR and hosted handoff. The 8211/5211 stack was stopped and no test placement remains visible.
  - PR [#84](https://github.com/LeeSinLiang/hackmit2026/pull/84) merged as `2dd8a53210f532d87502116e54f3bbbfeec4901c`; fresh `main` reported backend `Ran 263 tests in 8.334s / OK (skipped=5)`, frontend `tests 146 / pass 146 / fail 0` plus auth `tests 16 / pass 16 / fail 0`, and Vite `built in 2.49s`. Production API `dpl_dqX4AhxLQB6fT5bjKgV1qvQdz2G4` and frontend `dpl_AukY994s9yoG4Rp2AaAJBfY5Zsvw` reported Ready with source identity `11c7da526e8785e27493b816a07d548bc590691c6b7f8e781256d43a147c7909`. Direct and rewritten API health were HTTP 200 with that identity, matching hosted frontend `data-build`. Hosted `/room/empty-room` showed the compact category rail, Chairs expansion, and a visible Space jump that returned under gravity. Added a Walk-input integration test on this follow-up branch to drive Space+W onto a placed sofa, stand there, then walk off and fall; `npm test` → `tests 147 / pass 147 / fail 0` plus auth `tests 16 / pass 16 / fail 0`. The sofa-top test uses a PlayCanvas camera and event loop with mocked window/document, so it does not claim a full browser furniture placement replay. Re-publish the new source identity after merge.
  - Completion: input-level PlayCanvas test in PR #85 verified Space+W onto a placed sofa, standing on top, walking off, and falling. PRs #84/#85 are on `main` at `68367e8`; final source identity and deployments are recorded in `docs/frontend/room-cart-deployment.md`. Browser checks on the final public room verified load and matching DOM/API identity; the prior hosted replay showed a visible jump and return to floor height. Full browser furniture-top landing remains unobserved, as documented.

- **Keep category rail visible when furniture panel collapses** (Kanban `f5b7aa7e-70e7-4b8c-acaa-7c0f30368873`). The user wants the minimized furniture panel to retain its rounded category rail, category icons and bubble chevron instead of a lone edge tab. Plan: retain the catalogue component and its state while hiding only its item list; make a collapsed category choice reopen that category, verify the real room route and keyboard behavior, then merge and refresh production. The earlier main source at `9b394f7` was manually redeployed to API `dpl_HATsqGouwnBH5nKYZ1gUQFZ8iW1f` and frontend `dpl_ZkHxcmYPrzTRDhE2HWtoay5nGfmn`, identity `77f06279d39fe1988806413d62993cebfda8d070d390e4dc1ceabe25cd864d1d`; browser initially showed a loading transition, then the room rendered correctly on a fresh visit. This UI refinement is active and production must be refreshed again after merge.
  - Implemented in `frontend/src/{SplatEditor.tsx,FurnitureCatalogue.tsx,splat-editor.css}` and updated indexed `docs/frontend/room-capture/playcanvas-implementation.md`. Canonical 8211/5211 browser on `/room/empty-room` showed the rounded compact rail and circular chevron; selecting Chairs opened Chairs content and collapsing kept Chairs active. `npm run build` → `built in 2.94s`; backend `manage.py test` → `Ran 263 tests in 10.365s / OK (skipped=5)`; frontend `npm test` → `tests 142 / pass 142 / fail 0` and `tests 16 / pass 16 / fail 0`; board tests → `tests 6 / pass 6 / fail 0`. Merge and hosted replay are pending.
  - Completion: PR #83 merged on `main`; the public room showed the rounded category rail with circular chevron, and clicking Chairs opened Chairs content. Production was refreshed with the final `68367e8` source identity in `docs/frontend/room-cart-deployment.md`.

- **Collapse furniture panel behind persistent chevron** (Kanban `f5b7aa7e-70e7-4b8c-acaa-7c0f30368873`, [issue #77](https://github.com/LeeSinLiang/hackmit2026/issues/77), [PR #79](https://github.com/LeeSinLiang/hackmit2026/pull/79)). Merged into `main` at `9701b1bceb58b9977c5c51925fc5533e036a34fb`. Updated `frontend/src/{SplatEditor.tsx,FurnitureCatalogue.tsx,splat-editor.css}` and indexed `docs/frontend/room-capture/playcanvas-implementation.md`; removed the Add furniture button, replaced the panel X with a persistent chevron, and preserved category/favorite/scroll state while collapsed. The local canonical 8211/5211 browser replay verified collapsed default, open/collapse/reopen, keyboard Enter, hidden panel `aria-hidden`/`inert`, retained Chairs category and Kivik favorite, and live catalogue search. Screenshots covered both desktop states; narrow breakpoint CSS was reviewed but not runtime resized. Stopped the stack; no listeners remain on 8211/5211. Post-merge fresh `main`: `uv run python manage.py test` → `Ran 263 tests in 8.829s / OK (skipped=5)`; `npm test` → `tests 142 / pass 142 / fail 0` plus `tests 16 / pass 16 / fail 0`; `npm run build` → `built in 2.47s`; board tests → `tests 6 / pass 6 / fail 0`. PR CI backend/frontend and GitGuardian passed. Production frontend `dpl_6qmdKhcAwomkFpFAXEy4RQ75KfHk` and API `dpl_6JkuqjxPqM4JGLkPRUmP8fjDChAw` are Ready. Public `/room/empty-room` showed the collapsed chevron with Rooms/Cart, opening exposed the sofa catalogue, and collapse restored the compact view. Public `/api/health` and the direct API alias returned HTTP 200; API `X-Friday-Build` and frontend DOM `data-build` matched fresh source identity `90d3b365dcc71c8844fb2514a3fbbdfb17c5733077b667d69f9ced1a948e7109`. Release IDs and Git deployment handoff are recorded in `docs/frontend/room-cart-deployment.md`; `.github/extensions/project-manager/data/board.json` marks this UI task done. GitHub-triggered Vercel deployment remains issue #74.

- **Integrate local room editor UI with shopping pages and Vercel release** (Kanban `e43d4acb-3e24-4597-9db0-93dc39daf6fd`, [issue #71](https://github.com/LeeSinLiang/hackmit2026/issues/71), [PR #72](https://github.com/LeeSinLiang/hackmit2026/pull/72)). Merged at `2ad607710d42a437f6e024639291547b3e573212`. Changed `frontend/src/{SplatEditor.tsx,FurnitureCatalogue.tsx,PlayCanvasScene.tsx,splat-editor.css}`, `frontend/src/catalogue/{SplatCatalogueLayer.tsx,CatalogueShelf.tsx,shelf.css}`, `frontend/src/scene/{FloorMap.tsx,playcanvas/interaction.ts,playcanvas/navigation.ts,playcanvas/interaction.test.ts}`, `shared/scene-fixtures.json`, `backend/api/test_scene.py`, and `docs/frontend/room-capture/playcanvas-implementation.md`. Kept Rooms/Cart/account/checkout routes, live purchasable catalogue search and explicit cart confirmation; provisional concept sofas remain room previews. Fresh `main`: `uv run python manage.py test` → `Ran 254 tests in 8.452s / OK (skipped=5)`; `npm test` → `tests 142 / pass 142 / fail 0` and `tests 16 / pass 16 / fail 0`; `npm run build` → `built in 2.61s`. PR CI backend/frontend and GitGuardian passed. Canonical isolated 8211/5211 local replay covered Rooms → empty-room editor → HERRÅKRA placement confirmation → Cart (1) / cart page; Kivik concept did not add to cart. Production API `dpl_Bn11auHniy4fUNNcLX9sNUmDnnsa` and frontend `dpl_JBwFo3EX9fTzQkVaGYmSfPTugDkM` are Ready. Public `/room/empty-room` shows Rooms/Cart, Walk, map, new sofa catalogue and live HERRÅKRA search; public `/api/health` returned 200. Frontend DOM `data-build` and API `X-Friday-Build` both equal `ba13129eb7ab69ae709894fc7653f284c3ebe4e91c95785b54abe6da8222fa1a`, matching the fresh `main` source identity. In-app browser denied pointer lock, so headed pointer movement remains unverified here. Original 5173 stack/data were untouched; started 8211/5211 stack was stopped and both ports have no listener. Automatic GitHub-to-Vercel deployment remains issue #74. Release status, deployment IDs and the CI/CD owner handoff are recorded in `docs/frontend/room-cart-deployment.md`, this TODO, and `.github/extensions/project-manager/data/board.json`.
- 2026-09-20: **Verify skyscraper scale and floor-surface dragging** — completed the bounded local test on `codex/skyscraper-glb` (base `13297be`), card `ea8e4665-bce1-4de3-b90d-71140ad6d8c3`. Added `scripts/skyscraper/prepare.py`, `shared/skyscraper-test-scene.json`, three floor manifest/spatial/license packages, strict canonical visual sharing, `SkyscraperTestPanel.tsx`/CSS, explicit editor routes and `get_scene_context` in `backend/api/engine_tools.py`. Added paired real-floor tests in backend/frontend plus shared-asset path/alias regressions. Indexed [reproduction and evidence](docs/frontend/room-capture/skyscraper-glb-test.md), updated the placement plan and engine-tools contract.
  - Exact source GLB remains unchanged: source/HTTP-served/build SHA-256 all `0d4d516057aa6b8e6a79cdbd0b3446432edd831a151206c2c1879e944dc87de9`; one built visual asset, all 58 meshes visible. Geometry checks approve 32 m² on C01/C17/C32 independently at 7.180015564, 78.785018921 and 145.231170654 m, with no unsupported area/projected blocker up to 230 cm. Maximum lower-zone support variation 2.533 mm; no equal storey/slab assumption.
  - Browser at canonical 5234/8234: complete-building overview, all three floors, rendered reference 200 × 100 × 100 cm/base Y=0, pointer drag X=250→300 saved on each, rejected boundary drag preserves pose/revision, reload preserves each floor; lower-floor undo/redo and rotation passed. Screenshots/logs in `.scratch/skyscraper-research/`. Development parse/type issues were fixed before these successful replays; no current-load console error after the initial 06:52 development reload.
  - `OPENAI_API_KEY= DEEPGRAM_API_KEY= ELASTIC_URL= ELASTIC_API_KEY= COMPILE_LIVE_TEST=0 uv run python manage.py test` → `Ran 242 tests in 8.758s / OK (skipped=4)`; `npm test` → `tests 141 / pass 141 / fail 0` and `tests 18 / pass 18 / fail 0`; `npm run build` → `built in 2.60s`. Existing optional-download/large-chunk warnings remain. `git diff --check`, document links and INDEX coverage passed. Process cwd proved this checkout; stopped the owned stack and verified no 5234/8234 listeners; closed the test tab. Teammate stacks left alone.
  - Scope: local floor proof only, no commit/push/merge/deployment, no all-floor detection claim, no table/cabinet attachments, no performance benchmark or live AI compiler registration. `get_scene_context` is a tested SDK-independent adapter. Read remote main `9e63d94` without fetching/merging: GLB rendering/floor dragging exist, but Y=0 poses and `ALLOW_STACKING=false` remain. Main integration must preserve newer editor/catalogue work and re-run its current suite.


- 2026-09-20: **Research support surfaces and cabinet containment** — corrected the minimal plan to the user's required workflow: complete London skyscraper GLB in current PlayCanvas → dedicated reproducible test-scene file → explicit scale/reference object → selected reviewed floor surface → drag and reject invalid positions. Reuse the existing declared 200 × 100 × 100 cm scale-reference asset after geometry verification; test lower/middle/upper candidates using measured, unequal elevations and floor-aware AI refs. Table/lamp and cabinet/object checks now follow inside the skyscraper. Updated `docs/frontend/3d-object/supported-placement-mvp-plan.md`, research milestone wording and INDEX description. Six plan sections, local links, index coverage, required workflow details and `git diff --check` verified. Planning only; no fixture file, GLB changes, application implementation, server or runtime test created. Existing card `7d543e22-c744-43a3-ac28-0dcc4aa52dd0` records completed planning; runtime acceptance remains pending.

- 2026-09-20: **Research support surfaces and cabinet containment** — minimal implementation plan completed under existing card `7d543e22-c744-43a3-ac28-0dcc4aa52dd0`. Added [engine and AI reference plan](docs/frontend/3d-object/supported-placement-mvp-plan.md), indexed it and linked it from the research. Plan specifies one lamp/table and object/open-cabinet demo, trusted geometry profiles, one attachment level, derived Y, matching frontend/backend validation, save/move/undo semantics and stable AI surface/compartment IDs. It reuses current compiler/preview/confirmation and `try_place`, with explicit live-AI/runtime acceptance. Reviewed existing asset metadata: LISABO/open bookshelf are inspection candidates; HEKTAR is an unbound floor lamp and unsuitable for this proof. `git diff --check`, local links, INDEX coverage and task identity passed. Planning only: no application changes, model edits, dependency changes, tests or server runs in this turn. Implementation remains the next action, not a completed behavior.

- 2026-09-20: **Research support surfaces and cabinet containment** (Kanban `7d543e22-c744-43a3-ac28-0dcc4aa52dd0`) — research complete, implementation not started. Added [lamp/table and cabinet placement design](docs/frontend/3d-object/supported-object-placement.md), its INDEX entry and a cross-link from the skyscraper research. Checked pose/picking/collision, catalogue authority, scene commands, undo/redo and DSL against source `13297be`. Primary references support annotated receptacles, compartment volumes, compound solid proxies and parent transforms. Proposed one reviewed table/lamp and open cabinet/object pair, height-aware checks, authoritative profiles and atomic assembly operations. Verified document links, board identity, index coverage and `git diff --check`. No application changes, GLB edits, new runtime dependencies or servers; no new tests/runtime screenshots claimed for this documentation-only follow-up.

- 2026-09-20: **Research multistory GLB integration and floor detection** (Kanban `bb9306c1-5350-4917-8048-995c64709e7f`) — completed research only on fresh-main `13297be8c637f0669660a434282f20af1b3d2b97`, isolated branch `codex/skyscraper-glb`. Added [research note](docs/frontend/room-capture/skyscraper-glb-research.md), measured evidence in `docs/evidence/skyscraper-floor-probe.json`, and an INDEX link. Read-only inspection found 58 mesh nodes, 141,583 triangles, 45 embedded images and 32 strong tower floor candidates, with adjacent rises of 4.3678–4.4936 m. Identified single-floor pose/navigation assumptions, hardcoded frontend room selection, packaging restrictions and the compiler's mock room references. Recommended one preserved GLB with independently reviewed floor contexts; lobby/roof/split levels need review. Diagnostic image and scripts: `.scratch/skyscraper-research/`.
  - Verification: `OPENAI_API_KEY= DEEPGRAM_API_KEY= ELASTIC_URL= ELASTIC_API_KEY= COMPILE_LIVE_TEST=0 uv run python manage.py test` → `Ran 240 tests in 8.709s / OK (skipped=4)`; `npm test` → `tests 139 / pass 139 / fail 0` and `tests 16 / pass 16 / fail 0`; `npm run build` → `built in 2.83s` with existing chunk/missing optional asset warnings. Research links, evidence JSON, 32 candidate records, source SHA-256 and `git diff --check` verified; geometry figure visually inspected. The source model and application code were unchanged, and no application server was started. No integration, browser workflow screenshots, furniture placement or performance results claimed; those remain a documented future acceptance plan under the user's research-only instruction. No commit, push or deployment.
  - Follow-up: verified GLB-on-GLB stacking is explicitly disabled (`ALLOW_STACKING = false`); furniture-on-floor within a prepared GLB room is supported, while arbitrary tabletop/cabinet placement lacks vertical poses and support/container-aware collisions. Added the distinction to the research note. Source inspection only; no new implementation or test run. Existing research card remains done.

- 2026-09-20: **Fix seven frontend failures blocking the local main pull** (part of the existing UI integration card; issue #66). Restored the catalogue test's original window descriptor and added an isolation regression; no production UI/event changes. `npm test`: 139/139 and 16/16 pass; `npm run build`: built in 2.29s; backend: `Ran 240 tests in 7.744s`, `OK (skipped=4)`. Canonical browser replay verified room selection, model-only catalogue, confirmed placement, cart count, undo/redo and cart sign-in summary. Stopped both task-started servers and closed the test tab; no listeners remain on 8211/5211. The broader integration card remains open because local sync is not a GitHub-main merge or a hosted release; production is intentionally unchanged.

- 2026-09-20: **Review Visa/auth PR before merge** (Kanban `ae490364-b75e-41ca-8d81-0485d0d78a73`, William; PR #21). Merged upstream `main` through `be795d8` into the feature branch as `a391928`, preserving the six board components, 48 cards, five upstream phase values, Saketh's Blender and catalogue-import notes, and both catalogue cache test protections. Updated `.github/extensions/project-manager/data/board.json`, `TODO_SAKETH.md`, `backend/catalogue/test_compile.py`, `docs/account-mfa-checkout.md`, and `docs/handoffs/visa-auth-integration.md`; upstream scene/catalogue and throttle changes were included unchanged. Published the branch and PR description; GitHub reports the branch mergeable into `be795d8`, and GitGuardian passed. Fresh Codex review of exact code head `a391928` reported no major issues and no new inline findings. The review card is done; the authorized PR merge follows this review.
- Verification: `./setup.sh` and migration drift passed; 169 backend tests ran with 168 passing and one optional live-model skip; 57 scene/region, 9 auth frontend, and 6 board tests passed; TypeScript/Vite build, JavaScript syntax and whitespace checks passed. The existing furniture chunk warning remains. Canonical `./run-local.sh` served the PR worktree on 5174/8001; the frontend, health/search (12,040 listings), and Visa tester path/hash matched current source. The PR board launched through `bin.mjs`, served all six columns and owner controls, and was stopped. The separate main-checkout board source is byte-identical and serves owner controls. No account data, MFA key, email, Visa request, payment or vendor order was changed or sent. Account/checkout UI routing, shared cart data and guest-room ownership remain open separately.

- 2026-09-19: **Render the owner-based Kanban snapshot without losing legacy task data** ([issue #23](https://github.com/LeeSinLiang/hackmit2026/issues/23), part of PR #21 review card `ae490364-b75e-41ca-8d81-0485d0d78a73`). Added owner model methods and nondestructive migration in `.github/extensions/project-manager/board.mjs`, ported the existing owner controls and canvas adapter in `server.mjs`/`extension.mjs`, and retained atomic writes, lock coordination, watcher cleanup and SSE. Kept legacy phase methods and restored the two known parent-snapshot phase values by task ID. Preserved all current components, IDs, priority, completion and concurrent catalogue/asset-board additions. Updated board README and the indexed Visa handoff.
- Verification: Chrome reproduced every owner card defaulting to Ideation before the fix. Two new regression cases failed before the fix and pass after it; all six board tests pass, including 80 concurrent writes, invalid JSON recovery, lock timeout and cross-process SSE. Syntax/whitespace checks passed. Chrome DevTools recorded HTTP 200 for add/owner/move/toggle/reload/delete, with no runtime exceptions or console errors; the temporary verification card was removed. The canonical user command now starts PID 21301 at `http://127.0.0.1:54221/`, with all three deployed source hashes matching this branch. Old board ports 50668 and 53139 are stopped. The shared board's separate checkout was refreshed without touching its asset-browser edits or other app servers. Copilot host integration is not available locally; its adapter uses the same verified model/server API. This fix makes no auth/Visa changes.

- 2026-09-19: **Resolve catalogue test compatibility with the persistent auth cache** (Kanban `e5258f21-f195-46ce-b869-1df5f4989564`, [issue #20](https://github.com/LeeSinLiang/hackmit2026/issues/20)). Addressed PR #19's P1 feedback with a class-only `override_settings(CACHES=...)` using a dedicated `LocMemCache` in `backend/catalogue/test_compile.py`, plus cache cleanup after each test. Existing test assertions are preserved. Runtime `backend/config/settings.py`, all accounts/checkout/Visa code and the HTML tester, editor and Vite configuration are unchanged from PR #19 head `f5d2d90`. Updated README, `docs/account-mfa-checkout.md`, `docs/handoffs/visa-auth-integration.md` and the existing board task/snapshot. Their INDEX entries already exist; no new documentation file was added.
- Verification: the exact README backend command reproduced **159 tests, five failures, one optional live skip** before the patch; afterward it reports **158 passed, one optional live-model test skipped, zero failures**. It includes all 36 Visa/account/checkout tests and the existing paid-endpoint throttle assertion (`200, 200, 429`). Verified `DatabaseCache → LocMemCache → DatabaseCache` through unittest's complete class cleanup lifecycle. An initial manual diagnostic omitted `doClassCleanups()` and therefore incorrectly expected settings restoration immediately after `tearDownClass()`; using the actual unittest lifecycle passed without further source changes. TypeScript/Vite production build passed with its existing large-chunk warning; whitespace checks passed. Frontend unit tests were not repeated because frontend source is unchanged.
- Freshness/data: the executed Python test module resolves to this Visa worktree's current source; tests create/destroy a separate test database. Inventoried the existing Vite 95944/5174 and Django 95948/8001 processes in this worktree and preserved the independent 8000/5173 lane. No application source changed or required redeployment, and no user account, MFA, mail configuration, email send or Visa submission was touched. Restarted only the previously stopped shared board through its canonical command at `http://127.0.0.1:50668/`; it reads the main checkout's existing owner-coloured board and was updated through its API.
- Delivery: created [replacement PR #21](https://github.com/LeeSinLiang/hackmit2026/pull/21) on William's requested `codex/visa-cache-compatibility` branch, retaining the complete original Visa/auth implementation. The replacement carries the verified correction; PR #19 is superseded without deleting its branch/history. No merge or push to main is authorized. The review and UI/cart/scene-ownership tasks remain active.

- 2026-09-19: **Open the Visa/auth pull request for Codex review.** Created [PR #19](https://github.com/LeeSinLiang/hackmit2026/pull/19) as an open, non-draft PR from `codex/visa-sandbox` into `main`. Its body records the implementation scope, prior test results, five known catalogue/cache test failures, unwired account/checkout routes, remaining cart/ownership contracts and review focus. Verified the PR state, base/head branches and published head through GitHub. Linked the PR from `docs/handoffs/visa-auth-integration.md` and added the open review card to the running Project Plan and this branch's snapshot without changing existing cards.
- Only documentation/work-log/board data changes accompany PR creation; no application code, HTML tester, credentials or runtime changed, and no application tests or external email/Visa calls were repeated. The Codex app's attachment call could not find this side conversation; the GitHub PR itself exists and is directly linked here. No merge or direct push to main was performed.

- 2026-09-19: **Publish the latest Visa/auth handoff sync** (existing Kanban `836acd1c-7b85-46c7-bc0c-22d80438eb34`). Corrected the missing branch push after the second sync: `git push origin codex/visa-sandbox` advanced GitHub from `e11f450` to `e1a4476`. Verified the exact remote head with `git ls-remote`; `main` remained `e6a046e`. GitHub's PR listing returned no PR for this branch. The documented next delivery step is a PR from `codex/visa-sandbox` into `main`, followed by review and merge; pushing the feature branch alone does not update main. See `AGENTS.md` and `docs/handoffs/visa-auth-integration.md` under Branch sharing and Project Plan.
- This publication follow-up changes only `TODO_WILLIAM.md`; application source, the existing HTML tester, teammate checkout and runtime remain untouched. Checked the running board: pull/handoff remains done and account/checkout UI integration remains open, so no board mutation is needed. Existing combined-suite cache failures and UI wiring gaps remain as recorded below; no new runtime test was required for a documentation-only publication record.

- 2026-09-19: **Pull latest main and prepare the Visa/auth handoff — second sync** (Kanban `836acd1c-7b85-46c7-bc0c-22d80438eb34`). Fetched `origin/main` at `e6a046e` and merged it into the existing `codex/visa-sandbox` as William requested. This is maintenance of the requested feature branch, not new work branched from an old feature. Included PRs #13/#15/#16/#17: wall-exception fixes, region solver and unit boundary, nothing-fits/near explanations and task-boundary sync instructions. Resolved the only conflict in this TODO by retaining both our handoff and Saketh's appended sync note. Saketh's completed-work history and William-requested Blender MCP TODO were both preserved. Incoming catalogue/editor/region implementation is unchanged from main; the original Visa HTML and all Visa/auth/checkout implementation are unchanged from the prior feature commit.
- Updated `docs/handoffs/visa-auth-integration.md`, `docs/account-mfa-checkout.md` and `docs/backend/contracts/blender-mcp-agent-handoff.md` to reference the new baseline, reuse the implemented `region/boundary.ts` adapter, and distinguish frontend solver rules from backend enforcement and unfinished overlay/catalogue persistence. Recorded the verified solver foundation on the shared board while keeping broader integration tasks open. Existing INDEX links and new upstream solver-doc index entry were preserved.
- Verification: canonical `./setup.sh` passed with no new migrations and preserved the private key. Backend: **159 tests, the same 5 catalogue DatabaseCache/SimpleTestCase failures, 1 optional live skip**; all others passed. Frontend: **54 scene/region tests + 9 auth tests passed**; TypeScript/Vite build passed with the existing bundle-size warning. No teammate test or security configuration was changed to suppress the failures.
- Runtime: refreshed canonical 5174/8001 stack (Vite 95944, Django 95948; paths resolve to this worktree). Chrome loaded the updated dev catalogue with 12,040 results and the unchanged HTML tester; a synthetic sample validated locally without a Visa submission. Direct/proxied source hashes match `dbb448fea881aab1fee800168e37ad86d027237f0ca2e420a4bd9e73e01042b8`. Account/email/MFA rows and encryption-key fingerprints are unchanged. Preserved main checkout asset work, 5173/8000 servers, and the existing board server. No external email/IDX call was made. This pull creates a local feature-branch merge; it does not push either branch or merge Visa work into main. The previously pushed feature head remains `e11f450` until a later push.

- 2026-09-19: **Prepare Saketh's Blender MCP agent-context handoff.** Added `docs/backend/contracts/blender-mcp-agent-handoff.md`, indexed it, and added William's requested follow-up under `TODO_SAKETH.md` → Next. Open Saketh-owned Kanban card: `6ae44373-c5ee-44b4-867c-99e96937aef8`, **Add Blender MCP-style scene and object context to the agent**. The note distinguishes official Blender Lab MCP (`ff54e4d8`) from community MCP for Blender (`6f992ffb`), describes actual model-facing dictionary/JSON-text/image returns, maps all 26 official and 31 community tools, and specifies stable object IDs, coordinates, units, bounds, metadata/provenance, scene revisions, action/capture feedback, implementation order and acceptance checks. Inspected primary source via the official project's API and GitHub, plus current FRIDAY scene/engine/catalogue contracts. Verified complete tool-name coverage, relative links/indexing, example JSON and its bounds. Documentation and board snapshot only; no live Blender/agent test, application code change, HTML change or runtime refresh needed. Publish on `codex/visa-sandbox` only; the implementation card remains open for Saketh.

- 2026-09-19: **Pull latest main and prepare the Visa/auth handoff** (Kanban `836acd1c-7b85-46c7-bc0c-22d80438eb34`). Pulled main through `44daea7` and then the newly pushed `98ca88d`; retained catalogue/compile, the room editor, capture contracts, and Gaussian research docs. Reconciled our additive backend settings/routes/dependencies, setup, configuration template and documentation. Added `docs/handoffs/visa-auth-integration.md`, indexed it, updated README and account/Visa docs, and copied the current six-component FRIDAY Project Plan into this branch. Delivery is on `codex/visa-sandbox`, not a direct change to main; William explicitly requested the feature-branch push.
- Final scope: per William's correction, removed exploratory edits to the teammate's App, Vite proxy, catalogue tests and board implementation. All are identical to `98ca88d`. The existing HTML tester is byte-for-byte identical to the pre-pull checkpoint (SHA-256 `ed0e0e1b7b9491a88e531a22f95efb46037968174e1851b267be59d10a52e9db`). Account/checkout components and APIs remain available as source for integration; the new editor does not mount their routes. The handoff explains routing, same-origin CSRF/proxy setup, account state, cart contracts, units, approval, and tests.
- Final verification: **36 Visa/accounts/checkout tests passed**, **27 scene and 9 auth frontend tests passed**, migration drift checks and production build passed. The combined backend run was **153 tests, 5 failures, 1 optional live skip**: the five existing catalogue endpoint cases forbid the database access required by our persistent cache. An exploratory TestCase edit made the suite pass but was removed; it is not the delivered result. The existing Three.js bundle warning remains. No new external email, Visa request, payment or vendor order was sent during this reconciliation.
- Runtime/data: canonical `./run-local.sh` refreshed the isolated 5174/8001 stack (Vite 58728, Django 58738); both direct and proxied tester bootstrap paths report the current source hash `dbb448fea881aab1fee800168e37ad86d027237f0ca2e420a4bd9e73e01042b8`. Chrome validated a fresh sample locally after restart and showed **No request sent to Visa**. Account/email/authenticator records and the MFA key match their pre-pull fingerprints. Main 5173/8000 processes 86101/86105 and concurrent asset-browser source changes were preserved. The existing owner-coloured board is running from the main checkout at `http://127.0.0.1:60611/`; its implementation was not altered by this handoff. Recovery stashes `3a3751a` and `69efdb4` remain as backups.
- Board evidence: the pushed catalogue seed/index/facets/compiler foundations and editor/shared-scene foundations are recorded as complete, using teammates' existing logs plus the local checks. Live Elasticsearch was not revalidated here. Kept broad spatial/semantic search, agent orchestration, actual catalogue/cart checkout and UI integration open; added cache compatibility and guest-room ownership tasks. Preserved unrelated asset-browser progress and existing task owners, including the existing Saketh ownership on the checkout-MFA card.

- 2026-09-19: **Clarify authenticator setup and verify the phone MFA workflow** ([issue #12](https://github.com/LeeSinLiang/hackmit2026/issues/12), Kanban `ba819881-005c-47dc-8aaf-c65c7509b6c9`). Reproduced the reported post-verification screen: the QR was intentionally gated behind password confirmation, but the copy did not explain that sequence. Current real account had already completed enrollment. Updated `frontend/src/auth/AuthenticatorSetup.tsx` to say **Get your authenticator QR code**, **FRIDAY account password**, and **Show setup QR code**, with accessible help for a recently reset password and the next scan/code step. Updated `docs/account-mfa-checkout.md`. Authentication policy and storage are unchanged; no new dependency, migration or test-only bypass.
- Verification: through iPhone Mirroring, read the real FRIDAY entry's current code and submitted it in the user's signed-in Chrome security page; the backend accepted it and the UI showed **Identity confirmed**. Did not replace the user's authenticator/recovery codes or record their secret/codes in project artifacts. An isolated SQLite/local-inbox Chrome replay completed signup → email verification → initial setup → wrong-password rejection → QR rendering → wrong-code rejection with QR retained → valid activation → ten recovery codes → acknowledgement → ready → logout → password → MFA challenge → ready. No external email or Visa request was made. All 15 account tests and 9 frontend API/routing tests, production build and whitespace checks passed; only the existing Three.js chunk-size warning remains.
- Delivery: rebuilt and restarted the canonical `./run-local.sh` stack: launcher 38862, Vite 38874/5174 and Django 38880/8001 resolve to this worktree. Served `AuthenticatorSetup.tsx` source-map content exactly matches the file, SHA-256 `8dbb7f55160cd14de21069cd600fd917bbf337ec7a18e787c1a5062ff0635f3e`; the production bundle contains the new copy and proxy health is 200. Account/email/authenticator rows and the encryption key matched their pre-restart fingerprints. The user's Chrome session survived and ends at **You're signed in**, with no QR setup shown. Removed the disposable test runtime/database/key/settings; main 5173/8000 processes 86101/86105 were preserved. Changes remain uncommitted/unpushed.

- 2026-09-19: **Fix trapped email verification and password-reset navigation** ([issue #10](https://github.com/LeeSinLiang/hackmit2026/issues/10), Kanban `8c99e38b-d82d-4077-8337-d4a3055cfc61`). Confirmed that password reset left an older allauth verification process pending, while history/header changes did not cancel it. Added validated `clearAuthSession`/`restartAuth` in `frontend/src/auth/api.ts`; AccountPages now clears the attempt before returning from reset, keeps a safe success/retry screen if cleanup fails, and offers spaced Back to sign in/Create a new account/Reset password controls. AccountApp header navigation and sign-out use the same cleanup. Updated account.css, API tests, backend account tests and account workflow docs. Backend policies, user records, MFA and Gmail SMTP configuration were preserved. No commit/push.
- Verification: reproduced the exact duplicate-signup → pending verification → reset link → saved password → contradictory verification form in Chrome against an isolated SQLite/local-inbox instance. Header Sign in also reproduced the loop. After the fix, the same sequence opened sign-in with its success notice and accepted the new password. Header Sign in, Back to sign in and Create a new account escaped the challenge; an unverified account still required email verification on a fresh login. Existing rate limits were respected (quick retries correctly hit throttling and succeeded after cooldown). All 15 account tests and 9 frontend API/routing tests, Django checks, production build and whitespace checks passed. Regression tests prove CSRF, stale-code rejection, reset/password persistence, unchanged MFA and required MFA after reset. Only the pre-existing Three.js chunk-size warning remains.
- Delivery: refreshed through canonical `./run-local.sh`. One Vite listener PID 27519/5174 and Django PID 27522/8001 resolve to this worktree; the live sign-in browser route and proxied health passed. Served AccountApp/AccountPages/api source maps and CSS exactly match current files; combined SHA-256 `bcb12ae4b2dd2e2575361564287c5e5db7afe7a5a9600492a7a3aa06acbea765`. Stopped the disposable 5314/8101 stack and removed only its temporary database/settings/key. The main 5173/8000 stack and real account database remain intact. No external email or Visa call was sent for the fix.

- 2026-09-19: **Refactor sign in, signup, and authenticator onboarding** (Kanban `1cd469d5-b69a-4391-958c-470426341fd2`). Added separate `/account/sign-in` and `/account/create` pages. New/unverified users verify email, accounts without MFA see one-time QR setup and recovery-code acknowledgement, and enrolled users enter an authenticator/recovery code after their password. Completed setup leads to a simple ready screen; recovery management and email reconfirmation live at `/account/security`. Added `frontend/src/auth/flow.ts`, `flow.test.mjs`, `AuthenticatorSetup.tsx`; updated AccountApp, AccountPages, AuthProvider, SecuritySettings, account.css, README and docs/account-mfa-checkout.md. Preserved Gmail SMTP, backend policies, reset/reconfirmation fixes, existing account storage and schema. No commit or push.
- Verification: 36 backend tests, 7 frontend API/routing tests, Django checks, migration drift checks, build and whitespace checks passed. Fixed a JSX closing-brace error found during the first build; the final build passed with the existing Three.js size warning. Chrome checked separate create/sign-in/reset pages, wrong-code rejection, returning MFA login, QR rendering and activation on the disposable workflow account, 10 recovery codes and acknowledgement, the separate settings route, and checkout navigation. A checkout-heading locator initially used the wrong text; a fresh DOM snapshot confirmed the correct page. The user's Gmail account and MFA were not modified. The disposable account was signed out at handoff; a physical phone scan remains unverified. No new external email or Visa submission was performed.
- Storage/runtime: retained ignored `backend/db.sqlite3` (three existing accounts), Django password hashes and encrypted MFA records. Pre/post-restart digests prove account, email verification and authenticator records plus `backend/.runtime/mfa.key` survived intact; actual password/MFA login also passed after restart. Canonical `./run-local.sh` launcher PID 21213, Vite PID 21225/5174, Django PID 21229/8001 all resolve to this worktree. Six served auth source maps exactly match current files; combined source SHA-256 `c42423075d838ba805adbf1bcacaceae035105110df8e30572ba0375da45fbd1`. Main 5173/8000 servers were preserved. Shared Kanban and main-thread handoff reflect this completed UI work; teammate cart integration and phone verification remain next steps.

- 2026-09-19: **Set up Gmail SMTP for demo verification emails** (Kanban `c136f9c5-d1ab-4624-bf00-7ba91fb5041f`). After the user completed Google's identity check and requested continuation, used Computer Use to create **FRIDAY HackMIT SMTP** in the existing Gmail account; no new Google account was created. Gmail STARTTLS authentication returned 235. Set the private `.env` to `smtp.gmail.com:587`, TLS, the existing Gmail sender and its app password; local inbox remains disabled. Preserved prior Resend mail settings in ignored `backend/.runtime/resend-smtp.json` for rollback, with no active Resend mail backend. Runtime credential files and `.env` are mode 0600 and ignored. No secrets or codes are in this record.
- Verification: rebuilt the frontend (existing Three.js size warning), passed Django checks and whitespace checks, and restarted via canonical `./run-local.sh`. The actual account UI sent a confirmation through Gmail SMTP; Gmail showed it in **Inbox** (also Sent) at 21:54:49 UTC. Submitted its received code in the browser, which showed **Email confirmed using the new code.** Account/password/MFA were preserved. This configuration-only switch did not change authentication code; the prior 13 account and 3 API-error tests were not rerun. Both provider secrets were absent from tracked/trackable files. Delivery to a separate recipient was not directly tested.
- Runtime: one Vite listener on 5174, PID 12699, and one Django listener on 8001, PID 12705; both working directories resolve to this feature worktree. Proxy health returned 200. The user's account tab is open with successful email confirmation. Original main-checkout PIDs 86101/5173 and 86105/8000 remain intact. Updated `README.md`, `INDEX.md`, and `docs/account-mfa-checkout.md` to describe Gmail as current and Resend as rollback. No commit/push.
- Removed the now-obsolete **Verify Resend sending domain for other recipients** card (`3ba28d2d-2fd7-42de-b8c6-98e0b9dfd2e3`): a domain is not required for this Gmail demo sender. Domain verification remains relevant only if the team later returns to Resend for broader sending. Google's sending limits and mailbox filtering still apply.
- 2026-09-19: **Deliver and verify account emails through Resend SMTP** ([issue #7](https://github.com/LeeSinLiang/hackmit2026/issues/7), Kanban `cff2cce7-a23d-449e-865c-2963ddf25d9c`, done). Superseded the earlier local-inbox-first choice. Configured the ignored mode-0600 `.env` for Resend STARTTLS/587, send-only credentials and `FRIDAY <onboarding@resend.dev>`, with the live local inbox disabled. SMTP authentication succeeded. The first real confirmation arrived in Gmail Inbox at 21:42:30 UTC; the replacement arrived in Spam at 21:44:20 UTC. The earlier code was rejected, and the newest Gmail code was accepted at 21:44:54 UTC; the browser showed **Email confirmed using the new code.** No credential or code is retained in this record.
- Added `frontend/src/auth/EmailConfirmation.tsx` under `SecuritySettings.tsx` for a signed-in user's fresh primary-email challenge. Added `/api/accounts/email/confirm/` in `backend/accounts/views.py`/`urls.py`, reusing allauth's verification process and input validator. Its normal signup verification returned 500 for an already verified address; this authenticated reconfirmation consumes a valid challenge without changing password, verified flag or MFA. Normal signup remains on allauth's original endpoint. Updated `backend/accounts/tests.py`, `README.md`, and `docs/account-mfa-checkout.md`.
- Verification: all 13 account tests, 3 API-error tests, Django checks, frontend build and whitespace checks passed. Tests cover wrong/expired/replaced/reused codes, maximum attempts, CSRF, anonymous and separate authenticated browser sessions, and unchanged password/MFA records. Gmail-to-browser live confirmation succeeded; current account and local-inbox-disabled state survived the final build/restart. Resend credential scan found no key in tracked/trackable files; `.env` is ignored and private. No commit or push requested.
- Runtime: canonical `./run-local.sh`, Vite5174 PID 7650 and Django8001 PID 7652, both resolving to this worktree. Served source maps match current `EmailConfirmation.tsx` SHA-256 `6c4dd5a699df83c01850615c3fd0cee1d7394e2c84500076580bd408d12773d4` and `SecuritySettings.tsx` `ed8e7ccf1e572b5f0696562de85f248cf0edd2bcd3d7ae829dd71d1580664733`; current reset/API sources also match. Backend account views/routes/settings fingerprint: `e3322e83b5119008fb32794baa1a0a8bc629c5cb2ad74a5cb1432e58e44ce215`. Original main-checkout listeners 86101/5173 and 86105/8000 remain intact. General-recipient email support and consistent Inbox placement remain open; the latest message was in Spam.
- 2026-09-19: **Fix duplicate signup and misleading authentication errors** ([issue #6](https://github.com/LeeSinLiang/hackmit2026/issues/6), Kanban `52890a38-b58e-41bd-902f-8f9649c69b83`, marked done). Added missing `account_reset_password` frontend URL in `backend/config/settings.py`; `AccountPages.tsx` honors reset mode even with pending verification; `api.ts` explains 429/500 responses. Added the duplicate-signup regression in `backend/accounts/tests.py` and three API-error tests in `frontend/src/auth/api.test.mjs`; updated the account guide. Exact browser reproduction succeeded before and after the canonical restart: duplicate signup produced Account Already Exists mail without a server error; the reset route opened its form and requested a reset email. The test used localhost to preserve the user's separate 127.0.0.1 session; the emitted reset URL's path/query were checked on that isolated host. Cleared the disposable challenge, closed its tab, and restored the original user account. No user password or authenticator was changed.
- Verification: 11 account tests, 3 API-error tests, Django checks, frontend build and whitespace checks passed. Restoring the old missing-URL config in an isolated test reproduces the exact `ImproperlyConfigured` failure. Runtime via `./run-local.sh`: frontend PID 3129/5174 and Django PID 3131/8001 resolve to this worktree. Served source maps match `AccountPages.tsx` SHA-256 `c427c7e0e166a1b3add35848c9da29874493a80a6e5e640cb82908e6126aebc5` and `api.ts` `92805f7ace8e09bd1b2e8d955fae1437a71b770f7e71b49da94f776226bc5d3e`. Original 5173/8000 stack retained. Changes remain uncommitted and unpushed.
- 2026-09-19: Stopped the QR investigation after the user clarified that the issue was missing email. Confirmed separate Chrome/test-account and in-app/user-account sessions; no authenticator was changed. Restored the user's in-app tab to the original `127.0.0.1` session, which shows **Add an authenticator**. Removed the superseded QR investigation card.
- 2026-09-19: **Clarify local email verification and inbox delivery** ([issue #4](https://github.com/LeeSinLiang/hackmit2026/issues/4), shared Kanban card `c97919d8-15ff-441a-a9fe-26adfeb7b53d`). Confirmed that `LocalInboxBackend` saves session-scoped test mail and no SMTP host/user/password is configured. Gmail search found no message from the application sender. The user's actual local verification code was accepted in the in-app browser and advanced to **Add an authenticator**. This verifies the local challenge workflow, not ownership of an external inbox.
- Changed `frontend/src/auth/AccountPages.tsx` to identify local delivery before signup and during verification, reset, and resend. `AccountApp.tsx` and `LocalInbox.tsx` open the inbox during verification and distinguish 30-minute message retention from 10-minute code expiry. Updated `docs/account-mfa-checkout.md`. Authentication policy, email backend, credentials, session isolation, expiry, and schema are unchanged; no commit or push requested.
- Verification: frontend production build passed (existing Three.js chunk warning); all 10 account tests, Django checks, and whitespace checks passed. After restarting the canonical `./run-local.sh` stack, a separate localhost browser session completed signup, showed **Check the local test inbox** with the inbox open, loaded its actual code, and passed verification. The test session was signed out and closed. The user's original 127.0.0.1 session remains verified and open at authenticator setup.
- Runtime identity: one isolated Vite listener on 5174 (PID 91975) and one Django listener on 8001 (PID 91977), both resolving to this worktree. Vite source-map contents exactly match all three edited TSX files. SHA-256: `AccountPages.tsx` = `06adf424a48d9e8d08d4a5addbeeebcfd658bddb13b6101f6e75911b95cb29f8`; `AccountApp.tsx` = `307c05d1df54fcf66d13c37a1fd3ab8acfb32d30c89dc116e4953d176e9cf0e1`; `LocalInbox.tsx` = `8ad24b4dd6f9c6490f5ed28e87dbc2a8158e53854c2697d4ade7a9311fc62cb9`. The main checkout's separate 5173/8000 application runtime and concurrent asset edits were preserved. External email delivery remains unconfigured and unverified; Gmail inbox access does not provide the app with an outgoing mail provider.
- 2026-09-19: Implemented the approved signup → local email verification → authenticator enrollment/login/recovery → checkout-specific MFA → real IDX sandbox workflow. Added backend/accounts (allauth policy, encrypted secret/recovery seed adapter, session-scoped local inbox, QR rendering, recent-MFA management gate, key initialization and tests), backend/checkout (fixture catalogue, strict request serializers, immutable snapshots, single-use approval, atomic submission, receipt and tests), frontend/src/auth and frontend/src/checkout. Updated settings, URLs, dependency lockfile, Vite auth proxy, app entry, setup, private runtime ignore rule, environment template, README, INDEX, and docs/account-mfa-checkout.md. Replaced the old custom email/PyOTP plan in docs/visa-sandbox.md with current status and remaining work.
- Latest verification: 33 distinct backend tests passed across api/visa/accounts/checkout; Django checks and migration drift checks passed; TypeScript/Vite build and git diff --check passed. npm ci reported zero known dependency vulnerabilities. Existing Three.js chunk-size warning remains. Live-secret scan found no Visa key/secret/private key or MFA encryption key in tracked/trackable files; runtime key is ignored and private.
- Browser workflow in Chrome: actual signup, wrong email code rejected, local verification email consumed, password confirmation before QR enrollment, independent RFC6238 code accepted, recovery codes displayed once, logout, expired TOTP rejected, MFA login, itemized USD 504.00 sample review, explicit checkout checkbox plus fresh TOTP, real Visa response, persisted receipt after reload. Also tested password reset through the local inbox, MFA still required afterward, recovery-code login, reused recovery code rejection, and login with a fresh TOTP. Fixed handling of allauth's successful-but-unauthenticated password-reset 401 response; replay passed. No pre-verified account or MFA bypass was used. Phone-app scanning was not performed.
- Real integrated IDX evidence: checkout 84428212-2ba2-4c62-aec6-0cb79f1fb8f3, transaction 67a9848a-79e1-4ff6-a9af-e7b99013f244, HTTP 200, encrypted response and matching transaction ID, state accepted. Sanitized receipt in docs/evidence/account-checkout-2026-09-19.json. Approval belongs to the account, was consumed once, and was not sent to Visa as an invented tag. No payment authorization or order endpoint exists.
- Final runtime: stopped only the old isolated stack, reran ./setup.sh and the frontend build, and relaunched ./run-local.sh. Fixed its port preflight to permit closed TIME_WAIT sockets with SO_REUSEADDR; a second simultaneous launch still fails on the occupied port. Exactly one isolated frontend listens on 5174 (PID 88187) and backend on 8001 (PID 88192); both working directories and backend executable resolve to this worktree. Vite-served source maps for account/reset, MFA settings, and checkout match current source bytes. Source fingerprint for account/checkout/config/frontend modules: b39c5733b0202022b6a0b8cc486e20e0d0570e44ca171ba1d2b51bdd9f68f1fb. Backend health, account proxy, headless proxy, and saved browser receipt passed after restart. Main checkout files and its server processes were not modified by this work.

### Initial standalone IDX tester

- 2026-09-19: Built the synthetic IDX test harness for William on `codex/visa-sandbox` in `/Users/williamxu/.codex/worktrees/visa-sandbox/hackmit2026`. Added backend/visa (strict supported schema, fixed sandbox client, X-Pay, MLE, attempt model/migration, loopback/debug/CSRF gates, explicit approval, payload hashes, five-minute expiration, atomic one-send guard, saved evidence), and templates/visa/sandbox_test.html. Updated backend settings/routes/dependencies/lockfile and .env.example. Added docs/visa-sandbox.md with the detailed remaining account/MFA/checkout/payment/order plan and indexed it with sanitized evidence.
- With William's explicit approval, registered a dedicated public wrapping key in the existing Visa project's X-Pay settings and decrypted its existing shared secret. Private credentials are outside Git in a mode-0700 directory with mode-0600 files. Existing API credential and MLE certificate were retained. Downloaded the public certificates, normalized a private-key copy, and verified its public key matches the active client certificate; preserved the original download.
- Live verification: two actual POST requests to https://sandbox.api.visa.com/vidx/v1/requests returned HTTP 200, encrypted responses, matching transID values, and idxMatchKey values. The optional DCAP request returned indicator 1 (not eligible) and an acquirer-region warning. Neither payment authorization nor vendor order creation was attempted. Full sanitized receipts are in docs/evidence/visa-idx-2026-09-19.json. The live calls also verified the X-Pay signing path `v1/requests` and signing of the final encrypted body.
- Automated verification: all 14 api/visa tests passed, Django checks passed, no missing migrations, frontend TypeScript/Vite build passed, and git diff --check passed. Tests cover strict fields/types and fixture restrictions, HMAC reference/body binding, JWE round trip/tampering, encrypted/correlated responses, error/timeout behavior, CSRF, local/debug gates, explicit approval/hash/expiry/session ownership, and duplicate sends. No credentials or private-key material appeared in tracked/trackable files during an exact-value scan. Existing Three.js chunk-size build warning remains.
- Browser verification in Chrome through the frontend proxy: configuration loaded, unsupported `mfaVerified` tag rejected locally, review and approval worked, both live receipts rendered, persisted receipt survived reload, a fresh sample cleared prior success, and editing approved JSON disabled sending. Visual inspection and browser console check passed. One browser locator inspection timed out; a fresh DOM inspection and label-based interaction completed the check.
- Runtime: launched with the canonical ./run-local.sh in this worktree. One tester backend listens on 8001 (PID 66902 at verification) and its frontend on 5174 (PID 66894); both process working directories match this worktree. Direct and proxied bootstrap source SHA-256 both match current-source `dbb448fea881aab1fee800168e37ad86d027237f0ca2e420a4bd9e73e01042b8`. Live page: http://127.0.0.1:5174/api/visa/sandbox-test/. Kept the successful result open in Chrome.

## Blockers / handoff

- Delivery source is the `codex/visa-sandbox` feature branch and its isolated checkout; main is not merged or pushed by this work. The shared board snapshot and handoff travel with the branch. See `docs/handoffs/visa-auth-integration.md` for the current baseline and integration steps.
- The new editor's account routes/proxy remain unwired by request, and the combined suite has the five documented catalogue cache/test failures. These are explicit handoff tasks, not completed integration claims.
- Account/MFA and sample checkout are implemented and locally verified. External email delivery to William's Gmail and acceptance of its new code are now verified. General-recipient email delivery, physical authenticator-app scanning, the shared catalogue/cart, production deployment, payment authorization, and merchant ordering have not been verified or delivered. The separate developer tester checkbox remains distinct from the new account checkout MFA. Live IDX acceptance does not establish DCAP eligibility, issuer authorization, or visibility into Visa's internal stored fields.

## Note from Saketh's lane — `AGENTS.md` changed (2026-09-19)

Appended by Saketh's agent so the edit is not silent; nothing above was changed. `AGENTS.md` has a new section, **Sync before starting work**:

- Pull at task boundaries only — at task start and after a merge to `main` — never mid-task. The existing "do not fetch mid-task unless asked" rule stands and now says so explicitly.
- At each boundary: `git checkout main && git pull`, `./setup.sh` (other lanes add Python and frontend dependencies), full test suite and frontend build on that fresh `main`, branch off `main` never off a previous feature branch, re-read `AGENTS.md` and `INDEX.md`.
- If clean `main` is red, stop and tell your owner; it is a whole-team problem.
- When merging a stacked PR, retarget the dependent PR to `main` before deleting the base branch. Deleting it first closes the dependent PR (it happened to #3).

## Note from Saketh's lane — `AGENTS.md` changed again (2026-09-19)

Appended by Saketh's agent; nothing above was changed. New section **Clean up after yourself**: stop every process you start and verify with `lsof` before reporting; use your own ports, never 8000/5173 (`BACKEND_PORT` / `FRONTEND_PORT`); scratch files go under the gitignored `.scratch/`; check a push succeeded before acting on it (`set -o pipefail`).

## Note from Saketh's lane — shared SQLite settings changed (2026-09-20)

Appended by Saketh's agent; nothing above was changed. `backend/config/settings.py` now opens SQLite with `transaction_mode: IMMEDIATE`, `journal_mode=WAL` and `timeout: 20`. Reason: accounts/MFA, your `DatabaseCache` table and scene saves share one file, and under concurrent load 70% of scene saves returned 503 (`scripts/hammer_storage.py` reproduces it; 0 after the change). All 174 backend tests pass, including accounts, checkout and visa. If sign-in latency under load matters to you, that script also exercises `/_allauth/browser/v1/auth/login`. Also relevant to "guest-room transfer and account scene ownership": room presets key a layout as `<session key>:<room id>`.

## Note from Saketh's lane — `AGENTS.md`: after every merge, run the suite on fresh `main` (2026-09-20)

Appended by Saketh's agent; nothing above was changed. It binds all four agents. New section **Trust the remote, not your terminal**:

- After a push: never pipe a git command whose exit code matters; check `git rev-parse HEAD` equals `git rev-parse origin/<branch>`; a behavioural claim in a PR description needs a test behind it.
- After every merge: `git checkout main`, `git pull`, delete the merged branch locally, **run the full suite and the frontend build on that fresh `main`**, then branch fresh. Merging updates GitHub, not your clone. A PR is tested against the `main` it was cut from, not the one it lands in, so two green PRs can take `main` down together with no conflict; it has happened here once already. Whoever merges runs the suite at once, and a red `main` is a whole-team stop, announced before anything else.

## Note from Saketh's lane — three catalogue views are now explicitly anonymous (2026-09-20)

Appended by Saketh's agent; nothing above was changed, and none of your files were touched. `search`, `compile_program` and `transcribe_audio` in `backend/catalogue/views.py` now declare `authentication_classes = []`. Why it concerns you: since accounts landed, a signed-in shopper's POST to `/api/compile` was refused by DRF's `SessionAuthentication` for a missing CSRF token before the view ran. These three are public and stateless, so they are made anonymous rather than taught to send a token. Two consequences: `request.user` is always anonymous inside them, so **do not hang account or scene-ownership logic off these views**; and their throttles are per client address for everyone, where before a signed-in session bypassed `AnonRateThrottle` entirely. Scene, account and checkout endpoints are unchanged; a test asserts a signed-in scene write without a CSRF token is still 403.

## Note from Saketh's lane — `AGENTS.md`: `main` freezes after the rehearsal (2026-09-20)

Two additions, from Saketh. Please read the new "Merging close to the demo" section.

- **Freeze.** Once the demo has been rehearsed end to end on the presenting machine, `main` takes only fixes to things that break that rehearsed run. No polish, no refactors. If unsure whether the freeze has started, ask before merging.
- **Chains stop on the first failure, and a status is quoted, not remembered.** `&&` not `;`, no pipe that eats an exit code, do not trust `set -e` blindly, and paste the `Ran N tests … OK` line you actually read. The full backend suite is `manage.py test` with **no app labels** (206 tests); a label list silently skips apps.
- **Said out loud, because this is what the freeze is for:** tonight #34 went into `main` without the suite being run and left `main` red, seven backend failures, until #37. Nobody was careless in an unusual way; it was a reasonable-looking asset PR late at night. That is exactly the change the freeze rule exists to stop from landing an hour before we present. Whoever merges runs the full suite on fresh `main` at once, and says so with the numbers.

## Note from Saketh's lane — CI now runs on every pull request (2026-09-20, 00:55)

`.github/workflows/ci.yml` is on `main`. Every PR gets two checks within about a minute: **Backend** (`manage.py test` with no app labels, plus `manage.py check`) and **Frontend** (`npm test`, `npm run build`). No secrets, nothing to configure.

- **A red X on your PR means do not press Merge.** Tonight #34 and #42 both went into `main` without the suite and left it red; each was caught only because someone happened to look. This is the thing that stops that.
- It is **advisory**, not a required check: it will never block an emergency fix.
- It does not replace running the suite on fresh `main` after you merge. CI tests your PR against the `main` it was cut from.
- `npm test` is now the whole frontend suite (it used to skip the two auth test files), and the backend suite is `manage.py test` with **no labels** (`test api catalogue` skips three apps). README has the three commands.
- When you report a result, name the command and quote its output line, e.g. "`python manage.py test` → Ran 217 tests in 6.951s / OK (skipped=3)", not "tests pass".

## Note from Saketh's lane — a rare, real race in the TOTP test helper (2026-09-20, 01:40)

Not changed by me; it is your test code. It matters now because CI runs on every PR and this will show up as an occasional red X that is nobody's fault.

- **Seen:** `manage.py test` → `Ran 221 tests in 13.755s / FAILED (failures=1, skipped=3)` on a docs-only branch: `checkout.tests.CheckoutWorkflowTests.test_expired_approval_and_modified_snapshot_rejected`, failing inside `accounts/tests.py` `enrolled()` with `400 {"message": "Incorrect code.", "code": "incorrect_code"}`. The same code, run again: `Ran 221 tests in 12.344s / OK (skipped=3)`; that test alone: `Ran 1 test in 0.385s / OK`.
- **Mechanism, from the code:** `totp(secret)` computes `int(time.time()) // 30` when the test calls it; the server checks the code against its own clock a few milliseconds later, and no step tolerance is configured (`MFA_TOTP_TOLERANCE` is not set, so it is 0). If a 30-second boundary falls between those two moments, the test's code is one step old and is refused. Roughly 0.1% per TOTP submission, a few percent per full run; one hit in about twenty-five runs tonight fits.
- **Suggested fix, test-only, your call:** either `@override_settings(MFA_TOTP_TOLERANCE=1)` on `AccountTestCase` (allauth then accepts the neighbouring step), or have `totp()` wait out the boundary: `if time.time() % 30 > 29: time.sleep(1.1)` before computing. The first is one line and does not slow anything.
